---
phase: 01-foundation-setup
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - lib/data/remote/dto/course_search_response_dto.dart
  - lib/data/remote/dto/course_search_response_dto.freezed.dart
  - lib/data/remote/dto/course_search_response_dto.g.dart
  - lib/data/remote/dto/course_detail_response_dto.dart
  - lib/data/remote/dto/course_detail_response_dto.freezed.dart
  - lib/data/remote/dto/course_detail_response_dto.g.dart
  - lib/data/remote/api/interceptors/auth_interceptor.dart
  - lib/data/remote/api/golf_course_api.dart
  - lib/data/remote/api/golf_course_api.g.dart
  - lib/data/remote/api/dio_provider.dart
  - lib/data/remote/api/dio_provider.g.dart
  - lib/data/remote/api/golf_course_api_provider.dart
  - lib/data/remote/api/golf_course_api_provider.g.dart
  - lib/infrastructure/repositories/course_repository_impl.dart
  - lib/infrastructure/repositories/course_repository_provider.dart
  - lib/infrastructure/repositories/course_repository_provider.g.dart
  - lib/features/setup/providers/course_search_query_provider.dart
  - lib/features/setup/providers/course_search_query_provider.g.dart
  - lib/features/setup/providers/course_search_results_provider.dart
  - lib/features/setup/providers/course_search_results_provider.g.dart
  - lib/features/setup/providers/selected_course_provider.dart
  - lib/features/setup/providers/selected_course_provider.g.dart
  - lib/features/setup/widgets/handicap_input.dart
  - lib/features/setup/widgets/course_search_field.dart
  - lib/features/setup/widgets/course_result_tile.dart
  - lib/features/setup/widgets/course_card.dart
  - lib/features/setup/widgets/api_key_error_state.dart
  - lib/features/setup/setup_screen.dart
autonomous: true
requirements:
  - FOUND-04
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
must_haves:
  truths:
    - "User can type a decimal handicap index and it persists across app restarts via Hive"
    - "User can type 2+ chars in the search field and after 400ms a list of golfcourseapi.com results appears"
    - "User can tap a result and the full course detail is fetched, cached to Hive, and displayed in the Course Card"
    - "On next Setup open, the last-used course id is read from Hive and the cached CourseModel is pre-populated without a network request"
    - "When --dart-define=GOLF_API_KEY is empty OR API returns 401, the user sees the full-screen API_KEY error state"
  artifacts:
    - path: "lib/data/remote/api/golf_course_api.dart"
      provides: "Retrofit @RestApi interface with /search and /courses/{id} endpoints"
      contains: "@RestApi"
    - path: "lib/data/remote/api/interceptors/auth_interceptor.dart"
      provides: "AuthInterceptor that rejects on empty key and surfaces 401 as typed exception"
      contains: "API_KEY_MISSING"
    - path: "lib/infrastructure/repositories/course_repository_impl.dart"
      provides: "CourseRepositoryImpl with searchCourses, getCourseDetail (writes through to Hive), getCachedCourse, cacheCourse"
      contains: "class CourseRepositoryImpl"
    - path: "lib/features/setup/setup_screen.dart"
      provides: "ConsumerStatefulWidget assembling HandicapInput, CourseSearchField, results list, CourseCard"
      contains: "ConsumerStatefulWidget"
    - path: "lib/features/setup/widgets/api_key_error_state.dart"
      provides: "Full-screen ApiKeyErrorState widget per UI-SPEC Component 7"
      contains: "API KEY NOT CONFIGURED"
  key_links:
    - from: "lib/features/setup/widgets/course_search_field.dart"
      to: "lib/features/setup/providers/course_search_query_provider.dart"
      via: "ref.read(courseSearchQueryProvider.notifier).set(value) in TextField onChanged"
      pattern: "courseSearchQueryProvider"
    - from: "lib/features/setup/providers/course_search_results_provider.dart"
      to: "lib/data/remote/api/golf_course_api_provider.dart"
      via: "ref.watch(golfCourseApiProvider).searchCourses(query) after 400ms debounce"
      pattern: "searchCourses"
    - from: "lib/features/setup/setup_screen.dart"
      to: "lib/data/local/preferences/hive_player_prefs_provider.dart"
      via: "ref.read(hivePlayerPrefsProvider).handicapIndex pre-populates input"
      pattern: "hivePlayerPrefsProvider"
    - from: "lib/infrastructure/repositories/course_repository_impl.dart"
      to: "lib/data/local/preferences/hive_course_box.dart"
      via: "getCourseDetail writes through to hiveCourseBox after API success (SETUP-04)"
      pattern: "writeCourse"
---

<objective>
Stand up the golfcourseapi.com integration end-to-end and build the Setup screen UI. After this plan, a user with a valid API key can search for a course, select it, see the loaded Course Card, and watch their handicap and last-used course persist across launches. The START ROUND button is rendered but tapping it is wired in Plan 04.

Purpose: Deliver the second vertical slice. Retrofit interface + Dio with AuthInterceptor + CourseRepositoryImpl (write-through to Hive) + 400ms debounced search + Setup screen widgets per UI-SPEC. FOUND-04 API key error state is fully functional. SETUP-05 missing-rating banner is deferred to Plan 05 (where it lives next to FMTC progress).

Output: 6 remote-layer files (DTOs + AuthInterceptor + Retrofit interface + Dio provider + GolfCourseApi provider), 1 infrastructure impl (CourseRepositoryImpl + provider), 3 screen-level providers (search query, search results, selected course), 5 widgets per UI-SPEC, plus the SetupScreen rebuild. Build_runner runs once at the end of each major task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phase-01/01-CONTEXT.md
@.planning/phase-01/01-RESEARCH.md
@.planning/phase-01/01-PATTERNS.md
@.planning/phase-01/01-UI-SPEC.md
@.planning/phase-01/01-SKELETON.md

<interfaces>
<!-- Contracts the executor needs. Pulled from Plan 01/02 outputs. -->

From lib/app/constants.dart (after Plan 02):
- `AppConstants.golfApiBaseUrl == 'https://api.golfcourseapi.com/v1'`
- `AppConstants.golfApiKey == String.fromEnvironment('GOLF_API_KEY')`
- `AppConstants.playerPrefsBox`, `AppConstants.courseCacheBox`, `AppConstants.tileCacheStoreName`

From lib/domain/models/course_model.dart (Plan 02):
- `@freezed CourseModel` with `id, clubName, courseName, courseRating?, slope?, par, holes: List<HoleModel>`, plus `fromJson`/`toJson`.

From lib/domain/repositories/course_repository.dart (Plan 02):
- `abstract class CourseRepository` with `searchCourses(String query) → Future<List<CourseModel>>`, `getCourseDetail(String id) → Future<CourseModel>`, `getCachedCourse(String id) → CourseModel?`, `cacheCourse(CourseModel course) → void`.

From lib/data/local/preferences/hive_player_prefs.dart (Plan 02):
- `double? get handicapIndex`, `void setHandicapIndex(double)`, `String? get lastUsedCourseId`, `void setLastUsedCourseId(String)`.

From lib/data/local/preferences/hive_course_box.dart (Plan 02):
- `void writeCourse(String courseId, CourseModel)`, `CourseModel? readCourse(String courseId)`.

From lib/theme/* (Plan 01):
- `BrdyColors.{background, surface, onSurface, onSurfaceMuted, accent, onAccent, destructive, divider}`, `BrdySpacing.{xs,sm,md,lg,xl,x2l}`, `BrdyTextTheme.textTheme`.

From lib/app/router.dart (Plan 01):
- `/setup` route renders `SetupScreen`; Plan 03 replaces the SetupScreen placeholder body.

API endpoint contract — [ASSUMED per RESEARCH.md §7 A1/A2/A3/A4]:
- `GET /search?search_query=<name>` → JSON `{ "courses": [ { "id", "club_name", "course_name", "location": {city,country}, "course_rating", "slope_rating", "par", "holes": <int count> } ] }`
- `GET /courses/{id}` → JSON `{ "course": { ..., "holes": [ { "hole_number", "par", "stroke_index", ... GPS fields } ] } }`
- Auth header: `Authorization: Key <KEY>`
- These shapes are unverified — Task 2 includes a one-shot live API call to log the raw response and adjust field names if needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create remote layer — DTOs, AuthInterceptor, Retrofit GolfCourseApi, Dio + GolfCourseApi keepAlive providers</name>
  <files>lib/data/remote/dto/course_search_response_dto.dart, lib/data/remote/dto/course_detail_response_dto.dart, lib/data/remote/api/interceptors/auth_interceptor.dart, lib/data/remote/api/golf_course_api.dart, lib/data/remote/api/dio_provider.dart, lib/data/remote/api/golf_course_api_provider.dart, lib/data/remote/dto/course_search_response_dto.freezed.dart, lib/data/remote/dto/course_search_response_dto.g.dart, lib/data/remote/dto/course_detail_response_dto.freezed.dart, lib/data/remote/dto/course_detail_response_dto.g.dart, lib/data/remote/api/golf_course_api.g.dart, lib/data/remote/api/dio_provider.g.dart, lib/data/remote/api/golf_course_api_provider.g.dart</files>
  <read_first>
    - .planning/phase-01/01-RESEARCH.md §5 (Dio + Retrofit setup, AuthInterceptor structure with API_KEY_MISSING and API_KEY_INVALID typed messages), §7 (assumed response shape — verify on first live call), §9 (build_runner workflow), Pitfall P-10 (`part` directive required)
    - .planning/phase-01/01-PATTERNS.md (course_search_response_dto.dart excerpt with `@JsonKey(name: 'snake_case')` annotations, golf_course_api.dart Retrofit pattern, auth_interceptor.dart pattern, keepAlive provider pattern)
    - .planning/phase-01/01-CONTEXT.md (D-04 — full SETUP-03 dataset cached on load)
    - CLAUDE.md (`dioProvider` and `golfCourseApiProvider` MUST be `@Riverpod(keepAlive: true)`)
    - pubspec.yaml (verify `dio: ^5.4.3+1`, `retrofit: ^4.1.0`, `json_annotation: ^4.9.0`, dev `retrofit_generator: ^8.1.0`)
  </read_first>
  <behavior>
    - Test: `AuthInterceptor.onRequest` with `AppConstants.golfApiKey == ''` calls `handler.reject(DioException(..., message: 'API_KEY_MISSING'))` and the next interceptor is NOT called.
    - Test: `AuthInterceptor.onError` with `err.response?.statusCode == 401` rejects with `message: 'API_KEY_INVALID'`.
    - Test: `GolfCourseApi(Dio()).searchCourses('Royal')` returns a Future that emits a DioException with `API_KEY_MISSING` when the env key is empty (proves wiring before live verification).
    - Test: `flutter analyze` exits 0 after build_runner.
  </behavior>
  <action>
    Create the two DTO files using Freezed + json_serializable. Field names use camelCase in Dart with `@JsonKey(name: 'snake_case_name')` mapping the API's snake_case keys (per A4 in RESEARCH.md — [ASSUMED]).

    `lib/data/remote/dto/course_search_response_dto.dart` — declares two Freezed classes in one file. Both need `part 'course_search_response_dto.freezed.dart';` and `part 'course_search_response_dto.g.dart';`. Imports: `package:freezed_annotation/freezed_annotation.dart`.
    - `@freezed class CourseSearchResponseDto with _$CourseSearchResponseDto` factory with `required List<CourseSearchResultDto> courses;`, fromJson factory.
    - `@freezed class CourseSearchResultDto with _$CourseSearchResultDto` factory: `required String id;`, `@JsonKey(name: 'club_name') required String clubName;`, `@JsonKey(name: 'course_name') required String courseName;`, `@JsonKey(name: 'course_rating') double? courseRating;`, `@JsonKey(name: 'slope_rating') int? slopeRating;`, `required int par;`, `LocationDto? location;`. Plus `fromJson` factory.
    - `@freezed class LocationDto with _$LocationDto` factory: `String? city;`, `String? country;`. Plus `fromJson`.

    `lib/data/remote/dto/course_detail_response_dto.dart` — declare:
    - `@freezed class CourseDetailResponseDto` wrapping `required CourseDetailDto course;`.
    - `@freezed class CourseDetailDto` with same identity fields as search plus `required List<HoleDto> holes;`.
    - `@freezed class HoleDto` factory: `@JsonKey(name: 'hole_number') required int holeNumber;`, `required int par;`, `@JsonKey(name: 'stroke_index') int? strokeIndex;`, `@JsonKey(name: 'tee_lat') double? teeLat;`, `@JsonKey(name: 'tee_lng') double? teeLng;`, `@JsonKey(name: 'green_lat') double? greenLat;`, `@JsonKey(name: 'green_lng') double? greenLng;`. Plus fromJson.
    - Note: the GPS field names are [ASSUMED]. The executor MUST run a single live `flutter run --dart-define=GOLF_API_KEY=<real-key>` call against `/courses/<known-id>` and log the raw JSON via `Logger().d(response)` from within `searchCourses`/`getCourseDetail` ONCE, then adjust `@JsonKey(name: ...)` to match real field names before completing Task 2. If field names differ (e.g., `tee_position.latitude` nested), update DTOs to match. STATE.md flags this as a Phase 1 gate.

    Create `lib/data/remote/api/interceptors/auth_interceptor.dart` per RESEARCH.md §5. Imports: `package:dio/dio.dart;`, `../../../app/constants.dart;`. Body extends `Interceptor` and overrides:
    - `onRequest`: if `AppConstants.golfApiKey.isEmpty` call `handler.reject(DioException(requestOptions: options, type: DioExceptionType.cancel, message: 'API_KEY_MISSING'))` and `return;` — do NOT call `handler.next(options)`.
    - `onError`: if `err.response?.statusCode == 401` call `handler.reject(DioException(requestOptions: err.requestOptions, response: err.response, type: DioExceptionType.badResponse, message: 'API_KEY_INVALID'))` and return; else `handler.next(err)`.

    Create `lib/data/remote/api/golf_course_api.dart` (Retrofit interface):
    - Imports: `package:dio/dio.dart`, `package:retrofit/retrofit.dart`, `../dto/course_search_response_dto.dart`, `../dto/course_detail_response_dto.dart`.
    - `part 'golf_course_api.g.dart';` (MANDATORY — P-10 protection).
    - `@RestApi() abstract class GolfCourseApi { factory GolfCourseApi(Dio dio, {String baseUrl}) = _GolfCourseApi; @GET('/search') Future<CourseSearchResponseDto> searchCourses(@Query('search_query') String query); @GET('/courses/{id}') Future<CourseDetailResponseDto> getCourseDetail(@Path('id') String id); }`.

    Create `lib/data/remote/api/dio_provider.dart` with `@Riverpod(keepAlive: true) Dio dio(Ref ref)` factory returning a Dio configured with `BaseOptions(baseUrl: AppConstants.golfApiBaseUrl, connectTimeout: const Duration(seconds: 10), receiveTimeout: const Duration(seconds: 15), headers: {'Authorization': 'Key ${AppConstants.golfApiKey}'})` and `..interceptors.add(AuthInterceptor())`. Imports: dio, riverpod_annotation, the AuthInterceptor and AppConstants. `part 'dio_provider.g.dart';`.

    Create `lib/data/remote/api/golf_course_api_provider.dart` with `@Riverpod(keepAlive: true) GolfCourseApi golfCourseApi(Ref ref) => GolfCourseApi(ref.watch(dioProvider));`. Per CLAUDE.md MUST be keepAlive. `part 'golf_course_api_provider.g.dart';`.

    Run `dart run build_runner build --delete-conflicting-outputs`. Expected: four `.g.dart` + four `.freezed.dart` files (2 DTOs × {freezed, json_serializable}, plus dio_provider.g.dart, golf_course_api.g.dart, golf_course_api_provider.g.dart). `flutter analyze` MUST exit 0.

    Live verification step (do this BEFORE Task 2): with a real API key set via `--dart-define=GOLF_API_KEY=<value>`, run a temporary smoke test (a `void main()` script or hand-edited `setup_screen.dart` call) hitting `GolfCourseApi.searchCourses('Royal Melbourne')`. Log the raw JSON. Verify response includes `stroke_index` per hole and field names match assumed casing. If any names differ, update the DTOs' `@JsonKey(name: ...)` BEFORE Task 2 wires the UI. Document any deviation in the SUMMARY.md as a A1/A2/A3/A4 verification outcome.
  </action>
  <verify>
    <automated>test -f lib/data/remote/dto/course_search_response_dto.dart && test -f lib/data/remote/dto/course_detail_response_dto.dart && test -f lib/data/remote/api/interceptors/auth_interceptor.dart && test -f lib/data/remote/api/golf_course_api.dart && test -f lib/data/remote/api/dio_provider.dart && test -f lib/data/remote/api/golf_course_api_provider.dart && test -f lib/data/remote/api/golf_course_api.g.dart && test -f lib/data/remote/dto/course_search_response_dto.freezed.dart && test -f lib/data/remote/dto/course_search_response_dto.g.dart && test -f lib/data/remote/dto/course_detail_response_dto.freezed.dart && test -f lib/data/remote/dto/course_detail_response_dto.g.dart && grep -q "part 'golf_course_api.g.dart'" lib/data/remote/api/golf_course_api.dart && grep -q '@RestApi()' lib/data/remote/api/golf_course_api.dart && grep -q "@Query('search_query')" lib/data/remote/api/golf_course_api.dart && grep -q "@Path('id')" lib/data/remote/api/golf_course_api.dart && grep -q 'API_KEY_MISSING' lib/data/remote/api/interceptors/auth_interceptor.dart && grep -q 'API_KEY_INVALID' lib/data/remote/api/interceptors/auth_interceptor.dart && grep -q 'AppConstants.golfApiKey.isEmpty' lib/data/remote/api/interceptors/auth_interceptor.dart && grep -q '@Riverpod(keepAlive: true)' lib/data/remote/api/dio_provider.dart && grep -q '@Riverpod(keepAlive: true)' lib/data/remote/api/golf_course_api_provider.dart && grep -q '@JsonKey(name: ' lib/data/remote/dto/course_search_response_dto.dart && flutter analyze --no-fatal-warnings</automated>
  </verify>
  <acceptance_criteria>
    - All six new source files exist plus their generated `.g.dart` / `.freezed.dart` counterparts
    - `golf_course_api.dart` declares `@RestApi()`, abstract class, `factory GolfCourseApi(Dio dio, {String baseUrl}) = _GolfCourseApi`, two endpoints with `@Query('search_query')` and `@Path('id')`
    - `auth_interceptor.dart` produces `API_KEY_MISSING` on empty key (cancel type) and `API_KEY_INVALID` on 401 (badResponse type) — both are typed DioException messages the UI can pattern-match
    - `dioProvider` and `golfCourseApiProvider` BOTH `@Riverpod(keepAlive: true)` per CLAUDE.md
    - DTOs use `@JsonKey(name: 'snake_case_name')` annotations for non-camelCase API field names
    - Build_runner exits 0; flutter analyze exits 0
    - Live verification: SUMMARY.md records whether `stroke_index` is present in `/courses/{id}` response (STATE.md gate A3) and any DTO field-name corrections applied
  </acceptance_criteria>
  <done>Retrofit + Dio + AuthInterceptor + DTOs all generated and analyse cleanly; live API contract verified or corrections applied; keepAlive providers in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CourseRepositoryImpl (write-through to Hive), repository provider, and the three screen-level providers (search query, results, selected course)</name>
  <files>lib/infrastructure/repositories/course_repository_impl.dart, lib/infrastructure/repositories/course_repository_provider.dart, lib/features/setup/providers/course_search_query_provider.dart, lib/features/setup/providers/course_search_results_provider.dart, lib/features/setup/providers/selected_course_provider.dart, lib/infrastructure/repositories/course_repository_provider.g.dart, lib/features/setup/providers/course_search_query_provider.g.dart, lib/features/setup/providers/course_search_results_provider.g.dart, lib/features/setup/providers/selected_course_provider.g.dart</files>
  <read_first>
    - .planning/phase-01/01-RESEARCH.md §3 (course_search_results_provider with 400ms debounce + ref.keepAlive(), courseRepositoryProvider as keepAlive, selectedCourse + roundSetupNotifier as auto-dispose), Integration Points diagram
    - .planning/phase-01/01-PATTERNS.md (course_search_results_provider.dart excerpt, debounced FutureProvider pattern)
    - .planning/phase-01/01-CONTEXT.md (D-04 cache full dataset; D-05 JSON string serialization; SETUP-02 search + SETUP-03 load + SETUP-04 cache flow)
    - .planning/phase-01/01-UI-SPEC.md §2 (Course Search Field — 400ms debounce, min 2 chars)
    - CLAUDE.md (`courseRepositoryProvider` MUST be `@Riverpod(keepAlive: true)`)
    - lib/domain/repositories/course_repository.dart (interface from Plan 02 — must implement exactly)
    - lib/data/remote/dto/course_search_response_dto.dart (just created — search returns `List<CourseSearchResultDto>`, must map to `List<CourseModel>` in the impl)
  </read_first>
  <behavior>
    - Test: `courseRepository.searchCourses('Royal')` returns a `Future<List<CourseModel>>` mapped from `CourseSearchResultDto` (no holes — search response has no holes; map holes to empty list with a comment).
    - Test: `courseRepository.getCourseDetail('abc123')` calls the API, then `hiveCourseBox.writeCourse('abc123', model)` is invoked with the resulting CourseModel — verifiable by reading back via `getCachedCourse('abc123')` after the call without any further API hit.
    - Test: `courseSearchResultsProvider` with `query = ''` returns `[]` instantly (no API call).
    - Test: `courseSearchResultsProvider` with `query = 'a'` returns `[]` (min 2 chars; no API call).
    - Test: `courseSearchResultsProvider` with `query = 'Royal'` waits 400ms then calls `golfCourseApi.searchCourses('Royal')`; if the query changes within 400ms, the keepAlive is dropped and the API call is skipped.
  </behavior>
  <action>
    Create `lib/infrastructure/repositories/course_repository_impl.dart`. Class declaration: `class CourseRepositoryImpl implements CourseRepository`. Constructor takes `GolfCourseApi _api` and `HiveCourseBox _hive`. Imports: `dart:convert`, the domain `course_repository.dart`, domain `course_model.dart` and `hole_model.dart`, remote `golf_course_api.dart`, DTOs `course_search_response_dto.dart` and `course_detail_response_dto.dart`, and Hive wrapper `hive_course_box.dart`.

    Method implementations:
    - `Future<List<CourseModel>> searchCourses(String query)` — call `_api.searchCourses(query)`, map `response.courses` to `CourseModel` via a private `_searchDtoToModel` helper that constructs `CourseModel(id: dto.id, clubName: dto.clubName, courseName: dto.courseName, courseRating: dto.courseRating, slope: dto.slopeRating, par: dto.par, holes: const [])`. The search response carries no per-hole data — holes is intentionally empty.
    - `Future<CourseModel> getCourseDetail(String id)` — call `_api.getCourseDetail(id)`, map `response.course` to `CourseModel` (including the full `holes` list mapped from `HoleDto` to `HoleModel`). Immediately after mapping (D-04 write-through), call `_hive.writeCourse(model.id, model)` so SETUP-04 pre-population works on the next launch. Return the model.
    - `CourseModel? getCachedCourse(String id)` — `return _hive.readCourse(id);`.
    - `void cacheCourse(CourseModel course)` — `_hive.writeCourse(course.id, course);`.

    Create `lib/infrastructure/repositories/course_repository_provider.dart` with `@Riverpod(keepAlive: true) CourseRepository courseRepository(Ref ref) => CourseRepositoryImpl(ref.watch(golfCourseApiProvider), ref.watch(hiveCourseBoxProvider));`. `part 'course_repository_provider.g.dart';`. Per CLAUDE.md MUST be keepAlive.

    Create `lib/features/setup/providers/course_search_query_provider.dart` (auto-dispose Notifier holding the debounced raw query string): `@riverpod class CourseSearchQuery extends _$CourseSearchQuery { @override String build() => ''; void set(String value) => state = value; }`. `part 'course_search_query_provider.g.dart';`.

    Create `lib/features/setup/providers/course_search_results_provider.dart` (auto-dispose Future) per RESEARCH.md §3:
    - Imports the search query provider, the golf_course_api provider, the search DTO, and the CourseRepository provider.
    - Body: `@riverpod Future<List<CourseSearchResultDto>> courseSearchResults(Ref ref) async { final query = ref.watch(courseSearchQueryProvider); if (query.length < 2) return []; final link = ref.keepAlive(); await Future.delayed(const Duration(milliseconds: 400)); link.close(); final api = ref.watch(golfCourseApiProvider); final response = await api.searchCourses(query); return response.courses; }`. The `ref.keepAlive()` returns a `KeepAliveLink`; if the query changes within 400ms the provider is rebuilt with the new query and the original keepAlive link is auto-disposed when its closure no longer references it. (UI-SPEC §2 — 400ms debounce, min 2 chars.) The provider returns `List<CourseSearchResultDto>` (DTO type) — Setup screen maps to display strings inline.

    Create `lib/features/setup/providers/selected_course_provider.dart` (auto-dispose Notifier holding the currently selected `CourseModel`): `@riverpod class SelectedCourse extends _$SelectedCourse { @override AsyncValue<CourseModel?> build() => const AsyncData(null); Future<void> loadCourse(String courseId) async { state = const AsyncLoading(); try { final repo = ref.read(courseRepositoryProvider); final course = await repo.getCourseDetail(courseId); ref.read(hivePlayerPrefsProvider).setLastUsedCourseId(course.id); state = AsyncData(course); } catch (e, st) { state = AsyncError(e, st); } } void overrideRating({double? rating, int? slope}) { state.whenData((c) { if (c != null) state = AsyncData(c.copyWith(courseRating: rating, slope: slope)); }); } void loadFromCache(CourseModel course) => state = AsyncData(course); void clear() => state = const AsyncData(null); }`. The `overrideRating` method supports SETUP-05 manual entry from Plan 05; `loadFromCache` is required by Task 3's SETUP-04 offline pre-populate path — declared here so Task 3 can call it without an API round-trip. Imports: course_model.dart, hive_player_prefs_provider.dart, course_repository_provider.dart.

    Run `dart run build_runner build --delete-conflicting-outputs`. Expected: four `.g.dart` files generated. `flutter analyze` exits 0.
  </action>
  <verify>
    <automated>test -f lib/infrastructure/repositories/course_repository_impl.dart && test -f lib/infrastructure/repositories/course_repository_provider.dart && test -f lib/features/setup/providers/course_search_query_provider.dart && test -f lib/features/setup/providers/course_search_results_provider.dart && test -f lib/features/setup/providers/selected_course_provider.dart && test -f lib/infrastructure/repositories/course_repository_provider.g.dart && test -f lib/features/setup/providers/course_search_results_provider.g.dart && grep -q 'implements CourseRepository' lib/infrastructure/repositories/course_repository_impl.dart && grep -q '_hive.writeCourse' lib/infrastructure/repositories/course_repository_impl.dart && grep -q '@Riverpod(keepAlive: true)' lib/infrastructure/repositories/course_repository_provider.dart && grep -q 'query.length < 2' lib/features/setup/providers/course_search_results_provider.dart && grep -q 'Duration(milliseconds: 400)' lib/features/setup/providers/course_search_results_provider.dart && grep -q 'ref.keepAlive' lib/features/setup/providers/course_search_results_provider.dart && grep -q 'class SelectedCourse extends _\$SelectedCourse' lib/features/setup/providers/selected_course_provider.dart && grep -q 'setLastUsedCourseId' lib/features/setup/providers/selected_course_provider.dart && grep -q 'overrideRating' lib/features/setup/providers/selected_course_provider.dart && grep -q 'loadFromCache' lib/features/setup/providers/selected_course_provider.dart && flutter analyze --no-fatal-warnings</automated>
  </verify>
  <acceptance_criteria>
    - `CourseRepositoryImpl implements CourseRepository` with all four interface methods; `getCourseDetail` writes through to Hive via `_hive.writeCourse(model.id, model)` (D-04 + SETUP-04)
    - `courseRepositoryProvider` annotated `@Riverpod(keepAlive: true)` (CLAUDE.md mandatory)
    - `courseSearchResultsProvider` rejects queries `< 2 chars`, debounces 400ms via `ref.keepAlive()` + `Future.delayed`, and calls `golfCourseApi.searchCourses(query)`
    - `SelectedCourseProvider.loadCourse(id)` calls `getCourseDetail` then `setLastUsedCourseId(course.id)` (SETUP-04 cache key write)
    - `overrideRating(rating, slope)` mutates state via `copyWith` (Plan 05 will use this for manual entry — declared here so the API surface is locked)
    - `loadFromCache(CourseModel course)` declared — required by Task 3 SETUP-04 offline pre-populate; sets state to `AsyncData(course)` directly without an API call
    - Build_runner exits 0; flutter analyze exits 0
  </acceptance_criteria>
  <done>Repository implementation + provider + three screen-level providers compile; write-through and debounce semantics verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build Setup screen UI — HandicapInput, CourseSearchField, CourseResultTile, CourseCard, ApiKeyErrorState — and rebuild SetupScreen as ConsumerStatefulWidget assembling them</name>
  <files>lib/features/setup/widgets/handicap_input.dart, lib/features/setup/widgets/course_search_field.dart, lib/features/setup/widgets/course_result_tile.dart, lib/features/setup/widgets/course_card.dart, lib/features/setup/widgets/api_key_error_state.dart, lib/features/setup/setup_screen.dart</files>
  <read_first>
    - .planning/phase-01/01-UI-SPEC.md — Components 1 (HandicapInput), 2 (CourseSearchField with debounce + min 2 chars), 3 (CourseResultTile), 4 (Loaded Course Card), 6 (START ROUND button — wired in Plan 04), 7 (ApiKeyErrorState full-screen), Screen Layout, Copywriting Contract, Animation Contract
    - .planning/phase-01/01-PATTERNS.md (StatelessWidget + ConsumerWidget patterns, ConsumerStatefulWidget pattern for SetupScreen, animation pattern with flutter_animate, InkWell ripple color, haptic feedback usage)
    - .planning/phase-01/01-CONTEXT.md (D-04, D-05 — course cache shape; SETUP-04 pre-populate from Hive on open)
    - CLAUDE.md (Navigation: `context.go()` not `context.push()`; Critical Don'ts: no continuous GPS, no hardcoded API key)
    - lib/theme/brdy_colors.dart, brdy_spacing.dart, brdy_text_theme.dart (Plan 01 — locked tokens)
    - lib/features/setup/providers/* (Task 2 — search query, results, selected course, plus hive_player_prefs_provider from Plan 02)
    - pubspec.yaml (verify `flutter_animate: ^4.5.0`, `haptic_feedback: ^0.5.0+2`, `gap: ^3.0.1`)
  </read_first>
  <behavior>
    - Test: HandicapInput pre-populates from `ref.read(hivePlayerPrefsProvider).handicapIndex` on initState; onChanged writes the parsed double back via `setHandicapIndex` (SETUP-01).
    - Test: Typing 1 character in CourseSearchField does NOT trigger the API (results provider gate); typing 2+ chars triggers an API call 400ms later.
    - Test: Tapping a CourseResultTile invokes `ref.read(selectedCourseProvider.notifier).loadCourse(courseId)`, which writes `last_used_course_id` to Hive.
    - Test: Re-opening Setup with `selectedCourse == null` BUT `hivePlayerPrefsProvider.lastUsedCourseId != null` AND `hiveCourseBoxProvider.readCourse(id) != null` shows the cached CourseCard without network — SETUP-04 verified.
    - Test: With `--dart-define=GOLF_API_KEY=` empty, attempting search renders `ApiKeyErrorState` full-screen (FOUND-04).
  </behavior>
  <action>
    All widgets live under `lib/features/setup/widgets/` and use Brdy theme tokens exclusively — never hardcode colors, spacing, fonts. Use `Theme.of(context).textTheme.*` or `BrdyTextTheme.textTheme.*` directly.

    `handicap_input.dart` — `ConsumerStatefulWidget` per UI-SPEC Component 1. Internal `TextEditingController` initialised in initState from `ref.read(hivePlayerPrefsProvider).handicapIndex?.toString()`. TextFormField with `decoration: InputDecoration(labelText: 'HANDICAP INDEX')` (theme handles fill + borders), `keyboardType: TextInputType.numberWithOptions(decimal: true)`, `inputFormatters: [FilteringTextInputFormatter.allow(RegExp(r'^\d{0,2}(\.\d{0,1})?$'))]` (max 4 chars accepting `54.0`), `style: Theme.of(context).textTheme.labelLarge`. onChanged: parse to double, validate `0.0 <= v <= 54.0`; if valid, call `ref.read(hivePlayerPrefsProvider).setHandicapIndex(v)` and clear error; else show inline error in destructive color: `'Handicap index must be between 0.0 and 54.0'` (UI-SPEC Copywriting Contract). Dispose controller in dispose().

    `course_search_field.dart` — `ConsumerWidget` per UI-SPEC Component 2. Renders `TextField` with `decoration` containing `labelText: 'SEARCH COURSES'`, `prefixIcon: Icon(Icons.search, size: 20, color: BrdyColors.onSurfaceMuted)`, and a conditional `suffixIcon: IconButton(tooltip: 'Clear search', icon: const Icon(Icons.close, size: 20), onPressed: () => controller.clear())`. onChanged calls `ref.read(courseSearchQueryProvider.notifier).set(value)`. Below the field, render a thin 1dp `LinearProgressIndicator(color: BrdyColors.accent, backgroundColor: BrdyColors.divider)` only when `courseSearchResultsProvider` is in `AsyncLoading` state.

    `course_result_tile.dart` — `StatelessWidget` per UI-SPEC Component 3. Required props: `String courseName, String? city, String? country, double? courseRating, int? slope, VoidCallback onTap`. Layout: `Material(color: BrdyColors.surface, child: InkWell(splashColor: Colors.white.withOpacity(0.08), highlightColor: Colors.white.withOpacity(0.04), onTap: onTap, child: Padding(padding: EdgeInsets.symmetric(horizontal: BrdySpacing.md, vertical: 12), child: Row(children: [Expanded(Column(crossAxisAlignment: start, children: [Text(courseName, style: labelLarge), Text('${city ?? ''}, ${country ?? ''}', style: bodySmall.copyWith(color: BrdyColors.onSurfaceMuted)), if (courseRating != null && slope != null) Text('CR $courseRating / SL $slope', style: jetBrainsMono 12)])), Icon(Icons.chevron_right, size: 20, color: BrdyColors.onSurfaceMuted)])))`. Bottom border: 1dp `BrdyColors.divider`. Min height 64dp.

    `course_card.dart` — `ConsumerWidget` per UI-SPEC Component 4. Reads `ref.watch(selectedCourseProvider)`; renders only when `.when(data: (course) => course != null ? ... : SizedBox.shrink(), ...)`. Container with 1dp `BrdyColors.accent` border on all sides, 16dp padding, `BrdyColors.surface` background, 0dp radius. Course name in `labelLarge` accent color, then `'PAR ${course.par}'` in `bodySmall` muted, then a Row with two columns: left `Column([Text('RATING', bodySmall muted), Text(course.courseRating?.toStringAsFixed(1) ?? 'N/A', bodyMedium)])`, right same shape for `SLOPE`. Wrap the whole card in `.animate().fadeIn(duration: 200.ms, curve: Curves.easeOut).slideY(begin: 0.1, end: 0)`. Add a `'change course'` lowercase underlined link at the bottom that calls `ref.read(selectedCourseProvider.notifier).clear()`. Do NOT render the missing-rating banner here — Plan 05 will add it.

    `api_key_error_state.dart` — `StatelessWidget` per UI-SPEC Component 7. Full-screen `Scaffold(backgroundColor: BrdyColors.background, body: SafeArea(child: Center(child: Column(mainAxisAlignment: center, children: [Icon(Icons.key_off, size: 48, color: BrdyColors.destructive), Gap(BrdySpacing.lg), Text('API KEY NOT CONFIGURED', style: labelLarge), Gap(BrdySpacing.sm), Text('Add your Golf Course API key via --dart-define=GOLF_API_KEY=<value> and restart the app.', style: bodySmall.copyWith(color: BrdyColors.onSurfaceMuted), textAlign: center)]))))`. Two-mode variant via constructor param `final bool isInvalidKey;`: when `true`, swap heading to `'API KEY INVALID'` and body to `'The Golf Course API key was rejected. Check the key and restart the app.'`. No retry button needed for Phase 1 — restart is the only fix.

    `setup_screen.dart` — replace the existing StatelessWidget with `ConsumerStatefulWidget`. The `_SetupScreenState` MUST in `initState` (run via WidgetsBinding.instance.addPostFrameCallback so ref is available): read `ref.read(hivePlayerPrefsProvider).lastUsedCourseId`; if present, look up `ref.read(hiveCourseBoxProvider).readCourse(id)`; if present, call `ref.read(selectedCourseProvider.notifier).loadCourse(id)` — but ONLY if no network is needed. Implementation: directly set state via a new method `loadFromCache(course)` added to SelectedCourseNotifier — add: `void loadFromCache(CourseModel course) => state = AsyncData(course);`. Add this method in Task 2 (note in Task 2: add this method as well). The pre-populate path uses no API: `final cached = ref.read(hiveCourseBoxProvider).readCourse(id); if (cached != null) ref.read(selectedCourseProvider.notifier).loadFromCache(cached);` — completes SETUP-04 offline pre-populate.

    SetupScreen body layout (UI-SPEC Screen Layout): `Scaffold(backgroundColor: BrdyColors.background, body: SafeArea(child: Padding(padding: EdgeInsets.symmetric(horizontal: BrdySpacing.md), child: Column(children: [Gap(BrdySpacing.md), Text('BRDY.01', style: jetBrainsMono 28 bold), Gap(BrdySpacing.lg), HandicapInput(), Gap(BrdySpacing.xl), CourseSearchField(), Gap(BrdySpacing.sm), Expanded(child: ref.watch(selectedCourseProvider).maybeWhen(data: (course) => course != null ? CourseCard() : _SearchResultsList(), orElse: () => _SearchResultsList())), Gap(BrdySpacing.x2l), _StartRoundButton()])))`.

    `_SearchResultsList` private widget inside setup_screen.dart consumes `courseSearchResultsProvider` via `.when(loading: () => SizedBox.shrink(), error: (e, _) => _ErrorBanner(e), data: (results) => ListView.separated(itemCount: results.length, separatorBuilder: (_, __) => Divider(color: BrdyColors.divider, height: 1), itemBuilder: (_, i) => CourseResultTile(courseName: results[i].clubName, city: results[i].location?.city, country: results[i].location?.country, courseRating: results[i].courseRating, slope: results[i].slopeRating, onTap: () => ref.read(selectedCourseProvider.notifier).loadCourse(results[i].id))))`.

    `_StartRoundButton` private widget inside setup_screen.dart: `ConsumerWidget` reading `selectedCourseProvider`. Renders an `ElevatedButton(onPressed: course != null ? () => /* Plan 04 will wire createRound */ null : null, child: Text('START ROUND'))`. Helper text below when disabled: `'Search and select a course to start'` in bodySmall muted. Plan 04 replaces the `null` onPressed body with the actual `roundSetupNotifierProvider.createRound(course, handicapIndex)` call + `context.go('/shot-capture/$roundId')`.

    Error handling — wrap `courseSearchResultsProvider.when(error: ...)` so DioException with `message == 'API_KEY_MISSING'` returns `ApiKeyErrorState(isInvalidKey: false)` as a full-screen replacement of the Setup body (not a snackbar — UI-SPEC says full-screen for API key errors), and `message == 'API_KEY_INVALID'` returns `ApiKeyErrorState(isInvalidKey: true)`. Other errors → in-list snackbar `'Could not reach the course database. Check your connection and try again.'`.

    Haptic feedback (UI-SPEC §Interaction Contracts): on successful course load (data branch of selectedCourseProvider transitions from null to non-null), call `await HapticFeedback.lightImpact();` from the `haptic_feedback` package. Plan 04 will add medium impact on START ROUND tap.
  </action>
  <verify>
    <automated>test -f lib/features/setup/widgets/handicap_input.dart && test -f lib/features/setup/widgets/course_search_field.dart && test -f lib/features/setup/widgets/course_result_tile.dart && test -f lib/features/setup/widgets/course_card.dart && test -f lib/features/setup/widgets/api_key_error_state.dart && test -f lib/features/setup/setup_screen.dart && grep -q 'ConsumerStatefulWidget' lib/features/setup/setup_screen.dart && grep -q 'BrdyColors.background' lib/features/setup/setup_screen.dart && grep -q 'API KEY NOT CONFIGURED' lib/features/setup/widgets/api_key_error_state.dart && grep -q 'API KEY INVALID' lib/features/setup/widgets/api_key_error_state.dart && grep -q "labelText: 'HANDICAP INDEX'" lib/features/setup/widgets/handicap_input.dart && grep -q "labelText: 'SEARCH COURSES'" lib/features/setup/widgets/course_search_field.dart && grep -q 'BrdyColors.accent' lib/features/setup/widgets/course_card.dart && grep -q "tooltip: 'Clear search'" lib/features/setup/widgets/course_search_field.dart && grep -q 'HapticFeedback.lightImpact' lib/features/setup/setup_screen.dart && ! grep -RE 'context\.push\(' lib/features/setup/ && flutter analyze --no-fatal-warnings && flutter test test/widget_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All five widget files exist with the structures described
    - SetupScreen is `ConsumerStatefulWidget`; on initState (post-frame) reads `hivePlayerPrefsProvider.lastUsedCourseId`, looks up `hiveCourseBoxProvider.readCourse(id)`, and pre-populates via `loadFromCache(course)` if the cache hit — SETUP-04 offline pre-populate proved
    - `HandicapInput` persists value to `hivePlayerPrefsProvider.setHandicapIndex(value)` on every valid keystroke (write-through to Hive, SETUP-01)
    - `CourseSearchField` calls `ref.read(courseSearchQueryProvider.notifier).set(value)` in onChanged and has the search icon prefix + accessible `'Clear search'` tooltip on the clear button
    - `CourseResultTile` onTap invokes `selectedCourseProvider.notifier.loadCourse(id)` — which writes `last_used_course_id` to Hive
    - `CourseCard` renders only when a course is loaded; uses 1dp accent border, course name in accent, RATING/SLOPE in JetBrains Mono; animates in via flutter_animate
    - `ApiKeyErrorState` is a full-screen Scaffold (NOT a snackbar) with the two locked copywriting strings and the `Icons.key_off` icon — surfaced whenever DioException message is `API_KEY_MISSING` or `API_KEY_INVALID`
    - SetupScreen contains NO `context.push(` calls; START ROUND button helper text matches `'Search and select a course to start'`
    - HapticFeedback.lightImpact() fires on successful course load
    - `flutter analyze` exits 0; `flutter test` exits 0
  </acceptance_criteria>
  <done>SETUP-01 / SETUP-02 / SETUP-03 / SETUP-04 / FOUND-04 functional via UI; user can enter handicap, search, select, see CourseCard; API key error states render full-screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phone → golfcourseapi.com (HTTPS) | All search/detail traffic crosses the public internet over TLS via Dio defaults |
| Build-time → runtime | `--dart-define=GOLF_API_KEY` value is compiled into the binary; visible to anyone with the APK/IPA |
| UI → Hive box | User input (handicap, last-used course id) and untrusted API response payload (course JSON) are written to local Hive boxes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Spoofing | golfcourseapi.com TLS validation | mitigate | Dio uses platform-default certificate pinning chain (no `badCertificateCallback` override); base URL hardcoded to `https://api.golfcourseapi.com/v1` so an attacker cannot redirect via DNS override without breaking TLS |
| T-01-03-02 | Tampering | Untrusted API JSON deserialised into Freezed DTOs and persisted to Hive | mitigate | json_serializable `fromJson` factories enforce types (a malicious server returning `course_rating: "stringy"` raises a typed `FormatException`, caught by `selectedCourseProvider.loadCourse` and surfaced as an error snackbar — no app crash, no Hive write) |
| T-01-03-03 | Information Disclosure | API key visible in compiled binary via `String.fromEnvironment` | accept | Project is local-only with no backend; key rotation is the user's responsibility; CLAUDE.md mandates `--dart-define` over hardcode — Plan 03 enforces this via the constants pattern |
| T-01-03-04 | Information Disclosure | DioException error messages may include the full request URL in logs | mitigate | `AuthInterceptor` rejects with typed message-only DioException (`'API_KEY_MISSING'`, `'API_KEY_INVALID'`); no raw URL or header is surfaced to the UI; `Logger().d()` calls for live API verification (Task 1) MUST be removed before completing Plan 03 |
| T-01-03-05 | Denial of Service | Search field calling API on every keystroke could exhaust free-tier 300 req/day | mitigate | 400ms debounce + min 2 chars + `ref.keepAlive()` cancellation pattern (UI-SPEC §2; A8 in RESEARCH.md); typical user under 5 searches per session |
| T-01-03-06 | Elevation of Privilege | 401 response handled by AuthInterceptor — could an attacker forge a 401 to lock the user out? | accept | Local-only app with no auth bypass surface; a forged 401 only triggers the `ApiKeyErrorState` UI; user simply restarts with a valid key; no escalation possible |
| T-01-03-07 | Repudiation | `last_used_course_id` written without a timestamp | accept | Local-only app, no audit log requirement; the value is a UX convenience, not a security record |
| T-01-03-SC | Tampering | npm/pip/cargo installs | accept | Plan 03 installs zero new packages — all deps (`dio`, `retrofit`, `freezed_annotation`, `json_annotation`, `haptic_feedback`, `gap`, `flutter_animate`) already audited in pubspec.yaml |

**Residual risk:** First live API call (Task 1 verification) MUST be done with a real API key. The raw JSON log statement MUST be removed before committing. If `stroke_index` is absent from `/courses/{id}` (P-09), document the workaround in SUMMARY.md and notify Phase 2 planner — does not block Plan 03 since Phase 1 only persists the raw `courseJson` snapshot.
</threat_model>

<verification>
1. `dart run build_runner build --delete-conflicting-outputs` exits 0 after each task.
2. `flutter analyze` exits 0.
3. `flutter test` exits 0 (BrdyApp smoke test from Plan 01 still passes; the new test pumps the full SetupScreen via a ProviderOverride for the API).
4. Live verification — with `--dart-define=GOLF_API_KEY=<real-key>`:
   - Search for `Royal Melbourne` → at least one result appears within 1s after 400ms debounce.
   - Tap a result → CourseCard appears with name in orange, par/rating/slope rendered.
   - Background `course_cache` Hive box now contains an entry keyed by the course id.
   - Background `player_prefs.last_used_course_id` is set.
   - Kill and relaunch (with airplane mode ON to prove no-network pre-populate) → CourseCard pre-populates from Hive cache without any API call (SETUP-04 verified).
   - Build with `--dart-define=GOLF_API_KEY=` empty → typing 2+ chars renders `ApiKeyErrorState` full-screen.
   - Build with a malformed key → server returns 401 → `ApiKeyErrorState(isInvalidKey: true)` renders.
</verification>

<success_criteria>
- Retrofit `GolfCourseApi` interface + DTOs compile and live-verify against golfcourseapi.com (Task 1 verification step)
- AuthInterceptor surfaces `API_KEY_MISSING` (cancel) and `API_KEY_INVALID` (badResponse) typed errors
- `CourseRepositoryImpl` writes through to Hive on `getCourseDetail` (SETUP-04 cache write)
- 400ms debounce + min 2 chars enforced in `courseSearchResultsProvider`
- SetupScreen pre-populates handicap from `player_prefs` and last-used course from `course_cache` on open
- All UI uses ONLY Brdy theme tokens; no hardcoded colors/spacing/fonts
- `ApiKeyErrorState` full-screen rendered on `API_KEY_MISSING` and `API_KEY_INVALID`
- HapticFeedback.lightImpact on course load
- Plan 04 START ROUND wire-up is the ONLY remaining gap (button rendered, helper text in place, onPressed null until Plan 04)
- `flutter analyze` clean; `flutter test` passes
</success_criteria>

<output>
Create `.planning/phases/01-foundation-setup/01-03-SUMMARY.md` when done with: remote-layer files created, live API verification result (does `/courses/{id}` return `stroke_index`? what field-name corrections were needed?), build_runner outputs, screen widgets created, and one-line note: "Setup screen wired through to SelectedCourseProvider; awaiting Plan 04 to wire START ROUND".
</output>
