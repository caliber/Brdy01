---
phase: 01-foundation-setup
plan: 05
type: execute
wave: 5
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - lib/infrastructure/repositories/tile_cache_repository.dart
  - lib/infrastructure/repositories/tile_cache_provider.dart
  - lib/infrastructure/repositories/tile_cache_provider.g.dart
  - lib/features/setup/providers/tile_cache_progress_provider.dart
  - lib/features/setup/providers/tile_cache_progress_provider.g.dart
  - lib/features/setup/widgets/missing_rating_banner.dart
  - lib/features/setup/widgets/manual_rating_form.dart
  - lib/features/setup/widgets/tile_cache_progress.dart
  - lib/features/setup/widgets/course_card.dart
  - lib/features/setup/setup_screen.dart
autonomous: false
requirements:
  - FOUND-05
  - SETUP-05
must_haves:
  truths:
    - "Loading a course with non-empty hole GPS coordinates triggers a non-blocking FMTC bulk download for the course bounding box at Z14–17"
    - "While tiles are downloading, a LinearProgressIndicator with copy 'DOWNLOADING MAP TILES… N%' renders below the Course Card"
    - "The START ROUND button is NEVER disabled or blocked by tile-cache state — user can start immediately (D-10)"
    - "If the loaded course has null or absent courseRating OR slope, the user sees the MissingRatingBanner inside the CourseCard with destructive styling"
    - "Tapping ENTER MANUALLY expands an inline form with COURSE RATING + SLOPE RATING fields and a SAVE RATING button; saving writes the values into selectedCourseProvider via overrideRating and removes the banner"
  artifacts:
    - path: "lib/infrastructure/repositories/tile_cache_repository.dart"
      provides: "TileCacheRepository.preCacheCourse(holeCoords) returning Stream<DownloadProgress>"
      contains: "RectangleRegion"
    - path: "lib/features/setup/widgets/missing_rating_banner.dart"
      provides: "MissingRatingBanner StatelessWidget with destructive accent and ENTER MANUALLY CTA"
      contains: "ENTER MANUALLY"
    - path: "lib/features/setup/widgets/manual_rating_form.dart"
      provides: "ManualRatingForm with CR (55.0–80.0, 1 decimal) and SLOPE (55–155 int) fields"
      contains: "SAVE RATING"
    - path: "lib/features/setup/widgets/tile_cache_progress.dart"
      provides: "TileCacheProgress LinearProgressIndicator widget"
      contains: "LinearProgressIndicator"
  key_links:
    - from: "lib/features/setup/setup_screen.dart"
      to: "lib/features/setup/providers/tile_cache_progress_provider.dart"
      via: "ref.listen on selectedCourseProvider → tileCacheProgressProvider.start(holes)"
      pattern: "tileCacheProgressProvider"
    - from: "lib/features/setup/widgets/course_card.dart"
      to: "lib/features/setup/widgets/missing_rating_banner.dart"
      via: "Conditionally rendered when course.courseRating == null || course.slope == null"
      pattern: "MissingRatingBanner"
    - from: "lib/features/setup/widgets/manual_rating_form.dart"
      to: "lib/features/setup/providers/selected_course_provider.dart"
      via: "ref.read(selectedCourseProvider.notifier).overrideRating(rating, slope) on SAVE RATING tap"
      pattern: "overrideRating"
---

<objective>
Close the last two requirements in Phase 1: FOUND-05 (OSM tile pre-caching for the loaded course bounding box) and SETUP-05 (missing-rating warning with manual entry). Both are best-effort, non-blocking flows that layer onto the working CourseCard from Plan 03.

Purpose: After this plan, Phase 1 fully delivers all 10 requirements. Phase 2 can proceed against a Drift schema that already has `holes` and `shots` tables (D-01..D-03), a Hive course cache with hole-level GPS coordinates (D-04..D-06), pre-cached OSM tiles ready for the Phase 5 map (FOUND-05), and a proven crash-safe round-persistence spine (FOUND-02/03).

Output: `TileCacheRepository` driving FMTC's `RectangleRegion` + `startForeground` bulk download, a `tileCacheProgressProvider` StreamProvider that powers the inline progress bar, the `MissingRatingBanner` + `ManualRatingForm` widgets per UI-SPEC Component 5, an updated `CourseCard` that renders the banner conditionally, and a SetupScreen update that wires `ref.listen` on `selectedCourseProvider` to kick off pre-caching. Closes the phase with a final human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phase-01/01-CONTEXT.md
@.planning/phase-01/01-RESEARCH.md
@.planning/phase-01/01-PATTERNS.md
@.planning/phase-01/01-UI-SPEC.md
@.planning/phase-01/01-SKELETON.md

<interfaces>
<!-- Contracts from Plans 01–04 (wave 5, depends_on: [01-01, 01-02, 01-03, 01-04]). -->

From lib/app/constants.dart (Plan 02):
- `AppConstants.tileCacheStoreName == 'brdy_tiles'`.

From lib/main.dart (Plan 02):
- `FMTCObjectBoxBackend().initialise()` + `FMTCStore('brdy_tiles').manage.create()` already called before runApp.

From lib/domain/models/course_model.dart (Plan 02) + hole_model.dart:
- `CourseModel.holes` is `List<HoleModel>`; each `HoleModel` has nullable `teeLat, teeLng, greenLat, greenLng` (D-06).
- `CourseModel.copyWith` supports `courseRating: double?, slope: int?` overrides.

From lib/features/setup/providers/selected_course_provider.dart (Plan 03):
- `selectedCourseProvider` exposes `AsyncValue<CourseModel?>`; declared method `overrideRating({double? rating, int? slope})` mutates state by calling `copyWith`.

From lib/features/setup/widgets/course_card.dart (Plan 03):
- `CourseCard` is `ConsumerWidget` reading `selectedCourseProvider`; currently does NOT render the missing-rating banner — Plan 05 adds it.

From lib/features/setup/setup_screen.dart (Plan 03 + Plan 04):
- SetupScreen body has an `Expanded` slot showing CourseCard when a course is loaded; Plan 05 wraps that area to also include `TileCacheProgress` widget below the CourseCard when active.

FMTC v9 API surface (RESEARCH.md §6, verified):
- `FMTCObjectBoxBackend().initialise()` — already called in main()
- `FMTCStore('brdy_tiles')` — store handle
- `RectangleRegion(LatLngBounds(LatLng minSW, LatLng maxNE))` — bounding box region
- `region.toDownloadable(minZoom: 14, maxZoom: 17, options: TileLayer(urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png', userAgentPackageName: 'com.brdy.brdy01'))` — DownloadableRegion
- `FMTCStore('brdy_tiles').download.startForeground(downloadable, parallelThreads: 3, seaTileRemoval: false)` — returns a record `(:downloadProgress, :tileEvents)` where `downloadProgress` is `Stream<DownloadProgress>` with `attemptedTiles`, `maxTiles`, `percentageProgress`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TileCacheRepository + provider + tileCacheProgressProvider (StreamProvider) and the TileCacheProgress widget</name>
  <files>lib/infrastructure/repositories/tile_cache_repository.dart, lib/infrastructure/repositories/tile_cache_provider.dart, lib/infrastructure/repositories/tile_cache_provider.g.dart, lib/features/setup/providers/tile_cache_progress_provider.dart, lib/features/setup/providers/tile_cache_progress_provider.g.dart, lib/features/setup/widgets/tile_cache_progress.dart</files>
  <read_first>
    - .planning/phase-01/01-RESEARCH.md §6 (FMTC v9 init + bulk download with RectangleRegion + toDownloadable + startForeground; non-blocking pattern), Pitfall P-08 (v9 vs v10 API differences — pubspec pin is `^9.1.1`), Pitfall P-14 (do not await — fire-and-forget with StreamProvider)
    - .planning/phase-01/01-CONTEXT.md (D-07 FMTC init done; D-08 derive bounding box from hole GPS coords + buffer; D-09 Z14–17; D-10 best-effort, non-blocking)
    - .planning/phase-01/01-UI-SPEC.md Component 8 (Tile Pre-Caching Progress — LinearProgressIndicator 2dp height, accent color, label `'DOWNLOADING MAP TILES… {n}%'` in onSurfaceMuted, silent completion)
    - CLAUDE.md (no continuous GPS — Phase 1 only uses GPS coords STATIC from API; no Permission needed in Plan 05)
    - pubspec.yaml (verify `flutter_map_tile_caching: ^9.1.1`, `flutter_map: ^7.0.2`, `latlong2: ^0.9.1`)
  </read_first>
  <behavior>
    - Test: `_deriveBounds([HoleModel with GPS])` returns `LatLngBounds` from min/max lat-lng plus a 0.005-degree buffer.
    - Test: `preCacheCourse(emptyHoleList)` returns null (Stream is null, no download started) — D-08 silent skip when no GPS data.
    - Test: `preCacheCourse(holeListWithGps)` returns a `Stream<DownloadProgress>` that the UI can listen to.
    - Test: `tileCacheProgressProvider` returns `AsyncValue<DownloadProgress?>` — null when no download is active, AsyncData(progress) while downloading, AsyncData(null) again when complete.
  </behavior>
  <action>
    Create `lib/infrastructure/repositories/tile_cache_repository.dart`. Imports: `package:flutter_map/flutter_map.dart` (for TileLayer), `package:flutter_map_tile_caching/flutter_map_tile_caching.dart` (for FMTC, RectangleRegion, DownloadProgress), `package:latlong2/latlong.dart` (for LatLng, LatLngBounds), `../../app/constants.dart`, `../../domain/models/hole_model.dart`.

    Class body: `class TileCacheRepository { Stream<DownloadProgress>? preCacheCourse(List<HoleModel> holes) { final coords = holes.where((h) => h.teeLat != null && h.teeLng != null).map((h) => LatLng(h.teeLat!, h.teeLng!)).toList(); if (coords.isEmpty) return null; final bounds = _deriveBounds(coords, bufferDegrees: 0.005); final region = RectangleRegion(bounds); final downloadable = region.toDownloadable(minZoom: 14, maxZoom: 17, options: TileLayer(urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png', userAgentPackageName: 'com.brdy.brdy01')); final (:downloadProgress, :tileEvents) = FMTCStore(AppConstants.tileCacheStoreName).download.startForeground(downloadable, parallelThreads: 3, seaTileRemoval: false); return downloadProgress; } LatLngBounds _deriveBounds(List<LatLng> coords, {required double bufferDegrees}) { var minLat = coords.first.latitude, maxLat = coords.first.latitude; var minLng = coords.first.longitude, maxLng = coords.first.longitude; for (final c in coords) { if (c.latitude < minLat) minLat = c.latitude; if (c.latitude > maxLat) maxLat = c.latitude; if (c.longitude < minLng) minLng = c.longitude; if (c.longitude > maxLng) maxLng = c.longitude; } return LatLngBounds(LatLng(minLat - bufferDegrees, minLng - bufferDegrees), LatLng(maxLat + bufferDegrees, maxLng + bufferDegrees)); } }`. Note: greenLat/greenLng could also feed the bounds — keep this simple by using tee positions only (sufficient for course coverage). If a hole has tee GPS data, we trust it; D-06 nullability is respected via `.where`.

    Create `lib/infrastructure/repositories/tile_cache_provider.dart`. Imports riverpod_annotation and `tile_cache_repository.dart`. `part 'tile_cache_provider.g.dart';`. Body: `@Riverpod(keepAlive: true) TileCacheRepository tileCache(Ref ref) => TileCacheRepository();`. KeepAlive because FMTC state spans screens.

    Create `lib/features/setup/providers/tile_cache_progress_provider.dart`. This is an auto-dispose StreamNotifier so the UI can drive `.start(holes)` from `ref.listen` on selectedCourseProvider. Imports riverpod_annotation, `package:flutter_map_tile_caching/flutter_map_tile_caching.dart`, `../../../infrastructure/repositories/tile_cache_provider.dart`, `../../../domain/models/hole_model.dart`. `part 'tile_cache_progress_provider.g.dart';`. Body:

    `@riverpod class TileCacheProgress extends _$TileCacheProgress { StreamSubscription<DownloadProgress>? _sub; @override DownloadProgress? build() { ref.onDispose(() => _sub?.cancel()); return null; } void start(List<HoleModel> holes) { _sub?.cancel(); final stream = ref.read(tileCacheProvider).preCacheCourse(holes); if (stream == null) { state = null; return; } _sub = stream.listen((progress) { state = progress; if (progress.percentageProgress >= 100) { state = null; _sub?.cancel(); } }, onError: (e) { state = null; _sub?.cancel(); }); } }`. Import `dart:async` for StreamSubscription.

    Create `lib/features/setup/widgets/tile_cache_progress.dart`. `ConsumerWidget` reading `ref.watch(tileCacheProgressProvider)`. If state is null, return `SizedBox.shrink()`. Otherwise render: `Column(crossAxisAlignment: start, children: [Text('DOWNLOADING MAP TILES… ${state.percentageProgress.toInt()}%', style: Theme.of(context).textTheme.bodySmall?.copyWith(color: BrdyColors.onSurfaceMuted)), Gap(BrdySpacing.xs), LinearProgressIndicator(value: state.percentageProgress / 100, color: BrdyColors.accent, backgroundColor: BrdyColors.divider, minHeight: 2)])` per UI-SPEC Component 8. Imports the theme + Gap widget + flutter_map_tile_caching for DownloadProgress.

    Run `dart run build_runner build --delete-conflicting-outputs`. Two `.g.dart` files expected. `flutter analyze` exits 0.
  </action>
  <verify>
    <automated>test -f lib/infrastructure/repositories/tile_cache_repository.dart && test -f lib/infrastructure/repositories/tile_cache_provider.dart && test -f lib/features/setup/providers/tile_cache_progress_provider.dart && test -f lib/features/setup/widgets/tile_cache_progress.dart && grep -q 'RectangleRegion' lib/infrastructure/repositories/tile_cache_repository.dart && grep -q 'minZoom: 14' lib/infrastructure/repositories/tile_cache_repository.dart && grep -q 'maxZoom: 17' lib/infrastructure/repositories/tile_cache_repository.dart && grep -q 'AppConstants.tileCacheStoreName' lib/infrastructure/repositories/tile_cache_repository.dart && grep -q 'startForeground' lib/infrastructure/repositories/tile_cache_repository.dart && grep -q 'userAgentPackageName' lib/infrastructure/repositories/tile_cache_repository.dart && grep -q '@Riverpod(keepAlive: true)' lib/infrastructure/repositories/tile_cache_provider.dart && grep -q 'StreamSubscription<DownloadProgress>' lib/features/setup/providers/tile_cache_progress_provider.dart && grep -q 'DOWNLOADING MAP TILES' lib/features/setup/widgets/tile_cache_progress.dart && grep -q 'LinearProgressIndicator' lib/features/setup/widgets/tile_cache_progress.dart && grep -q 'BrdyColors.accent' lib/features/setup/widgets/tile_cache_progress.dart && flutter analyze --no-fatal-warnings</automated>
  </verify>
  <acceptance_criteria>
    - `TileCacheRepository.preCacheCourse(holes)` returns `null` when no hole has tee GPS data (D-08 silent skip)
    - When GPS data is present, returns a `Stream<DownloadProgress>` from FMTC `startForeground` with Z14–17 (D-09) over the derived bounding box plus 0.005-degree buffer
    - Uses `userAgentPackageName: 'com.brdy.brdy01'` per OSM tile server policy
    - `tileCacheProvider` is `@Riverpod(keepAlive: true)`; `tileCacheProgressProvider` is auto-dispose StreamNotifier with `start(holes)` method
    - Subscription cancelled in `ref.onDispose` (no leak)
    - `TileCacheProgress` widget renders ONLY when progress is non-null; uses accent color, 2dp minHeight, and the locked label copy `'DOWNLOADING MAP TILES… {n}%'`
    - Build_runner exits 0; flutter analyze exits 0
  </acceptance_criteria>
  <done>FMTC pre-cache repository + StreamProvider + progress widget compile and respect D-08/09/10 (non-blocking, silent skip on no GPS).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MissingRatingBanner + ManualRatingForm widgets, update CourseCard to render banner conditionally, wire SetupScreen to trigger tile pre-cache via ref.listen on selectedCourseProvider</name>
  <files>lib/features/setup/widgets/missing_rating_banner.dart, lib/features/setup/widgets/manual_rating_form.dart, lib/features/setup/widgets/course_card.dart, lib/features/setup/setup_screen.dart</files>
  <read_first>
    - .planning/phase-01/01-UI-SPEC.md Component 5 (Missing Rating Banner — destructive 15% over surface bg, 3dp left border in destructive, ENTER MANUALLY underlined CTA), Manual Entry Inline Form (COURSE RATING 55.0–80.0 with 1 decimal, SLOPE 55–155 int, SAVE RATING CTA), §Copywriting (banner text, validation copy), §Animation Contract (banner slides down 200ms easeOut, form expands 250ms easeOut)
    - .planning/phase-01/01-PATTERNS.md (CourseCard pattern with conditional rendering, ConsumerWidget for widgets reading providers, InkWell ripple, animation pattern)
    - .planning/phase-01/01-CONTEXT.md (SETUP-05 — warning + manual entry; D-04 course cached with potentially null CR/Slope)
    - lib/features/setup/widgets/course_card.dart (Plan 03 — current shape; just add the conditional banner inside the column)
    - lib/features/setup/providers/selected_course_provider.dart (Plan 03 — `overrideRating({double? rating, int? slope})` method is declared and ready to receive values from the form)
    - lib/features/setup/setup_screen.dart (Plans 03 + 04 — must add ref.listen on selectedCourseProvider and a TileCacheProgress slot in the body)
  </read_first>
  <behavior>
    - Test: A CourseCard rendered with `courseRating == null` shows the `MissingRatingBanner` inline below the rating/slope row.
    - Test: A CourseCard with both `courseRating` and `slope` non-null does NOT render the banner.
    - Test: Tapping ENTER MANUALLY animates open the `ManualRatingForm`; tapping SAVE RATING with values 74.0 and 145 calls `selectedCourseProvider.notifier.overrideRating(rating: 74.0, slope: 145)`; banner disappears and rating/slope rows update.
    - Test: SetupScreen `ref.listen` on `selectedCourseProvider` fires `tileCacheProgressProvider.notifier.start(course.holes)` when the course transitions from null → non-null. If course.holes is empty or no GPS data, the progress widget remains hidden (D-08 silent skip).
    - Test: START ROUND remains enabled regardless of tile cache progress (D-10).
  </behavior>
  <action>
    Create `lib/features/setup/widgets/missing_rating_banner.dart`. `StatelessWidget` with required `VoidCallback onEnterManuallyTap`. Body: `Container(decoration: BoxDecoration(color: const Color(0x26CC2200) /* destructive 15% */, border: Border(left: BorderSide(color: BrdyColors.destructive, width: 3))), padding: EdgeInsets.all(BrdySpacing.md), child: Column(crossAxisAlignment: start, children: [Text('Course Rating or Slope not found. WHS differential will not be calculated. Enter values manually to enable it.', style: bodySmall.copyWith(color: BrdyColors.destructive)), Gap(BrdySpacing.sm), InkWell(onTap: onEnterManuallyTap, child: Text('ENTER MANUALLY', style: bodySmall.copyWith(color: BrdyColors.destructive, fontWeight: w700, decoration: TextDecoration.underline)))]))` per UI-SPEC Component 5. Wrap in `.animate().slideY(begin: -0.1, end: 0, duration: 200.ms, curve: Curves.easeOut)` per Animation Contract.

    Create `lib/features/setup/widgets/manual_rating_form.dart`. `ConsumerStatefulWidget` with required `VoidCallback onSaved`. Internal controllers for two `TextEditingController` instances. Body:
    - `TextFormField` for COURSE RATING — `keyboardType: TextInputType.numberWithOptions(decimal: true)`, `inputFormatters: [FilteringTextInputFormatter.allow(RegExp(r'^\d{0,2}(\.\d{0,1})?$'))]`, `decoration: InputDecoration(labelText: 'COURSE RATING')`, validator returning error when value not in `55.0..80.0`.
    - `TextFormField` for SLOPE — `keyboardType: TextInputType.number`, `inputFormatters: [FilteringTextInputFormatter.digitsOnly, LengthLimitingTextInputFormatter(3)]`, `decoration: InputDecoration(labelText: 'SLOPE RATING')`, validator returning error when value not in `55..155`.
    - `ElevatedButton(onPressed: _submit, child: Text('SAVE RATING'))` — on tap: validate both fields; if both pass, call `ref.read(selectedCourseProvider.notifier).overrideRating(rating: double.parse(cr), slope: int.parse(slope))`, then `onSaved()` to collapse the form. UI-SPEC: button uses labelLarge style which BrdyTheme already applies. Dispose controllers in dispose().
    - Wrap the entire form in `AnimatedSize(duration: 250.ms, curve: Curves.easeOut, child: ...)` per Animation Contract.

    Update `lib/features/setup/widgets/course_card.dart`. Add internal state `bool _manualFormOpen = false;` (change to `ConsumerStatefulWidget` if not already; Plan 03 created it as `ConsumerWidget` — convert if necessary). After the rating/slope Row, insert:

    `if (course.courseRating == null || course.slope == null) ...[Gap(BrdySpacing.sm), if (!_manualFormOpen) MissingRatingBanner(onEnterManuallyTap: () => setState(() => _manualFormOpen = true)) else ManualRatingForm(onSaved: () => setState(() => _manualFormOpen = false))]`

    Once `overrideRating` updates `selectedCourseProvider`, the CourseCard rebuilds; `course.courseRating != null && course.slope != null` becomes true → the entire conditional block disappears. (No need to manually `setState` after save — the provider rebuild handles it; `_manualFormOpen` resets to false in `onSaved` so next-time-null also works.)

    Update `lib/features/setup/setup_screen.dart`. Inside `_SetupScreenState.build`, add a `ref.listen<AsyncValue<CourseModel?>>(selectedCourseProvider, (prev, next) { final wasNull = prev?.valueOrNull == null; final nowLoaded = next.valueOrNull; if (wasNull && nowLoaded != null) { ref.read(tileCacheProgressProvider.notifier).start(nowLoaded.holes); } });`. This MUST be inside `build` (Riverpod's `ref.listen` belongs in build/initState). Also add the `TileCacheProgress` widget into the body — place it between the CourseCard area and `_StartRoundButton`, e.g. after the `Expanded(child: ...)` slot: `const TileCacheProgress(), Gap(BrdySpacing.x2l), _StartRoundButton()`. The progress widget self-hides when state is null.

    Imports needed in setup_screen.dart: `selected_course_provider.dart`, `tile_cache_progress_provider.dart`, `tile_cache_progress.dart`.

    Run `dart run build_runner build --delete-conflicting-outputs`. No new `.g.dart` files expected from this task (widgets don't generate code) — build_runner is safe to re-run idempotently. `flutter analyze` exits 0. `flutter test` exits 0 (smoke test still passes).
  </action>
  <verify>
    <automated>test -f lib/features/setup/widgets/missing_rating_banner.dart && test -f lib/features/setup/widgets/manual_rating_form.dart && grep -q "ENTER MANUALLY" lib/features/setup/widgets/missing_rating_banner.dart && grep -q "Course Rating or Slope not found" lib/features/setup/widgets/missing_rating_banner.dart && grep -q 'BrdyColors.destructive' lib/features/setup/widgets/missing_rating_banner.dart && grep -q '0x26CC2200' lib/features/setup/widgets/missing_rating_banner.dart && grep -q "SAVE RATING" lib/features/setup/widgets/manual_rating_form.dart && grep -q "labelText: 'COURSE RATING'" lib/features/setup/widgets/manual_rating_form.dart && grep -q "labelText: 'SLOPE RATING'" lib/features/setup/widgets/manual_rating_form.dart && grep -q 'overrideRating' lib/features/setup/widgets/manual_rating_form.dart && grep -q 'MissingRatingBanner' lib/features/setup/widgets/course_card.dart && grep -q 'ManualRatingForm' lib/features/setup/widgets/course_card.dart && grep -q 'ref.listen' lib/features/setup/setup_screen.dart && grep -q 'tileCacheProgressProvider' lib/features/setup/setup_screen.dart && grep -q 'TileCacheProgress' lib/features/setup/setup_screen.dart && flutter analyze --no-fatal-warnings && flutter test test/widget_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `MissingRatingBanner` uses the LOCKED hex color `0x26CC2200` (destructive 15%) for background, 3dp `BrdyColors.destructive` left border, and the LOCKED copy strings from UI-SPEC §Copywriting Contract verbatim
    - `MissingRatingBanner` animates in via `.animate().slideY(begin: -0.1, end: 0, duration: 200.ms, curve: Curves.easeOut)` per Animation Contract
    - `ManualRatingForm` has CR field with `numberWithOptions(decimal: true)` keyboard and 55.0–80.0 validator; SLOPE field with `digitsOnly` formatter and 55–155 validator
    - SAVE RATING tap calls `ref.read(selectedCourseProvider.notifier).overrideRating(rating: ..., slope: ...)` then `onSaved()`
    - `CourseCard` renders banner conditionally on `course.courseRating == null || course.slope == null`; collapses entirely when both become non-null after override
    - `SetupScreen` uses `ref.listen` (NOT `ref.watch`) inside `build` to subscribe to `selectedCourseProvider` transitions and call `tileCacheProgressProvider.notifier.start(course.holes)` on null → loaded transition only (not on every rebuild)
    - `TileCacheProgress` slot inserted between CourseCard area and `_StartRoundButton`; progress hidden when state is null
    - START ROUND button onPressed remains UNGATED by tile cache state (D-10 enforcement — visual check)
    - Build_runner exits 0; flutter analyze exits 0; flutter test exits 0
  </acceptance_criteria>
  <done>SETUP-05 banner + manual form fully wired; FMTC pre-cache trigger fires on course load via ref.listen; progress bar renders inline; START ROUND not blocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify FOUND-05 tile pre-cache and SETUP-05 missing-rating flow end-to-end</name>
  <what-built>
    The final two requirements of Phase 1: FOUND-05 OSM tile pre-caching and SETUP-05 missing-rating warning + manual entry. This checkpoint exercises both flows on a real device with a real API key. After this passes, Phase 1 is complete and all 10 requirements (FOUND-01..05, SETUP-01..05) are observable.
  </what-built>
  <how-to-verify>
    Prerequisite: real golfcourseapi.com API key + a course known to have hole-level GPS coordinates (recommended: `Pebble Beach Golf Links`, `St Andrews Old Course`, or any well-known major championship course that the API likely has full GPS coverage for). A second course KNOWN to lack courseRating or slope is needed for SETUP-05 verification — if no such course is available, use developer tools to override one field via the test step described.

    Step 1 — FMTC tile pre-cache happy path:
    1. `flutter run --dart-define=GOLF_API_KEY=<real-key>`.
    2. Type a handicap, search for a course with GPS data (e.g. `Pebble Beach`), select it.
    3. Expected: CourseCard appears with course name, par, RATING, SLOPE.
    4. **Below the CourseCard, the `DOWNLOADING MAP TILES… N%` LinearProgressIndicator appears within 1-2 seconds.** The N% should tick upward.
    5. The START ROUND button is enabled and tappable from the moment the CourseCard appears — verify by tapping it immediately (before the progress hits 100%). The tap MUST proceed to Shot Capture, NOT wait for the tile download (D-10 enforcement).
    6. (If you tapped START ROUND in step 5 to test non-blocking behaviour) Navigate back via crash recovery: full-kill the app, relaunch. You land on Shot Capture from the crash-recovered round. Do not need to re-test SETUP-05 here.

    Step 2 — FMTC silent skip on missing GPS (D-08):
    1. Search for a small or local course unlikely to have hole-level GPS data (e.g. a municipal 9-hole course). If unsure, use developer tooling to verify the `/courses/{id}` response has no `tee_lat`/`green_lat` fields, or mock by editing the response.
    2. Expected: CourseCard appears as normal; NO `DOWNLOADING MAP TILES…` progress bar ever appears.
    3. START ROUND remains enabled and functional.

    Step 3 — SETUP-05 missing rating banner:
    1. Find a course whose API response has `courseRating: null` or `slopeRating: null`. (If the API does not naturally return null values, the simplest dev workaround is to set a breakpoint in `CourseRepositoryImpl.getCourseDetail` and override `courseRating: null` in the returned model. Alternatively, hand-edit a course's record via a mocked test build.)
    2. Load the course. Expected: CourseCard renders with `RATING N/A` (or blank) and `SLOPE N/A` (or blank), AND the `MissingRatingBanner` slides down with the dark-red destructive background, the LOCKED copy `'Course Rating or Slope not found. WHS differential will not be calculated. Enter values manually to enable it.'`, and the underlined `ENTER MANUALLY` link.

    Step 4 — Manual entry form:
    1. Tap `ENTER MANUALLY`. Expected: form expands (250ms animation) with two fields and a `SAVE RATING` button below them.
    2. Type `74.0` into COURSE RATING and `145` into SLOPE RATING. Tap `SAVE RATING`.
    3. Expected:
       - CourseCard rebuilds: RATING shows `74.0`, SLOPE shows `145`.
       - MissingRatingBanner disappears entirely.
       - Form collapses.
    4. Bonus: tap START ROUND. The created Drift `rounds` row should now have `courseRating = 74.0` and `courseSlope = 145` (overridden values, not nulls). Use SQLite browser to confirm if convenient.

    Step 5 — Validation:
    1. Re-trigger SETUP-05 (e.g. by clearing the loaded course and re-selecting). Tap ENTER MANUALLY.
    2. Enter invalid values: `100.0` for COURSE RATING (out of range 55.0–80.0) and `200` for SLOPE (out of range 55–155). Tap SAVE RATING.
    3. Expected: inline validation errors appear on both fields in destructive red. SAVE RATING does NOT submit. `selectedCourseProvider` is NOT updated.

    If all five steps pass, respond `approved`. If any step fails — particularly Step 1 step 5 (non-blocking START ROUND) or Step 4 step 3 (banner disappears after save) — describe what was observed and where it deviated. The executor will iterate.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue (which step, what was observed vs expected)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phone → tile.openstreetmap.org (HTTPS) | OSM tile downloads cross the public internet via TLS |
| Untrusted API GPS coords → FMTC bounding box | A malicious or buggy API response could return absurd lat/lng (e.g. NaN, ±1000) which would create a giant download region |
| FMTC ObjectBox store → filesystem | Tile cache writes to the device's app sandbox |
| Manual form input → CourseModel state | User-controlled numeric input that affects Phase 3 WHS differential calculation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Denial of Service | Malicious or buggy API returning out-of-range lat/lng creating multi-gigabyte tile download | mitigate | `_deriveBounds` uses `.where((h) => h.teeLat != null && h.teeLng != null)` so nulls skip; consider adding `if (bounds.span > MAX_DEGREES)` guard in a future hardening pass — accept for Phase 1 because (a) FMTC v9 itself caps tiles per region and (b) golfcourseapi.com is the trusted source (HTTPS-pinned). Document follow-up in SUMMARY.md |
| T-01-05-02 | Spoofing | OSM tile server TLS validation | mitigate | `tile.openstreetmap.org` hardcoded in `urlTemplate`; FMTC v9 uses platform default TLS chain; `userAgentPackageName: 'com.brdy.brdy01'` complies with OSM tile policy preventing rate-limit ban |
| T-01-05-03 | Tampering | Manual form values directly mutate `selectedCourseProvider.value.courseRating/slope` and persist to Drift on START ROUND | mitigate | Both fields validate before `overrideRating` is called: CR must be `55.0..80.0` (WHS range), SLOPE must be `55..155` (WHS range); invalid values block SAVE RATING entirely; the Phase 3 differential calc using these values is bounded by WHS algorithm — out-of-range cannot produce nonsensical differentials |
| T-01-05-04 | Information Disclosure | FMTC progress event stream could expose timing data (download took N seconds) | accept | Local-only app; no exfiltration channel; download timing is not sensitive |
| T-01-05-05 | Denial of Service | FMTC subscription leak on rapid course-select toggling | mitigate | `TileCacheProgressNotifier.start()` cancels any prior `_sub?.cancel()` before subscribing; `ref.onDispose` cancels on provider teardown — no leak across screen transitions |
| T-01-05-06 | Repudiation | Manually-entered CR/Slope persisted to Drift `courseJson` snapshot indistinguishable from API-returned values | mitigate | Phase 1 accepts this — the round was started with the user-entered values; future v2 could add an audit field `(rating_source: 'api' \| 'manual')` if WHS compliance requires it; deferred per CLAUDE.md "No feature not in REQUIREMENTS.md" |
| T-01-05-07 | Elevation of Privilege | Path traversal via OSM tile URL with malicious `{z}/{x}/{y}` values | accept | Tile URL params are integer-only by OSM convention; FMTC v9 internally constrains tile coordinates to valid zoom-level ranges before substitution |
| T-01-05-SC | Tampering | npm/pip/cargo installs | accept | Plan 05 installs zero new packages — `flutter_map`, `latlong2`, `flutter_map_tile_caching` already audited in pubspec |

**Residual risk:** Step 1 step 5 of the human-verify checkpoint is the only authoritative test of D-10 (non-blocking START ROUND). Automated tests cannot easily distinguish "button waited for download" from "button proceeded immediately" without instrumentation. If the checkpoint reveals the button is blocked, the executor MUST remove any `await` on the download stream from the START ROUND path.
</threat_model>

<verification>
1. `dart run build_runner build --delete-conflicting-outputs` exits 0 after Task 1.
2. `flutter analyze` exits 0 after each task.
3. `flutter test test/widget_test.dart` exits 0.
4. Task 3 human-verify checkpoint passes all five steps, especially:
   - Step 1 step 5: START ROUND is tappable BEFORE tile download completes (D-10).
   - Step 2: No GPS coords → no progress bar appears (D-08 silent skip).
   - Step 4: SAVE RATING with valid values updates CourseCard and removes banner.
   - Step 5: Invalid values block SAVE RATING via inline validation.
</verification>

<success_criteria>
- `TileCacheRepository.preCacheCourse(holes)` uses `RectangleRegion` + `toDownloadable(minZoom: 14, maxZoom: 17)` + `FMTCStore('brdy_tiles').download.startForeground` per D-08/09
- Returns null when no holes have GPS data (D-08 silent skip)
- `tileCacheProgressProvider` cancels prior subscription before starting a new one; nullable DownloadProgress state
- `TileCacheProgress` widget renders inline below CourseCard with `'DOWNLOADING MAP TILES… N%'` locked copy and accent LinearProgressIndicator
- `MissingRatingBanner` uses LOCKED hex `0x26CC2200` bg, 3dp destructive left border, LOCKED copy, animated slide-in
- `ManualRatingForm` validates CR 55.0–80.0 and SLOPE 55–155; submit calls `selectedCourseProvider.notifier.overrideRating(...)`
- `CourseCard` conditionally renders banner OR form based on `_manualFormOpen` state and CR/Slope nullability
- `SetupScreen.build` uses `ref.listen` (not `ref.watch`) for one-shot pre-cache trigger; START ROUND remains unblocked
- Human-verify checkpoint Step 1 step 5: START ROUND tappable before download completes (D-10)
- FOUND-05 + SETUP-05 observable via the checkpoint
- Phase 1 COMPLETE: all 10 requirements (FOUND-01..05, SETUP-01..05) satisfied
</success_criteria>

<output>
Create `.planning/phases/01-foundation-setup/01-05-SUMMARY.md` when done with: files created, tile-cache flow verification result (course used, download started time, total tiles, completion observed), missing-rating verification result (course used, banner displayed, manual values entered, banner removed), build_runner output, test results, human-verify checkpoint outcome (which steps passed, any failures and resolutions), and final note: "Phase 1 complete — all 10 requirements satisfied; ready for /gsd:verify-work and /gsd:plan-phase 2".
</output>
