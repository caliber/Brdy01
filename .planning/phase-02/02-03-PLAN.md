---
phase: 02-shot-capture
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - lib/features/shot_capture/widgets/fairway_gir_toggles.dart
  - lib/features/shot_capture/widgets/hole_nav_drawer.dart
  - lib/features/shot_capture/widgets/outcome_button_grid.dart
  - lib/features/shot_capture/shot_capture_screen.dart
  - lib/features/setup/providers/round_setup_notifier.dart
autonomous: false
requirements:
  - SHOT-03
  - SHOT-04
  - SHOT-05
  - SHOT-06
  - SHOT-07
  - SHOT-12

must_haves:
  truths:
    - "Tapping +/- on the putts counter writes to Drift immediately on every tap with no debounce or buffer"
    - "Fairway toggle is completely absent from the widget tree (not just hidden) on par 3 holes"
    - "Tapping a hole chip in the nav strip sets activeHoleIndexProvider to that index — never calls context.go() or context.push()"
    - "A new round (createRound) resets activeHoleIndexProvider to 0 before navigating to shot capture"
    - "flutter analyze passes clean with zero issues across all modified files"
  artifacts:
    - path: "lib/features/shot_capture/widgets/fairway_gir_toggles.dart"
      provides: "FairwayGirToggles — fairway (par3 gate) + GIR + voice-stub toggles"
    - path: "lib/features/shot_capture/widgets/hole_nav_drawer.dart"
      provides: "HoleNavDrawer — 18-chip horizontal strip, back-to-current NOW chip, animated show/hide"
    - path: "lib/features/shot_capture/shot_capture_screen.dart"
      provides: "Full assembly: HoleNavDrawer toggled by hole number tap, OutcomeButtonGrid toggle wiring wired, PuttsCounter in bottom zone"
    - path: "lib/features/setup/providers/round_setup_notifier.dart"
      provides: "P2-08 fix: activeHoleIndexProvider.set(0) after createRound"
  key_links:
    - from: "PuttsCounter SUB/ADD tap"
      to: "HoleScoreNotifier.setPutts()"
      via: "ref.read(holeScoreNotifierProvider(roundId, holeIndex).notifier)"
      pattern: "setPutts"
    - from: "FairwayGirToggles fairway tap"
      to: "HoleScoreNotifier.setFairwayHit()"
      via: "ref.read(holeScoreNotifierProvider(roundId, holeIndex).notifier)"
      pattern: "setFairwayHit"
    - from: "HoleNavDrawer chip tap"
      to: "activeHoleIndexProvider.notifier.set()"
      via: "ref.read(activeHoleIndexProvider.notifier).set(targetIndex)"
      pattern: "activeHoleIndexProvider\\.notifier.*set"
    - from: "RoundSetupNotifier.createRound()"
      to: "activeHoleIndexProvider.notifier.set(0)"
      via: "ref.read(activeHoleIndexProvider.notifier).set(0)"
      pattern: "activeHoleIndexProvider.*set.*0"
---

<objective>
Complete Phase 2 by wiring fairway/GIR toggles, hole navigation strip, and the P2-08 index-reset fix; then complete the full ShotCaptureScreen assembly and confirm with flutter analyze.

Purpose: After this plan every Phase 2 requirement is satisfied. The app can record a complete 18-hole round with putts (via embedded SUB/ADD in the button grid), fairway, and GIR tracking, navigate back to correct any hole, and complete the round correctly when a new round starts.

Output: 5 files modified. No new @riverpod providers — no build_runner step needed. Ends with a human-verify checkpoint.
</objective>

<execution_context>
@/Users/simonnewland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonnewland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonnewland/development/brdy01/.planning/PROJECT.md
@/Users/simonnewland/development/brdy01/.planning/phase-02/02-RESEARCH.md
@/Users/simonnewland/development/brdy01/.planning/phase-02/02-PATTERNS.md
@/Users/simonnewland/development/brdy01/.planning/phase-02/02-UI-SPEC.md
@/Users/simonnewland/development/brdy01/.planning/phases/02-shot-capture/02-01-SUMMARY.md
@/Users/simonnewland/development/brdy01/.planning/phases/02-shot-capture/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FairwayGirToggles, HoleNavDrawer and P2-08 fix</name>

  <read_first>
    - lib/features/setup/widgets/course_result_tile.dart (InkWell per-item tap; touch target constraints)
    - lib/features/setup/providers/round_setup_notifier.dart (current file — BEFORE adding P2-08 fix; note existing imports)
    - lib/features/shot_capture/providers/active_hole_index_provider.dart (import path for the P2-08 fix)
    - lib/features/shot_capture/providers/hole_score_notifier.dart (setFairwayHit, setGir signatures — from plan 01)
    - lib/features/shot_capture/providers/hole_list_provider.dart (holeListProvider — used in HoleNavDrawer)
    - lib/features/shot_capture/providers/highest_scored_hole_index_provider.dart (highestScoredHoleIndexProvider — used in HoleNavDrawer)
    - lib/domain/enums/hole_outcome.dart (HoleOutcome values — for chip colour mapping)
    - lib/theme/brdy_colors.dart (all tokens)
    - lib/theme/brdy_spacing.dart (spacing constants)
    - .planning/phase-02/02-UI-SPEC.md (§7 Bottom Toggle Strip full spec; §9 Hole Navigation Strip chip states and dimensions; Interaction Contracts §Fairway and GIR Toggles, §Hole Back-Navigation)
    - .planning/phase-02/02-PATTERNS.md (FairwayGirToggles, HoleNavDrawer pattern excerpts)
  </read_first>

  <action>
  Create two widgets and apply the P2-08 round_setup_notifier fix.

  Note: PuttsCounter is NOT created as a standalone widget. Putts interaction is handled by the SUB/ADD buttons embedded in OutcomeButtonGrid Row 1 (plan 02). No separate putts_counter.dart is needed for Phase 2.

  **lib/features/shot_capture/widgets/fairway_gir_toggles.dart**

  A ConsumerWidget accepting `({required int roundId, required int holeIndex, required int holePar})`.

  This widget replaces the toggle stubs in OutcomeButtonGrid from plan 02. It renders the three-column bottom toggle strip per UI-SPEC §7.

  Each toggle is a Column(children: [Text(label, Barlow Condensed 12dp w700), Text(descriptor, Barlow Condensed 10dp w400 `BrdyColors.background` / `#0A0A0A`)]) wrapped in a Container with `BoxDecoration(color: fillColor, borderRadius: BorderRadius.circular(4), boxShadow: [BoxShadow(color: Colors.black.withOpacity(0.3), blurRadius: 3, offset: Offset(0,1))])` and `constraints: BoxConstraints(minHeight: 48)`, wrapped in InkWell.

  Toggle state is derived from `ref.watch(holeScoreNotifierProvider(roundId, holeIndex))`. The current hole data is `holeState.value` — null if not yet scored.

  FAIRWAY toggle:
  - Hidden entirely (not even a SizedBox placeholder) when `holePar == 3` — use `if (holePar != 3)` in the Row children.
  - State: `fairwayHit = holeState?.fairwayHit`. Active (HIT) when `fairwayHit == true`. Inactive (MISS) when `fairwayHit == false || fairwayHit == null`.
  - Active fill: `BrdyColors.accent`. Active label color: `BrdyColors.onAccent`.
  - Inactive fill: `const Color(0xFFD0D0D0)`. Inactive label color: `BrdyColors.background` (`#0A0A0A`).
  - On tap: `HapticFeedback.selectionClick()` then `ref.read(holeScoreNotifierProvider(roundId, holeIndex).notifier).setFairwayHit(!(fairwayHit ?? false))`. Toggle semantics: if currently HIT → set to MISS (false); if MISS/null → set to HIT (true).
  - Semantics label: `'FAIRWAY HIT — currently ${fairwayHit == true ? "hit" : "missed"}'`.

  GIR toggle:
  - Always rendered (no par gate).
  - State: `gir = holeState?.greenInRegulation`. Active (GREEN) when `gir == true`.
  - Active fill: `BrdyColors.background` (#0A0A0A). Active label: `BrdyColors.onSurface` (#F0F0F0).
  - Inactive fill: `const Color(0xFFD0D0D0)`. Inactive label: `BrdyColors.background`.
  - On tap: `HapticFeedback.selectionClick()` then `setGir(!(gir ?? false))`.
  - Semantics label: `'GREEN IN REGULATION — currently ${gir == true ? "on" : "off"}'`.

  VOICE toggle (Phase 2 stub):
  - Always rendered.
  - Always inactive (OFF state). `onTap` does nothing (`onTap: () {}`).
  - Fill: `const Color(0xFFD0D0D0)`. Label: `'OFF'` Barlow Condensed 12dp w700 `BrdyColors.background`. Descriptor: `'VOICE'`.
  - No Semantics label needed (inactive feature).

  Row structure:
  ```
  Row(children: [
    if (holePar != 3) Expanded(child: FairwayToggle),
    if (holePar != 3) SizedBox(width: BrdySpacing.sm),
    Expanded(child: GirToggle),
    SizedBox(width: BrdySpacing.sm),
    Expanded(child: VoiceToggle),
  ])
  ```

  IMPORTANT: The fairway toggle write target is `fairwayHit` — only ever write `true` or `false` from this widget. Writing `null` for par 3 is handled by `HoleScoreNotifier.recordOutcome()`, not this widget.

  **lib/features/shot_capture/widgets/hole_nav_drawer.dart**

  A ConsumerStatefulWidget accepting `({required int roundId})` with local bool `_isOpen = false`.

  This widget renders the animated hole navigation strip. It is shown/hidden by toggling `_isOpen` from outside (the ShotCaptureScreen calls a callback when the hole number is tapped, or expose a method via a GlobalKey). Simpler approach: accept `(bool isOpen)` as a parameter and let the screen control visibility.

  Use `flutter_animate` height animation:
  ```dart
  AnimatedContainer(
    duration: const Duration(milliseconds: 200),
    curve: Curves.easeOut,
    height: isOpen ? 56 : 0,
    child: SingleChildScrollView(
      scrollDirection: Axis.horizontal,
      child: Row(children: chips),
    ),
  )
  ```

  Reads:
  - `ref.watch(activeHoleIndexProvider)` — current active hole
  - `ref.watch(holeListProvider(roundId))` — scored holes with outcome colors
  - `ref.watch(highestScoredHoleIndexProvider(roundId))` — max navigable index

  18 hole chips (index 0..17) plus optionally a NOW chip:

  Each chip is a `GestureDetector` (to avoid InkWell overflow issues in a Row) wrapping a Container:
  - Size: exactly 40×40dp
  - BorderRadius.circular(4)
  - Fill and border per UI-SPEC §9 chip states:
    - Unscored: `BrdyColors.surface` fill, 1dp `BrdyColors.divider` border
    - Eagle: `Color(0xFFFFD700)` fill, `BrdyColors.background` text
    - Birdie: `BrdyColors.accent` fill, `BrdyColors.onAccent` text
    - Par: `Color(0xFF2563EB)` fill, `Color(0xFFFFFFFF)` text  // UI-SPEC §9 active-state colour; distinct from scorecard "Plain" (CLAUDE.md scorecard spec = scorecard cells only, not nav chips)
    - Bogey: `BrdyColors.surface` fill, `BrdyColors.onSurface` text with `TextDecoration.underline`
    - Double/Pickup: `BrdyColors.destructive` fill, `BrdyColors.onDestructive` text
    - Future (index > highestScoredHoleIndex + 1): `BrdyColors.divider` fill, `BrdyColors.onSurfaceMuted` text, `onTap: null`
    - Active chip (index == activeHoleIndex): add `Border.all(color: BrdyColors.onSurface, width: 2)` overlay
  - Text: Barlow Condensed 14dp w700; `(index + 1).toString()` — no zero-padding for chip labels
  - Gap between chips: `BrdySpacing.xs` (4dp) via SizedBox
  - `Semantics(label: 'Hole ${index+1}, ${outcomeLabel or "unscored"}')` on each chip

  On chip tap (when `onTap != null`):
  ```
  await HapticFeedback.lightImpact();
  ref.read(activeHoleIndexProvider.notifier).set(targetIndex);
  ```
  NEVER call `context.go()` or `context.push()` — CLAUDE.md constraint.

  NOW chip: rendered after the 18 chips when `activeHoleIndex < highestScoredHoleIndex`. Same 40×40dp size. Fill `BrdyColors.accent`, text `BrdyColors.onAccent`, label `'NOW'` Barlow Condensed 11dp w700. On tap: `ref.read(activeHoleIndexProvider.notifier).set(highestScoredHoleIndex)`.

  Outer padding: `EdgeInsets.symmetric(horizontal: BrdySpacing.sm, vertical: BrdySpacing.sm)`.

  Outcome color lookup helper (private method in the widget): accepts `String? outcomeName`, returns the chip fill color. Use `HoleOutcome.values.byName(outcomeName!)` — guard with `outcomeName == null` returning unscored color.

  **lib/features/setup/providers/round_setup_notifier.dart — P2-08 fix**

  Read the current file. After `ref.read(activeRoundIdProvider.notifier).set(roundId);`, add:
  ```dart
  // Reset hole index to 0 — prevents new round starting on hole 18 (Pitfall P2-08)
  ref.read(activeHoleIndexProvider.notifier).set(0);
  ```
  Add the new import at the top (following import order: dart: first, package: second, relative last):
  ```dart
  import '../../shot_capture/providers/active_hole_index_provider.dart';
  ```
  No other changes to this file.
  </action>

  <verify>
    <automated>cd /Users/simonnewland/development/brdy01 && flutter analyze --no-fatal-warnings lib/features/shot_capture/widgets/ lib/features/setup/providers/round_setup_notifier.dart 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - fairway_gir_toggles.dart uses `if (holePar != 3)` to remove fairway toggle from widget tree — not Visibility or Opacity
    - Fairway toggle calls `setFairwayHit(true)` when toggling to active and `setFairwayHit(false)` when toggling to inactive — never writes null from the toggle widget
    - GIR toggle calls `setGir(true/false)` with toggle semantics
    - Voice toggle has no logic — `onTap: () {}` placeholder
    - hole_nav_drawer.dart chip taps call `ref.read(activeHoleIndexProvider.notifier).set(targetIndex)` — never context.go or context.push
    - Chips for indices beyond highestScoredHoleIndex+1 have onTap: null (locked / future)
    - NOW chip appears when activeHoleIndex < highestScoredHoleIndex
    - hole_nav_drawer.dart uses `HoleOutcome.values.byName()` for outcome→color mapping — never hardcoded string comparison
    - round_setup_notifier.dart now calls `ref.read(activeHoleIndexProvider.notifier).set(0)` after setting activeRoundId
    - Import for active_hole_index_provider.dart added to round_setup_notifier.dart
    - flutter analyze reports no issues
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire full ShotCaptureScreen assembly and flutter analyze clean</name>

  <read_first>
    - lib/features/shot_capture/shot_capture_screen.dart (current state after plan 02 — read the full file before modifying)
    - lib/features/shot_capture/widgets/outcome_button_grid.dart (current state after plan 02 — note the fairway/GIR toggle stubs to replace)
    - lib/features/shot_capture/widgets/fairway_gir_toggles.dart (created in task 1 of this plan)
    - lib/features/shot_capture/widgets/hole_nav_drawer.dart (created in task 1 of this plan)
    - lib/features/shot_capture/widgets/putts_counter.dart (created in task 1 of this plan)
    - .planning/phase-02/02-UI-SPEC.md (Screen Layout §, Interaction Contracts § — confirm all wiring points)
  </read_first>

  <action>
  Wire the full screen assembly and run the final verification pass.

  **lib/features/shot_capture/shot_capture_screen.dart — final wiring**

  Read the current state from plan 02. Make the following additions/changes:

  1. Add local bool state: `bool _navStripOpen = false;`

  2. In the `_TopZone` (or inline in `build()`), wrap the hole number area so that tapping the giant hole number toggles `_navStripOpen`. The HoleHeader widget from plan 02 handles the chevron navigation — add a `GestureDetector` wrapping only the Stack containing the hole number text (not the chevron buttons) that calls `setState(() => _navStripOpen = !_navStripOpen)` with `HapticFeedback.selectionClick()`. Pass `_navStripOpen` to HoleNavDrawer.

  3. Insert HoleNavDrawer immediately below the top zone divider (or within the top zone Column below the SHOTS row). Structure: Place it inside the top zone Column, between the SHOTS row and the zone's bottom edge:
  ```dart
  // Inside _TopZone Column, after SHOTS row:
  HoleNavDrawer(roundId: roundId, isOpen: _navStripOpen),
  ```
  HoleNavDrawer animates its own height from 0 → 56dp.

  4. Replace the fairway/GIR toggle stubs in OutcomeButtonGrid: if the stubs were placed inline in the OutcomeButtonGrid's bottom toggle area, replace them with `FairwayGirToggles(roundId: roundId, holeIndex: holeIndex, holePar: holePar)`. This may require passing `holePar` into OutcomeButtonGrid if it doesn't already have it.

  5. The PuttsCounter widget: SUB/ADD buttons are embedded in the OutcomeButtonGrid Row 1 (plan 02). The standalone PuttsCounter widget from task 1 does NOT need to be added to the screen again — it would be a duplicate of the embedded buttons. Only add it if the plan 02 implementation left the SUB/ADD buttons as no-ops. If plan 02 fully wired SUB/ADD in the grid, this widget is available for reuse but not inserted separately.

  After wiring, run the full verification suite:

  6. `flutter analyze --no-fatal-warnings` — must report zero issues. Fix any issues found before marking complete.

  7. Grep for common pitfalls to confirm absence:
  - `grep -rn "context.push" lib/features/shot_capture/` → must return 0 results
  - `grep -rn "keepAlive.*true" lib/features/shot_capture/providers/` → must return 0 results for new providers (activeHoleIndexProvider is keepAlive but is not in this directory; all new providers are auto-dispose)
  - `grep -v "^//" lib/features/shot_capture/widgets/outcome_button_grid.dart | grep -c "onDoubleTap"` → must equal 1 (only BIRDIE)
  - `grep -n "persist" lib/features/shot_capture/shot_capture_screen.dart` → must find `persist: false`

  8. Run `flutter test` — must continue to pass the existing BrdyApp smoke test.
  </action>

  <verify>
    <automated>cd /Users/simonnewland/development/brdy01 && flutter analyze --no-fatal-warnings 2>&1 | tail -10 && flutter test 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - _navStripOpen local bool state controls HoleNavDrawer visibility
    - Tapping the giant hole number toggles nav strip (GestureDetector wrapping hole number only — not the chevrons)
    - FairwayGirToggles widget replaces any fairway/GIR toggle stubs from plan 02
    - HoleNavDrawer is placed in the screen layout and receives isOpen parameter
    - `grep -rn "context.push" lib/features/shot_capture/` returns 0 results
    - `flutter analyze --no-fatal-warnings` reports zero issues across the entire lib/ directory
    - `flutter test` passes (1 test — BrdyApp smoke test)
    - P2-08 fix is confirmed: `grep -n "set(0)" lib/features/setup/providers/round_setup_notifier.dart` finds the reset call
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 Shot Capture screen. All 10 requirements (SHOT-01 through SHOT-12 excluding GPS/voice which are Phase 5) are implemented:
    - SHOT-01: PAR, BOGEY, DOUBLE, PICKUP, BIRDIE outcome buttons
    - SHOT-02: EAGLE via double-tap BIRDIE
    - SHOT-03: Auto-advance to next hole after outcome tap
    - SHOT-04: 4-second undo toast with UNDO action
    - SHOT-05: Putts +/- counter with immediate Drift write
    - SHOT-06: Fairway hit toggle (absent on par 3)
    - SHOT-07: GIR toggle
    - SHOT-10: Running score badge always visible
    - SHOT-11: Hole number, par, SI displayed
    - SHOT-12: Hole navigation strip with back-to-current
  </what-built>

  <how-to-verify>
    Connect an Android device or launch an Android emulator (requires Android Studio). Run:
    `flutter run --dart-define=GOLF_API_KEY=<your-key>`

    Work through these checks in order — each one tests a specific requirement:

    1. SETUP FLOW: Load a course, tap START ROUND. Confirm Shot Capture opens on HOLE 01.

    2. SHOT-11 — Hole header: Confirm the top zone shows the course name + par, HCP value, HOLE pill in top-right, and the giant zero-padded hole number (e.g. "01") in the centre with the score badge showing "E".

    3. SHOT-01 — Outcome buttons: Tap PAR. Confirm the hole advances to HOLE 02 and the undo toast appears.

    4. SHOT-04 — Undo toast: On HOLE 02 tap BOGEY. Toast shows "BOGEY — HOLE 2". Tap UNDO within 4 seconds. Confirm hole returns to 02 with no recorded outcome (outcome buttons show no active state). Wait >4 seconds on a subsequent hole — confirm toast auto-dismisses.

    5. SHOT-02 — EAGLE double-tap: On any hole, double-tap BIRDIE. Confirm the BIRDIE button shows gold fill with "EAGLE" label and the score badge drops by 2 from previous. Single-tap BIRDIE on the next hole — confirm it shows orange (birdie) not gold.

    6. SHOT-10 — Running score: Score several holes (birdie, par, bogey). Confirm the score badge changes after each tap: birdie=-1, par=0, bogey=+1.

    7. SHOT-03 — Auto-advance: After each outcome tap, confirm the hole number advances immediately without any manual "NEXT" tap.

    8. SHOT-05 — Putts: On any hole, tap ADD three times. Confirm the putts display in the button grid shows 3. Kill and relaunch the app (to Shot Capture on the same round). Confirm putts still shows 3 on that hole (Drift persistence).

    9. SHOT-06 — Fairway toggle: On a par 4 or 5 hole, confirm the FAIRWAY toggle is present. Tap it — confirm it highlights as HIT. Navigate to a par 3 hole — confirm the FAIRWAY toggle is completely absent (no grayed-out version, no "N/A" label).

    10. SHOT-07 — GIR toggle: Tap GIR toggle. Confirm it activates (GREEN/dark fill). Tap again — confirms it deactivates.

    11. SHOT-12 — Hole navigation: Score several holes. Tap the giant hole number — confirm the navigation strip slides in showing scored holes coloured by outcome. Tap hole 1's chip — confirm the screen shows HOLE 01 with the previously recorded outcome. Make a correction (tap a different outcome) — confirm the score badge updates. Tap NOW chip — confirm the screen returns to the current live hole.

    12. ROUND COMPLETION: Score all 18 holes (or use back-navigation to fill in gaps). After hole 18 is scored, confirm the app navigates automatically to the Round Review screen (placeholder for now).

    13. NEW ROUND TEST (P2-08): From Round Review, tap back or navigate to Setup. Start a new round. Confirm it opens on HOLE 01, not HOLE 18.
  </how-to-verify>

  <resume-signal>
    Type "approved" when all 13 checks pass, or describe which checks failed (e.g. "SHOT-06 failed — fairway visible on par 3").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HoleNavDrawer chip tap → activeHoleIndexProvider | Index must be within valid range [0..highestScoredHoleIndex]; future chips are disabled |
| FairwayGirToggles → HoleScoreNotifier | Toggle writes boolean; only called when hole has outcome (fairway/GIR are supplementary) |
| RoundSetupNotifier.createRound → activeHoleIndexProvider.set(0) | Reset must happen before context.go to shot-capture — order of calls is critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | HoleNavDrawer — chip index out of range | mitigate | `onTap: null` for chips where `index > highestScoredHoleIndex + 1`; chip index is 0..17 (bounded) |
| T-02-11 | Denial of Service | FairwayGirToggles tapped before outcome recorded | accept | `setFairwayHit` and `setGir` in the notifier guard with `if (current == null) return` — no-op if not yet scored |
| T-02-12 | Tampering | P2-08 race: activeHoleIndexProvider.set(0) fires after navigation | mitigate | set(0) called synchronously inside createRound before returning roundId; navigation is triggered by the caller after await — ordering guaranteed |
| T-02-13 | Repudiation | fairwayHit written as false instead of null for par 3 | mitigate | FairwayGirToggles never writes null; the par 3 null guard is in HoleScoreNotifier.recordOutcome (plan 01) — these two layers cannot conflict because fairway toggle is hidden on par 3 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 installs zero new packages |
</threat_model>

<verification>
Final Phase 2 state checks (run before human-verify checkpoint):

1. `flutter analyze --no-fatal-warnings` — zero issues
2. `flutter test` — passes (BrdyApp smoke test)
3. `grep -rn "context.push" lib/features/shot_capture/` — must be 0 results
4. `grep -v "^//" lib/features/shot_capture/widgets/outcome_button_grid.dart | grep -c "onDoubleTap"` — must equal 1
5. `grep -n "persist: false" lib/features/shot_capture/shot_capture_screen.dart` — must find persist: false
6. `grep -rn "keepAlive.*true" lib/features/shot_capture/providers/ | grep -v ".g.dart"` — must be 0 results (no new keepAlive)
7. `grep -n "set(0)" lib/features/setup/providers/round_setup_notifier.dart` — must find the P2-08 reset call
8. `grep -n "if.*holePar.*!=.*3\|if.*par.*!=.*3" lib/features/shot_capture/widgets/fairway_gir_toggles.dart` — must find the par3 gate
</verification>

<success_criteria>
- All 10 Phase 2 requirements are implemented (SHOT-01..07, SHOT-10..12)
- Putts, fairway, and GIR write through to Drift immediately on every interaction
- Hole navigation strip works without creating a route back-stack
- New round always starts on hole 1 (P2-08 fix confirmed)
- flutter analyze clean
- flutter test passes
- Human-verify checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/02-shot-capture/02-03-SUMMARY.md` when done.
</output>
