---
phase: 02-shot-capture
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/features/shot_capture/shot_capture_screen.dart
  - lib/features/shot_capture/widgets/outcome_button_grid.dart
  - lib/features/shot_capture/widgets/hole_header.dart
  - lib/features/shot_capture/widgets/score_bar.dart
autonomous: true
requirements:
  - SHOT-01
  - SHOT-02
  - SHOT-03
  - SHOT-04
  - SHOT-10
  - SHOT-11

must_haves:
  truths:
    - "Tapping PAR, BOGEY, DOUBLE, PICKUP records outcome to Drift immediately then advances activeHoleIndexProvider to next hole"
    - "Double-tapping BIRDIE records HoleOutcome.eagle; single-tapping BIRDIE records HoleOutcome.birdie (GestureDetector.onDoubleTap on BIRDIE only — no other button has onDoubleTap)"
    - "Undo toast appears for exactly 4 seconds with persist:false and a functional UNDO action that clears the outcome in Drift and returns to the previous hole"
    - "Running score badge shows E/+n/-n and rebuilds reactively on every Drift write without requiring user interaction"
    - "Hole header shows zero-padded hole number, par, and SI (or — when null) pulled from courseForRoundProvider"
  artifacts:
    - path: "lib/features/shot_capture/shot_capture_screen.dart"
      provides: "Full ShotCaptureScreen — two-zone layout, ref.listen for round completion, undo toast orchestration, NEXT button"
    - path: "lib/features/shot_capture/widgets/outcome_button_grid.dart"
      provides: "OutcomeButtonGrid — 4+4 button layout with dot indicators, BIRDIE GestureDetector, scale animations"
    - path: "lib/features/shot_capture/widgets/hole_header.dart"
      provides: "HoleHeader — course info row, giant hole number + score badge pill, SHOTS total, left/right chevrons"
    - path: "lib/features/shot_capture/widgets/score_bar.dart"
      provides: "ScoreBar — accent pill badge, reactive from runningScoreProvider"
  key_links:
    - from: "OutcomeButtonGrid._recordOutcome()"
      to: "HoleScoreNotifier.recordOutcome()"
      via: "ref.read(holeScoreNotifierProvider(roundId, holeIndex).notifier)"
      pattern: "holeScoreNotifierProvider.*notifier.*recordOutcome"
    - from: "ShotCaptureScreen"
      to: "roundCompleteProvider"
      via: "ref.listen<bool>(roundCompleteProvider(roundId), ...)"
      pattern: "ref\\.listen.*roundCompleteProvider"
    - from: "ShotCaptureScreen._handleOutcomeTapped()"
      to: "activeHoleIndexProvider.notifier.set()"
      via: "ref.read(activeHoleIndexProvider.notifier).set(nextIndex)"
      pattern: "activeHoleIndexProvider\\.notifier.*set"
---

<objective>
Replace the ShotCaptureScreen placeholder with the full scoring UI: the two-zone layout, outcome button grid with EAGLE double-tap, undo toast, hole header, running score badge, and NEXT button.

Purpose: After this plan the app is a working scoring tool end-to-end — tapping outcome buttons records to Drift, advances the hole, shows the undo toast, and updates the score bar reactively.

Output: 4 files (1 replaced, 3 new). No build_runner step needed — no new @riverpod files created in this plan; all providers come from plan 01.
</objective>

<execution_context>
@/Users/simonnewland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonnewland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonnewland/development/brdy01/.planning/PROJECT.md
@/Users/simonnewland/development/brdy01/.planning/phase-02/02-RESEARCH.md
@/Users/simonnewland/development/brdy01/.planning/phase-02/02-PATTERNS.md
@/Users/simonnewland/development/brdy01/.planning/phase-02/02-UI-SPEC.md
@/Users/simonnewland/development/brdy01/.planning/phases/02-shot-capture/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build HoleHeader and ScoreBar widgets</name>

  <read_first>
    - lib/features/setup/widgets/course_card.dart (ConsumerWidget + async .when() pattern; GoogleFonts usage; BrdyColors tokens)
    - lib/features/shot_capture/providers/active_hole_index_provider.dart (activeHoleIndexProvider — ref.watch to get current holeIndex)
    - lib/features/shot_capture/providers/course_for_round_provider.dart (courseForRoundProvider(roundId) — created in plan 01; AsyncValue<CourseModel?>)
    - lib/features/shot_capture/providers/running_score_provider.dart (runningScoreProvider(roundId) — int? from plan 01)
    - lib/domain/models/course_model.dart (CourseModel fields; holes list; HoleModel.par, HoleModel.strokeIndex)
    - lib/theme/brdy_colors.dart (all 9 color tokens)
    - lib/theme/brdy_spacing.dart (spacing constants)
    - .planning/phase-02/02-UI-SPEC.md (Component Contracts §2 Giant Hole Number, §3 SHOTS Total, §1 Course Info Row; running score badge spec; animation contract §Hole number change and §Running score badge)
    - .planning/phase-02/02-PATTERNS.md (HoleHeader full pattern excerpt; ScoreBar badge pill excerpt)
  </read_first>

  <action>
  Create two widgets.

  **lib/features/shot_capture/widgets/hole_header.dart**

  A ConsumerWidget accepting `(int roundId)` that renders the entire top zone content. It watches `activeHoleIndexProvider` and `courseForRoundProvider(roundId)`. While the course is loading, render a minimal placeholder (SizedBox.shrink() for course-dependent elements, but always show the hole number from activeHoleIndexProvider).

  Structure (from top to bottom inside a Column with BrdySpacing.lg gap, padded BrdySpacing.md horizontally):

  1. Course info row: `Row(mainAxisAlignment: MainAxisAlignment.spaceBetween)` with:
     - Left: Column(2dp gap) with line 1 `'{COURSE NAME} PAR {n}'` and line 2 `'HCP {n}'`. Truncate course name at 16 chars with `…` if needed. Barlow Condensed 11dp w700 `BrdyColors.onSurfaceMuted`. When course is loading/null, show `'— PAR —'` and `'HCP —'`.
     - Right: HOLE pill — Container with `Border.all(color: BrdyColors.onSurface, width: 1)`, `BorderRadius.circular(4)`, `EdgeInsets.symmetric(horizontal: 8, vertical: 4)`. Text: `'HOLE ${holeIndex + 1}'` Barlow Condensed 11dp w700 `BrdyColors.onSurface`.

  2. Giant hole number row: `Row(mainAxisAlignment: MainAxisAlignment.center)` with:
     - Left chevron `IconButton` (`Icons.chevron_left`, size 32dp, `BrdyColors.onSurface`); minimum 48×48dp via `constraints: BoxConstraints(minWidth: 48, minHeight: 48)`; disabled (`onPressed: null`, color `BrdyColors.onSurfaceMuted`) when `holeIndex == 0`. On tap: `HapticFeedback.lightImpact()`, then `ref.read(activeHoleIndexProvider.notifier).set(holeIndex - 1)`. Include Semantics label.
     - Centre: Stack(clipBehavior: Clip.none) containing:
       - Hole number Text: `(holeIndex + 1).toString().padLeft(2, '0')` using `GoogleFonts.jetBrainsMono(fontSize: 96, fontWeight: FontWeight.w700, height: 1.0, color: BrdyColors.onSurface)`. Wrap in `flutter_animate` fade-out/in (50ms + 100ms) keyed on `holeIndex`.
       - `Positioned(right: -8, top: 0)` child: ScoreBar widget (see below).
     - Right chevron: same as left but `Icons.chevron_right`; disabled when `holeIndex >= highestScoredHoleIndex`. To get `highestScoredHoleIndex`, accept it as a parameter passed from the screen (to avoid double-watching the provider).

  3. SHOTS total: `Text('SHOTS $totalShots')` centred. `totalShots` = sum of `(par + offset)` across all scored holes where offset is eagle=-2, birdie=-1, par=0, bogey=+1, doubleBogey=+2, pickup=+2. Derive directly from the same holeListProvider watch in the screen (pass as a parameter, or watch holeListProvider here — prefer watch here to keep the screen clean). Barlow Condensed 14dp w700 `BrdyColors.onSurface`. Show `'SHOTS 0'` when no holes scored.

  Semantics label on the hole number Stack: `'Hole ${holeIndex + 1} of 18 — tap to navigate holes'` (tapping the hole number to open nav strip is wired in plan 03).

  **lib/features/shot_capture/widgets/score_bar.dart**

  A ConsumerWidget accepting `(int roundId)`. Watches `runningScoreProvider(roundId)`. Renders an accent pill badge (the score badge spec from UI-SPEC §2):
  - Container with `BrdyColors.accent` fill, `BorderRadius.circular(12)`, `EdgeInsets.symmetric(horizontal: 8, vertical: 4)`.
  - `display` string: `score == null ? '–' : score == 0 ? 'E' : score > 0 ? '+$score' : '$score'` (negative shows as '-1' with ASCII hyphen-minus).
  - Text: `GoogleFonts.jetBrainsMono(fontSize: 18, fontWeight: FontWeight.w700, color: BrdyColors.onAccent)`.
  - Wrap in `flutter_animate` `.animate(key: ValueKey(score)).scale(begin: const Offset(1.0, 1.0), end: const Offset(1.1, 1.1), duration: 75.ms).then().scale(end: const Offset(1.0, 1.0), duration: 75.ms)` — pulse on score change.
  - Semantics label: `'Running score ${display} versus par'`.

  Both files must follow the import order from 02-PATTERNS.md: dart: first, package: second, relative last. ALL text strings in ALL CAPS as per CLAUDE.md.
  </action>

  <verify>
    <automated>cd /Users/simonnewland/development/brdy01 && flutter analyze --no-fatal-warnings lib/features/shot_capture/widgets/hole_header.dart lib/features/shot_capture/widgets/score_bar.dart 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - hole_header.dart is a ConsumerWidget (not ConsumerStatefulWidget) with parameter `int roundId`
    - Hole number is zero-padded via `.toString().padLeft(2, '0')`
    - Left chevron is disabled (onPressed: null) when holeIndex == 0
    - Course name truncated at 16 chars with ellipsis when longer
    - SHOTS total computed from holePar + outcome offset — not from putts count
    - score_bar.dart displays 'E' for score==0, '+n' for positive, '-n' for negative (ASCII hyphen not en-dash)
    - Score badge uses BrdyColors.accent fill always — does NOT change color based on score sign
    - flutter animate is applied to hole number (fade key on holeIndex) and score badge (scale key on score)
    - No hardcoded color literals — only BrdyColors tokens and the two approved inline colors (0xFFFFD700 gold, 0xFF2563EB blue)
    - flutter analyze reports no issues on these two files
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build OutcomeButtonGrid and replace ShotCaptureScreen</name>

  <read_first>
    - lib/features/shot_capture/shot_capture_screen.dart (current placeholder — read to understand what to replace)
    - lib/features/setup/setup_screen.dart (ConsumerStatefulWidget pattern; ref.listen pattern; ScaffoldMessenger.showSnackBar pattern; SnackBar structure)
    - lib/features/setup/widgets/course_result_tile.dart (InkWell pattern on dark surface; splashColor/highlightColor on dark bg)
    - lib/features/shot_capture/providers/hole_score_notifier.dart (holeScoreNotifierProvider(roundId, holeIndex); .notifier.recordOutcome / undoOutcome)
    - lib/features/shot_capture/providers/active_hole_index_provider.dart (activeHoleIndexProvider; .notifier.set(index))
    - lib/features/shot_capture/providers/round_complete_provider.dart (roundCompleteProvider(roundId) — bool)
    - lib/features/shot_capture/providers/course_for_round_provider.dart (courseForRoundProvider — to get hole par/SI)
    - lib/features/shot_capture/providers/highest_scored_hole_index_provider.dart
    - lib/app/router.dart (route /round-review/:roundId — used in context.go)
    - .planning/phase-02/02-UI-SPEC.md (§5 Outcome Button Grid full spec; §7 Bottom Toggle Strip; §6 Pagination Dots; §8 Undo Toast; Screen Layout ASCII diagram; Interaction Contracts §Outcome Recording, §EAGLE Double-Tap, §NEXT Button, §Undo)
    - .planning/phase-02/02-PATTERNS.md (ShotCaptureScreen full pattern: two-zone layout, ref.listen, SnackBar undo; OutcomeButtonGrid structure)
  </read_first>

  <action>
  Create OutcomeButtonGrid and replace ShotCaptureScreen. This is the most complex task in Phase 2 — read the UI-SPEC carefully before writing any code.

  **lib/features/shot_capture/widgets/outcome_button_grid.dart**

  A ConsumerWidget accepting `({required int roundId, required int holeIndex, required int holePar, required int? holeStrokeIndex, required void Function(HoleOutcome outcome, int par, int? si) onOutcomeTapped})`.

  The widget renders two rows of 4 buttons. Each button column has three stacked elements: abbreviated label (above), dot indicator, then the button itself.

  BUTTON LAYOUT — Row 1 (left to right): PAR, SUB (putts minus), ADD (putts plus), BIRDY
  BUTTON LAYOUT — Row 2 (left to right): PICKUP, BOGEY, DOUBLE, NEXT

  For each outcome button column build a private method or widget `_ButtonColumn` that takes `(abbrev, label, outcome, activeColor, dotActiveColor, isActive)`. Inner structure:
  - Text(abbrev, Barlow Condensed 10dp w700 `Color(0xFF4A4A4A)`, textAlign: center)
  - SizedBox(height: BrdySpacing.xs)
  - Container 6dp circle: filled `dotActiveColor` when `isActive`, outlined (1dp `dotActiveColor`, transparent fill) when not active
  - SizedBox(height: BrdySpacing.xs)
  - Button: `InkWell` wrapping a `Container` (except BIRDY — see below)

  Button Container spec (all outcome buttons and NEXT):
  - `BoxDecoration(color: isActive ? activeColor : BrdyColors.surface, borderRadius: BorderRadius.circular(8), boxShadow: [BoxShadow(color: Colors.black.withOpacity(0.4), blurRadius: 4, offset: Offset(0, 2))])`
  - `constraints: const BoxConstraints(minWidth: 64, minHeight: 80)` — CLAUDE.md mandate
  - Text: Barlow Condensed 13dp w700; color depends on active state — see UI-SPEC §5 per-button colour table
  - ALL CAPS label text

  Active state: read current hole outcome from `ref.watch(holeScoreNotifierProvider(roundId, holeIndex))`. The outcome stored in Drift is `HoleOutcome.name` — parse with `HoleOutcome.values.byName()` to compare. Do NOT use `== 'double'` comparisons.

  BIRDY button specifics:
  - When outcome is eagle: button fill becomes `Color(0xFFFFD700)`, text becomes `BrdyColors.background`, abbrev becomes `'EGL'`, dot fills gold. Apply `flutter_animate` color-tween (150ms) when transitioning to/from eagle state.
  - Wrapped in `GestureDetector(onTap: () => onOutcomeTapped(HoleOutcome.birdie, holePar, holeStrokeIndex), onDoubleTap: () => onOutcomeTapped(HoleOutcome.eagle, holePar, holeStrokeIndex), child: _buttonBody)` — NOT InkWell.
  - Semantics label: `'BIRDIE outcome button — double tap for EAGLE'`.

  SUB and ADD buttons (putts controls, positions 2 and 3 in Row 1):
  - These are NOT outcome buttons. They do NOT call `onOutcomeTapped`.
  - On tap: `ref.read(holeScoreNotifierProvider(roundId, holeIndex).notifier).setPutts(currentPutts ± 1)` with `HapticFeedback.selectionClick()`.
  - SUB: disabled (`onPressed: null` equivalent — wrap InkWell onTap in `putts > 0 ? () => ... : null`) when putts == 0. Current putts from `ref.watch(holeScoreNotifierProvider(roundId, holeIndex)).value?.putts ?? 0`.
  - ADD: always enabled.
  - No dot indicator concept for SUB/ADD — use a neutral dot (outlined, `BrdyColors.onSurfaceMuted` border).
  - Button background is always `BrdyColors.surface` (no active state).

  NEXT button (position 4 Row 2):
  - NOT an outcome button. Does NOT record any score.
  - Always accent-colored. Disabled (opacity 0.4 on button, `onPressed: null`) when current hole has no recorded outcome (`holeState?.value?.outcome == null`).
  - On tap: haptic lightImpact, then `onOutcomeTapped` is NOT called. Instead accept a `void Function() onNextTapped` parameter and call that.
  - No dot indicator. Abbrev label `'HLE'`.
  - `Semantics(label: 'Advance to next hole')`.

  Button press animation: wrap each button Container in `flutter_animate` `.animate(target: _pressing ? 1 : 0).scale(begin: const Offset(1,1), end: const Offset(0.95,0.95), duration: 80.ms).then().scale(end: const Offset(1,1), duration: 80.ms)`. Trigger via a short StatefulWidget or use a simpler `GestureDetector` with `onTapDown`/`onTapUp` + setState.

  InkWell ripple in bottom zone (light surface): `splashColor: Colors.black.withOpacity(0.08)`, `highlightColor: Colors.black.withOpacity(0.04)`.

  PAGINATION DOTS: Add a Row below the two button rows with two 8dp circle dots. Dot 1 active (filled `BrdyColors.background`), Dot 2 inactive (outlined 1dp `BrdyColors.background`). Gap 8dp.

  BOTTOM TOGGLE STRIP — Phase 2 stub (fairway/GIR are wired in plan 03, voice is always OFF): Render the three-column Row structure per UI-SPEC §7. FAIRWAY and GIR toggles call into `holeScoreNotifierProvider` (wire logic in plan 03). In plan 02, render them with correct visual state (read from holeState) but with no-op onTap for now — `onTap: null` acceptable as a placeholder only if you leave a TODO comment. VOICE toggle is always inactive, `onTap: null`, no TODO needed. Each toggle: min 48dp height, `BorderRadius.circular(4)`, labels ALL CAPS.

  The Fairway toggle: hidden entirely (not rendered) when `holePar == 3`. When hidden, GIR and VOICE expand to fill the row equally.

  Gap between Row 1 and Row 2: `BrdySpacing.sm` (8dp). Gap between buttons in a row: `BrdySpacing.xs` (4dp). Gap between pagination dots and toggle strip: `BrdySpacing.sm`.

  **lib/features/shot_capture/shot_capture_screen.dart**

  Replace the placeholder entirely. This is a `ConsumerStatefulWidget` (needs local state for `_lastScoredHoleIndex`).

  Screen structure:
  ```
  Scaffold(
    backgroundColor: BrdyColors.background,
    body: SafeArea(
      child: Column(children: [
        Expanded(flex: 55, child: _TopZone(...)),
        Container(height: 1, color: BrdyColors.divider),
        Expanded(flex: 45, child: _BottomZone(...)),
      ]),
    ),
  )
  ```

  In `build()` BEFORE `return Scaffold`:
  1. `final roundId = widget.roundId;`
  2. `final holeIndex = ref.watch(activeHoleIndexProvider);`
  3. `final highestIndex = ref.watch(highestScoredHoleIndexProvider(roundId));`
  4. Watch `courseForRoundProvider(roundId)` — get holePar and holeStrokeIndex from `course?.holes[holeIndex]`.
  5. `ref.listen<bool>(roundCompleteProvider(roundId), (prev, next) { if (next == true) { context.go('/round-review/$roundId'); } });`

  **_TopZone:** Padding(BrdySpacing.md horizontal) wrapping a Column containing:
  - BRDY.01 wordmark rule: `Row([ Text('BRDY.01', ...), Gap(BrdySpacing.sm), Expanded(child: Divider(color: BrdyColors.divider, thickness: 1)) ])`. Wait — per UI-SPEC the wordmark rule is in the BOTTOM zone, not the top zone. Top zone contains the HoleHeader widget only.
  - HoleHeader(roundId: roundId) passing `highestScoredHoleIndex: highestIndex`. (HoleHeader is built in task 1.)

  **_BottomZone:** Color `BrdyColors.onSurface` (#F0F0F0) background. Padding(BrdySpacing.md horizontal) wrapping Column(children):
  1. BRDY.01 wordmark rule: `Row([ Text('BRDY.01', GoogleFonts.barlowCondensed(14, w700, BrdyColors.background)), Gap(BrdySpacing.sm), Expanded(child: Divider(color: BrdyColors.divider, thickness: 1)) ])`.
  2. Gap(BrdySpacing.sm).
  3. OutcomeButtonGrid(roundId, holeIndex, holePar, holeStrokeIndex, onOutcomeTapped: _handleOutcomeTapped, onNextTapped: _handleNext).

  **_handleOutcomeTapped(HoleOutcome outcome, int par, int? si):**
  ```
  1. Store _lastScoredHoleIndex = holeIndex (from ref.read for safety in async)
  2. await HapticFeedback.mediumImpact()
  3. await ref.read(holeScoreNotifierProvider(roundId, holeIndex).notifier)
         .recordOutcome(outcome: outcome, par: par, strokeIndex: si)
  4. _showUndoToast(context, outcome, holeIndex + 1)
  5. If holeIndex < 17: ref.read(activeHoleIndexProvider.notifier).set(holeIndex + 1)
     // If holeIndex == 17, roundCompleteProvider fires to true → ref.listen navigates
  ```

  **_handleNext():**
  ```
  1. await HapticFeedback.lightImpact()
  2. final hi = ref.read(activeHoleIndexProvider)
  3. if (hi < 17) ref.read(activeHoleIndexProvider.notifier).set(hi + 1)
  ```

  **_showUndoToast(context, outcome, holeNumber):** As specified in UI-SPEC §8 and PATTERNS.md. Use `outcome.name` to look up the toast text (map: eagle→'EAGLE', birdie→'BIRDIE', par→'PAR', bogey→'BOGEY', doubleBogey→'DOUBLE', pickup→'PICKUP'). `ScaffoldMessenger.of(context)..hideCurrentSnackBar()..showSnackBar(SnackBar(content: Text('$label — HOLE $holeNumber'), backgroundColor: BrdyColors.surface, duration: Duration(seconds: 4), persist: false, action: SnackBarAction(label: 'UNDO', textColor: BrdyColors.accent, onPressed: _handleUndo)))`. Content text: Barlow Condensed 14dp w400 `BrdyColors.onSurface`.

  **_handleUndo():**
  ```
  1. final last = _lastScoredHoleIndex; if (last == null) return;
  2. await HapticFeedback.lightImpact()
  3. await ref.read(holeScoreNotifierProvider(roundId, last).notifier).undoOutcome()
  4. ref.read(activeHoleIndexProvider.notifier).set(last)
  5. setState(() => _lastScoredHoleIndex = null)
  ```

  Import order: dart: first, package: (flutter, riverpod, go_router, gap, google_fonts, flutter_animate) second, relative last. All strings ALL CAPS.
  </action>

  <verify>
    <automated>cd /Users/simonnewland/development/brdy01 && flutter analyze --no-fatal-warnings 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - ShotCaptureScreen is ConsumerStatefulWidget, not ConsumerWidget
    - Two-zone layout uses Expanded(flex:55) + Expanded(flex:45) separated by 1dp BrdyColors.divider container
    - ref.listen(roundCompleteProvider(roundId)) is present and calls context.go('/round-review/$roundId')
    - _handleOutcomeTapped stores _lastScoredHoleIndex BEFORE the Drift write (captures correct pre-advance index)
    - _handleOutcomeTapped advances activeHoleIndexProvider AFTER the Drift write (not before)
    - SnackBar has persist: false (critical — Flutter 3.38+ will not auto-dismiss without this)
    - SnackBar content text label maps 'doubleBogey' outcome name to 'DOUBLE' display text
    - GestureDetector wraps BIRDIE button only — PAR/BOGEY/DOUBLE/PICKUP/NEXT use InkWell
    - OutcomeButtonGrid.onDoubleTap is on the BIRDIE column only — not on any parent container
    - NEXT button is disabled (onTap: null equivalent) when holeState?.outcome == null
    - SUB putts button is disabled when putts == 0
    - Fairway toggle is absent from widget tree (not just hidden) when holePar == 3
    - Outcome buttons have BoxConstraints(minWidth: 64, minHeight: 80) — CLAUDE.md mandate
    - All text is ALL CAPS — no sentence case or title case
    - No context.push() anywhere — only context.go() for round completion
    - flutter analyze reports no issues
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap → _handleOutcomeTapped | Outcome enum is typed — no injection risk; par/SI come from Drift-sourced CourseModel |
| ref.listen roundCompleteProvider → context.go() | Navigation fires when roundCompleteProvider emits true; must not fire on stale build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | SnackBar UNDO — _lastScoredHoleIndex nullable | mitigate | Guard `if (last == null) return` in _handleUndo prevents stale undo after toast dismissed |
| T-02-06 | Denial of Service | GestureDetector 300ms BIRDIE delay | accept | Documented known Flutter behavior (flutter/flutter#50458); acceptable for golf app; golfer deliberates before tapping |
| T-02-07 | Repudiation | context.go() fires twice if roundCompleteProvider emits twice | mitigate | GoRouter replaceStack semantics — navigating to same route twice is idempotent; no data corruption |
| T-02-08 | Information Disclosure | courseJson deserialization in courseForRoundProvider | accept | Local device data; no PII exposed over network |
| T-02-09 | Tampering | NEXT tap when holeIndex == 17 | mitigate | _handleNext guards if (hi < 17) — prevents advancing past hole 18; round completion handled by roundCompleteProvider |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 installs zero new packages |
</threat_model>

<verification>
After both tasks complete:

1. `flutter analyze --no-fatal-warnings` — zero issues on all 4 modified files
2. `grep -n "persist: false" lib/features/shot_capture/shot_capture_screen.dart` — must find persist: false in SnackBar
3. `grep -n "onDoubleTap" lib/features/shot_capture/widgets/outcome_button_grid.dart` — must appear exactly once (BIRDIE only)
4. `grep -n "context.push" lib/features/shot_capture/` — must return 0 results
5. `grep -n "roundCompleteProvider" lib/features/shot_capture/shot_capture_screen.dart` — must find ref.listen usage
6. `grep -n "minHeight: 80" lib/features/shot_capture/widgets/outcome_button_grid.dart` — must find tap target constraint
7. `grep -rn "context.go" lib/features/shot_capture/shot_capture_screen.dart` — must find '/round-review/' target
</verification>

<success_criteria>
- Tapping any outcome button records to Drift and advances the hole
- EAGLE is only achievable via BIRDIE double-tap
- Undo toast appears for 4 seconds with working UNDO action
- Running score badge updates reactively
- Hole header shows correct hole number, par, and SI
- flutter analyze clean across all 4 files
</success_criteria>

<output>
Create `.planning/phases/02-shot-capture/02-02-SUMMARY.md` when done.
</output>
