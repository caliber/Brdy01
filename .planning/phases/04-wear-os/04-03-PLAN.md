---
phase: 04-wear-os
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - watch/lib/main.dart
  - watch/lib/app.dart
  - watch/lib/theme/brdy_colors.dart
  - watch/lib/providers/hole_state_provider.dart
  - watch/lib/screens/score_screen.dart
  - watch/lib/widgets/outcome_button.dart
  - watch/lib/widgets/connection_dot.dart
  - watch/pubspec.yaml
autonomous: true
requirements: [WEAR-01, WEAR-03]

must_haves:
  truths:
    - "Watch app launches into a circular-safe score screen showing hole number, par, and 6 outcome buttons"
    - "Each button is colour-coded per the BRDY system: EAGLE gold, BIRDIE green, PAR blue, BOGEY surface, DOUBLE orange, PICKUP red"
    - "A connection status dot is visible at all times — green when phone reachable, grey when not"
    - "When disconnected, buttons are disabled and a 'NO PHONE' label appears"
    - "Tapping a button triggers haptic + WearDataService.sendScore() — does not crash if offline (the put queues)"
    - "Ambient mode dims the display to a minimal hole-number-only readout"
  artifacts:
    - path: "watch/lib/screens/score_screen.dart"
      provides: "Main watch scoring UI"
      contains: "class ScoreScreen"
    - path: "watch/lib/widgets/outcome_button.dart"
      provides: "Color-coded circular tap target"
      contains: "class OutcomeButton"
    - path: "watch/lib/theme/brdy_colors.dart"
      provides: "BRDY colour constants for watch"
      contains: "abstract class BrdyColors"
  key_links:
    - from: "watch/lib/screens/score_screen.dart"
      to: "WearDataService.sendScore"
      via: "OutcomeButton onTap"
      pattern: "sendScore\\("
    - from: "watch/lib/screens/score_screen.dart"
      to: "holeStateStream"
      via: "Riverpod StreamProvider"
      pattern: "holeStateStream|holeStateProvider"
    - from: "watch/lib/screens/score_screen.dart"
      to: "connectionStatusProvider"
      via: "ref.watch"
      pattern: "connectionStatusProvider"
---

<objective>
Build the watch's user-facing UI: a circular-safe scoring screen with hole context, six colour-coded outcome buttons, a connection-status indicator, disabled state when offline, and an ambient-mode minimal display. The UI consumes services from Plan 04-02.

Purpose: Realises WEAR-01 (visible hole + buttons) and the user-visible half of WEAR-03 (connection indicator + disabled state when offline).

Output: A standalone watch app the developer can drive end-to-end on the Wear OS emulator (without phone yet — Plan 04-04 wires the phone-side response).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wear-os/04-RESEARCH.md
@.planning/phases/04-wear-os/04-02-SUMMARY.md

@lib/theme/brdy_colors.dart
@lib/theme/brdy_text_styles.dart

<interfaces>
From watch/lib/services/wear_data_service.dart (Plan 04-02):
```dart
class WearDataService {
  Future<bool> sendScore({required int holeNumber, required WatchOutcome outcome, DateTime? timestamp});
  Stream<HoleState> get holeStateStream;
}
final wearDataServiceProvider = Provider<WearDataService>(...);
```

From watch/lib/services/connection_status_service.dart (Plan 04-02):
```dart
final connectionStatusProvider = StreamProvider<bool>(...);
```

Galaxy Watch 4 safe zone: 84dp radius. Six buttons must fit at ~26dp each in a 2x3 grid (research finding).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up app shell, theme, and hole-state provider</name>
  <files>watch/pubspec.yaml, watch/lib/main.dart, watch/lib/app.dart, watch/lib/theme/brdy_colors.dart, watch/lib/providers/hole_state_provider.dart</files>
  <action>
    Add `go_router: ^14.0.0` to `watch/pubspec.yaml` dependencies. Run `flutter pub get` in `watch/`.

    Create `watch/lib/theme/brdy_colors.dart` mirroring (but locally redeclaring; do NOT import from phone) the BRDY palette. Required constants:
    ```dart
    abstract class BrdyColors {
      static const surface = Color(0xFF1A1A1A);
      static const onSurface = Color(0xFFFFFFFF);
      static const eagleGold = Color(0xFFFFB300);
      static const birdieGreen = Color(0xFF34D399);  // green — intentional, matches phone Phase 8 flash update
      static const parBlue = Color(0xFF3B82F6);
      static const bogeySurface = Color(0xFF4B5563);
      static const doubleOrange = Color(0xFFFB923C);
      static const pickupRed = Color(0xFFEF4444);
      static const connectedGreen = Color(0xFF22C55E);
      static const disconnectedGrey = Color(0xFF6B7280);
    }
    ```
    (Executor: confirm hex codes against `lib/theme/brdy_colors.dart`; substitute the real BRDY constants if different — the names above are the contract, the codes follow phone.)

    Create `watch/lib/providers/hole_state_provider.dart`:
    ```dart
    final holeStateProvider = StreamProvider<HoleState>((ref) =>
      ref.watch(wearDataServiceProvider).holeStateStream);
    ```

    Create `watch/lib/main.dart`:
    ```dart
    void main() {
      runApp(const ProviderScope(child: BrdyWatchApp()));
    }
    ```

    Create `watch/lib/app.dart` with `class BrdyWatchApp extends ConsumerWidget`. Use GoRouter with a single route `'/'` that returns `WatchShape(builder: (_, shape, child) => ScoreScreen(shape: shape))` wrapped in an `AmbientMode(builder: (context, mode, child) => child!, child: ScoreScreen(...))`. The `MaterialApp.router` should use a dark theme with `scaffoldBackgroundColor: BrdyColors.surface`.

    Refer to `wear_plus` README for exact `WatchShape` and `AmbientMode` widget signatures (the package ships these — research confirmed).
  </action>
  <verify>
    <automated>cd /Users/simonnewland/development/brdy01/watch && flutter analyze 2>&1 | tail -3 | grep -qE "(No issues found|info)"</automated>
  </verify>
  <done>`flutter analyze` reports no errors; `main.dart` boots `ProviderScope` + `BrdyWatchApp`; theme constants exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build OutcomeButton and ConnectionDot widgets</name>
  <files>watch/lib/widgets/outcome_button.dart, watch/lib/widgets/connection_dot.dart, watch/test/widgets/outcome_button_test.dart, watch/test/widgets/connection_dot_test.dart</files>
  <behavior>
    - Test 1: `OutcomeButton(outcome: WatchOutcome.eagle, enabled: true, onTap: cb)` renders with `BrdyColors.eagleGold` background and label `EAGLE`. Tapping invokes `cb` exactly once.
    - Test 2: `OutcomeButton(enabled: false, onTap: cb)` does NOT invoke `cb` on tap and renders with reduced opacity (e.g., 0.4).
    - Test 3: `ConnectionDot(connected: true)` paints `BrdyColors.connectedGreen`; `ConnectionDot(connected: false)` paints `BrdyColors.disconnectedGrey`.
  </behavior>
  <action>
    Create `watch/lib/widgets/outcome_button.dart`:
    - `class OutcomeButton extends StatelessWidget` with fields `WatchOutcome outcome`, `bool enabled`, `VoidCallback onTap`.
    - Internal map from `WatchOutcome` -> `(Color background, String label)`:
      - eagle -> (eagleGold, 'EAGLE')
      - birdie -> (birdieGreen, 'BIRDIE')
      - par -> (parBlue, 'PAR')
      - bogey -> (bogeySurface, 'BOGEY')
      - doubleBogey -> (doubleOrange, 'DOUBLE')
      - pickup -> (pickupRed, 'PICKUP')
    - Build: a circular container ~52dp diameter (button visual; tap target ~26dp radius per research). `InkWell.onTap` wired only when `enabled`. Opacity 0.4 when disabled. Text uses a high-contrast white label, monospace font family `'SometypeMono'` (executor: declare font in `pubspec.yaml` only if assets are bundled into the watch project; otherwise fall back to `Theme.of(context).textTheme.labelSmall` with `fontFeatures: [FontFeature.tabularFigures()]`).
    - On tap, also fire haptic via `HapticFeedback.lightImpact()` before invoking `onTap`.

    Create `watch/lib/widgets/connection_dot.dart`:
    - `class ConnectionDot extends StatelessWidget` with `bool connected`.
    - 8dp circular container; colour `BrdyColors.connectedGreen` if connected, else `BrdyColors.disconnectedGrey`.
    - Semantics label: `connected ? 'Phone connected' : 'Phone disconnected'`.

    Write the three widget tests in `watch/test/widgets/`.
  </action>
  <verify>
    <automated>cd /Users/simonnewland/development/brdy01/watch && flutter test test/widgets/ 2>&1 | tail -5 | grep -q "All tests passed"</automated>
  </verify>
  <done>All widget tests pass; both widgets reusable from ScoreScreen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement ScoreScreen with ambient + offline states</name>
  <files>watch/lib/screens/score_screen.dart, watch/test/screens/score_screen_test.dart</files>
  <behavior>
    - Test 1: Given holeState (holeIndex=4, par=4), the screen displays text `'HOLE 5'` and `'PAR 4'`.
    - Test 2: Given `connectionStatusProvider` emits `true`, all six OutcomeButtons are enabled and `NO PHONE` label is absent.
    - Test 3: Given `connectionStatusProvider` emits `false`, all six OutcomeButtons are disabled and a `NO PHONE` label is visible.
    - Test 4: Tapping the EAGLE button invokes `wearDataServiceProvider`'s `sendScore` with `holeNumber=5`, `outcome=WatchOutcome.eagle`.
    - Test 5: When holeState is loading, screen shows a placeholder `'WAITING…'`.
  </behavior>
  <action>
    Create `watch/lib/screens/score_screen.dart`:
    - `class ScoreScreen extends ConsumerWidget` with optional `WearShape? shape` parameter (use shape to inset content inside the 84dp safe radius on round watches).
    - In `build`:
      - Read `final holeAsync = ref.watch(holeStateProvider);`
      - Read `final connectionAsync = ref.watch(connectionStatusProvider);`
      - Read `final isConnected = connectionAsync.maybeWhen(data: (v) => v, orElse: () => false);`
    - Layout (use `AmbientMode` from `wear_plus` to switch on mode):
      - **Active mode**: Column with three sections:
        1. Top: hole header — `holeAsync.when(data: (s) => Text('HOLE ${s.holeNumber}', style: bigMono), loading: () => Text('WAITING…'), error: (e,_) => Text('ERR'))` and below it `Text('PAR ${s.par}')`.
        2. Middle: 2x3 GridView (or two Rows) of `OutcomeButton`s in the order EAGLE, BIRDIE, PAR / BOGEY, DOUBLE, PICKUP. `onTap` for each:
           ```dart
           () async {
             final hole = holeAsync.value;
             if (hole == null) return;
             await ref.read(wearDataServiceProvider).sendScore(
               holeNumber: hole.holeNumber,
               outcome: outcome,
             );
           }
           ```
           `enabled: isConnected && holeAsync.hasValue`.
        3. Bottom-right corner overlay (`Stack` + `Positioned`): `ConnectionDot(connected: isConnected)`.
      - When `!isConnected` AND in active mode, overlay a thin `Text('NO PHONE')` line above the grid.
      - **Ambient mode**: render only `Text('HOLE ${s.holeNumber}', style: ambientStyle)` centred on a black background. No buttons, no dot. Use low-luminance white for AMOLED burn-in safety.
    - Wrap content in `Padding` so it stays inside the round-watch safe zone (~84dp radius => roughly `EdgeInsets.all(16)` on a 192dp screen). Use `shape.isRound` (or `MediaQuery.size`) heuristic to apply circular padding.

    Write screen tests in `watch/test/screens/score_screen_test.dart` using `ProviderScope(overrides: [...])` to inject mocked providers (mock `WearDataService` and override `holeStateProvider`, `connectionStatusProvider`).
  </action>
  <verify>
    <automated>cd /Users/simonnewland/development/brdy01/watch && flutter test 2>&1 | tail -5 | grep -q "All tests passed"</automated>
  </verify>
  <done>All ScoreScreen tests pass; manual `flutter run` on Wear OS emulator shows the screen with placeholder hole state and grey dot (no phone paired yet).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user finger → OutcomeButton | Disabled state must reliably gate sendScore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | accidental tap while screen loading | mitigate | Buttons disabled until `holeAsync.hasValue && isConnected`. |
| T-04-09 | Information Disclosure | hole number visible on always-on display | accept | Score info is not sensitive; ambient mode dims display per Wear OS guidelines. |
| T-04-SC | Tampering | `go_router:^14.0.0` | mitigate | First-party flutter.dev package; verified in research. |
</threat_model>

<verification>
- `flutter test` passes for all watch tests.
- `flutter run` on a Wear OS emulator boots into ScoreScreen showing `WAITING…` placeholder + grey dot.
- Manual swipe to ambient (or `adb shell am broadcast -a com.google.android.wearable.action.ENTER_AMBIENT`) collapses UI to minimal hole readout.
</verification>

<success_criteria>
- All six outcomes visible and tappable when connected.
- Connection dot reflects the streamed connection state.
- Ambient mode does not crash.
</success_criteria>

<output>
Create `.planning/phases/04-wear-os/04-03-SUMMARY.md` documenting:
- Final layout (2x3 vs 3x2, exact dp values used)
- Any colour adjustments from the BRDY palette
- Wear OS emulator profile used for visual confirmation
- Any deviations from research
</output>
