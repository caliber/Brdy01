---
phase: 04-wear-os
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - lib/features/shot_capture/shot_capture_screen.dart
  - lib/features/shot_capture/services/wear_bridge_service.dart
  - lib/features/shot_capture/providers/hole_score_notifier.dart
  - .planning/phases/04-wear-os/EMULATOR-PAIRING.md
autonomous: false
requirements: [WEAR-02, WEAR-03]

must_haves:
  truths:
    - "When a /brdy/score/... DataItem arrives on the phone, HoleScoreNotifier.recordOutcome runs exactly once for that DataItem"
    - "The same undo toast that appears on phone-tap also appears on watch-originated scores"
    - "Watch button taps while phone is unreachable are queued by DataClient and processed when phone reconnects (verified via emulator pairing)"
    - "End-to-end: tapping EAGLE on the watch produces an EAGLE entry in the phone's active Drift round"
  artifacts:
    - path: ".planning/phases/04-wear-os/EMULATOR-PAIRING.md"
      provides: "Documented adb-pairing procedure for Wear OS emulator <-> phone emulator"
      contains: "telnet localhost"
  key_links:
    - from: "lib/features/shot_capture/shot_capture_screen.dart"
      to: "WearBridgeService.scoreEvents"
      via: "ref.listen / StreamSubscription"
      pattern: "scoreEvents"
    - from: "_handleWearScore"
      to: "HoleScoreNotifier.recordOutcome"
      via: "ref.read(...).recordOutcome(outcome, par)"
      pattern: "recordOutcome\\("
---

<objective>
Wire the phone-side score ingestion: listen to the WearBridgeService score stream, dispatch each event through the same `HoleScoreNotifier.recordOutcome` path as a phone tap, surface the undo toast, and validate the end-to-end loop on paired emulators.

Purpose: Closes the loop on WEAR-02 (watch tap writes to phone Drift round) and confirms WEAR-03 (offline queuing) via a human checkpoint with real emulators.

Output: A working phone↔watch scoring system, validated by the developer at the emulator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wear-os/04-RESEARCH.md
@.planning/phases/04-wear-os/04-01-SUMMARY.md
@.planning/phases/04-wear-os/04-02-SUMMARY.md
@.planning/phases/04-wear-os/04-03-SUMMARY.md

@lib/features/shot_capture/shot_capture_screen.dart
@lib/features/shot_capture/providers/hole_score_notifier.dart
@lib/features/shot_capture/providers/active_hole_index_provider.dart
@lib/features/shot_capture/services/wear_bridge_service.dart
@lib/domain/enums/hole_outcome.dart

<interfaces>
From Plan 04-01:
- `WearBridgeService.scoreEvents` -> Stream<WearScoreEvent { int holeIndex, HoleOutcome outcome, DateTime timestamp }>
- `WearBridgeService.pushHoleState(...)` already wired into ShotCaptureScreen (Plan 04-01 Task 4).

Existing phone APIs:
- `HoleScoreNotifier.recordOutcome(HoleOutcome outcome, int par)` — performs the Drift write.
- `_handleOutcomeTapped(HoleOutcome outcome, int par, int? si)` — the screen's existing tap handler; shows undo toast.

`activeHoleIndexProvider` exposes the current 0-based hole index on the phone.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `_handleWearScore` and subscribe to scoreEvents in ShotCaptureScreen</name>
  <files>lib/features/shot_capture/shot_capture_screen.dart, test/features/shot_capture/shot_capture_screen_wear_test.dart</files>
  <behavior>
    - Test 1: When `WearBridgeService.scoreEvents` emits an event whose `holeIndex` matches `activeHoleIndexProvider`, `HoleScoreNotifier.recordOutcome` is called once with the event's outcome and the active par.
    - Test 2: When the event's `holeIndex` does NOT match the current active hole, the event is ignored (and a debug log is emitted). This prevents scoring the wrong hole if the watch is stale.
    - Test 3: Duplicate events with the same `timestamp` (within a 2 s window) are deduped — second call to `recordOutcome` is suppressed.
    - Test 4: After `recordOutcome` succeeds, the same undo toast widget shown by `_handleOutcomeTapped` appears.
  </behavior>
  <action>
    In `lib/features/shot_capture/shot_capture_screen.dart` (a `ConsumerStatefulWidget`):

    - Add a `StreamSubscription<WearScoreEvent>? _wearScoreSub;` field on the state class.
    - Add a `final Set<int> _recentWearTimestamps = <int>{};` for the dedupe window. Use a small helper `bool _isDuplicate(DateTime ts) { final ms = ts.millisecondsSinceEpoch; if (_recentWearTimestamps.contains(ms)) return true; _recentWearTimestamps.add(ms); Future.delayed(const Duration(seconds: 2), () => _recentWearTimestamps.remove(ms)); return false; }`.
    - In `initState` (after super.initState), schedule a `WidgetsBinding.instance.addPostFrameCallback((_) { ... })` to safely access `ref`:
      ```dart
      _wearScoreSub = ref.read(wearBridgeServiceProvider).scoreEvents.listen(_handleWearScore);
      ```
    - In `dispose`: `_wearScoreSub?.cancel();` before super.dispose().
    - Implement `_handleWearScore(WearScoreEvent event)`:
      ```dart
      if (_isDuplicate(event.timestamp)) return;
      final currentHoleIndex = ref.read(activeHoleIndexProvider);
      if (event.holeIndex != currentHoleIndex) {
        developer.log('Ignoring wear score for stale holeIndex ${event.holeIndex} (active=$currentHoleIndex)');
        return;
      }
      // Read the current hole's par + si the same way _handleOutcomeTapped does.
      final par = _currentPar(); // executor: extract from the existing widget state; same source _handleOutcomeTapped uses
      final si = _currentSi();
      // Delegate to existing tap handler to share undo-toast UX.
      _handleOutcomeTapped(event.outcome, par, si);
      ```

    Do NOT add a parallel write path. The reuse of `_handleOutcomeTapped` guarantees identical Drift writes, identical undo toast, identical analytics.

    If `_handleOutcomeTapped` is currently `private` and not reachable from outside the State class, that is fine — the subscription lives in the same State class.

    Add the test file. Mock `WearBridgeService` via `Provider.override` and stub `scoreEvents` with a `StreamController<WearScoreEvent>.broadcast()`. Mock `HoleScoreNotifier` to capture calls. Use the existing test harness conventions in `test/features/shot_capture/` (refer to any existing notifier test there for setup style).
  </action>
  <verify>
    <automated>cd /Users/simonnewland/development/brdy01 && flutter test test/features/shot_capture/ 2>&1 | tail -5 | grep -q "All tests passed"</automated>
  </verify>
  <done>All shot_capture tests pass including the four new wear-ingestion behaviours; existing tap-tests unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Wear OS emulator pairing workflow</name>
  <files>.planning/phases/04-wear-os/EMULATOR-PAIRING.md</files>
  <action>
    Create `.planning/phases/04-wear-os/EMULATOR-PAIRING.md` capturing the exact commands the developer will run to verify the system. Use a numbered procedure. Required content:

    1. **Boot the phone emulator**: `flutter emulators --launch Pixel_8_API_34` (or the developer's preferred AVD). Note its adb id from `adb devices`.

    2. **Create a Wear OS AVD**: Document the Android Studio path — Device Manager → Wear OS → Wear OS Large Round → API 33 (Tiramisu). Or via command line:
       ```
       avdmanager create avd -n WearLargeRound33 -k "system-images;android-33;android-wear;x86_64" -d "wearos_large_round"
       ```

    3. **Boot the watch emulator**: `emulator -avd WearLargeRound33 &`. Capture its adb id.

    4. **Pair the two emulators** (Wear OS Companion-less mode for emulator testing):
       ```
       # Forward the watch's wear-host port to the phone
       adb -s <phone-id> forward tcp:5601 tcp:5601
       adb -s <watch-id> forward tcp:5601 tcp:5601
       ```
       Open the Wear OS emulator's settings → Developer options → "Pair with emulator". The emulator handles peer discovery via the forwarded port.

       Alternative (documented for reference): the official Android Studio "Pair to Phone" flow inside the Wear OS emulator companion screen.

    5. **Install phone APK** with release signing:
       ```
       cd /Users/simonnewland/development/brdy01
       flutter run --release -d <phone-id>
       ```

    6. **Install watch APK** with the same release keystore:
       ```
       cd /Users/simonnewland/development/brdy01/watch
       flutter run --release -d <watch-id>
       ```

    7. **Verify Data Layer connectivity**:
       ```
       adb -s <phone-id> logcat -v time | grep -iE "(WearData|DataClient|brdy/hole|brdy/score)"
       ```
       Expected: a `/brdy/hole` putDataItem succeeds within 5 s of opening the ShotCapture screen.

    8. **Offline test (WEAR-03)**:
       - With watch app open, on the watch emulator extended controls disable Wi-Fi/Bluetooth (or `adb -s <watch-id> shell svc bluetooth disable`).
       - Tap EAGLE on the watch. Confirm the dot turns grey and `NO PHONE` text appears but the tap still registers a queued DataItem (visible via logcat as `putDataItem` succeeds locally — Google's DataClient buffers).
       - Re-enable connectivity. Within 10 s, the phone should log `onDataChanged` with the queued `/brdy/score/...` item and the Drift round should contain the EAGLE entry.

    Include a final "Troubleshooting" section covering:
    - Mismatched applicationId or signing key (most common failure)
    - Forgotten `com.google.android.wearable.standalone` meta-data
    - Phone manifest missing `android_wear_capabilities` resource
  </action>
  <verify>
    <automated>test -f /Users/simonnewland/development/brdy01/.planning/phases/04-wear-os/EMULATOR-PAIRING.md && grep -q "Offline test" /Users/simonnewland/development/brdy01/.planning/phases/04-wear-os/EMULATOR-PAIRING.md</automated>
  </verify>
  <done>EMULATOR-PAIRING.md exists with steps 1-8 plus troubleshooting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end human verification on paired emulators</name>
  <what-built>
    A complete phone↔watch scoring loop:
    - Phone: ShotCaptureScreen pushes hole state on every hole change, and ingests `/brdy/score/...` DataItems via WearBridgeService into HoleScoreNotifier.
    - Watch: ScoreScreen displays the current hole + par streamed from the phone, with six colour-coded buttons that send /brdy/score/... DataItems. Connection dot reflects phone reachability; buttons disable + show "NO PHONE" when disconnected.
    - Offline queuing: DataClient buffers writes from either side and auto-syncs on reconnect.
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/04-wear-os/EMULATOR-PAIRING.md` end-to-end. Concretely:

    1. Boot phone + Wear OS emulators per the pairing doc. Confirm `adb devices` shows both.
    2. Install both APKs (release-signed). Confirm both launch and the watch shows `HOLE 1` + `PAR 4` (or whatever the active round's first hole is) within 5 seconds of the phone reaching ShotCaptureScreen. Connection dot should be **green**.
    3. Tap **EAGLE** on the watch. Within 2 seconds:
       - Phone shows the same undo toast as a phone-tap.
       - Drift DB has an EAGLE row for hole 1 (verify via `adb -s <phone-id> shell run-as com.brdy.brdy01 cat databases/brdy.db | sqlite3 - 'SELECT * FROM scores ORDER BY rowid DESC LIMIT 3;'` or via the in-app history view).
    4. Advance the phone to hole 2. Watch should update to `HOLE 2` automatically (no manual sync).
    5. **Offline test (WEAR-03)**: disable Bluetooth on the watch emulator. Watch dot turns **grey**, buttons fade to disabled, `NO PHONE` appears. Tap behaviours: buttons should NOT register taps while disabled.
    6. Re-enable Bluetooth. Dot returns to **green** within 3 seconds. Tap BIRDIE. Phone records BIRDIE on hole 2 within 2 seconds.
    7. **Buffered-tap variant** (optional, validates DataClient buffering even when buttons are tappable): briefly disable Bluetooth, force-enable a tap by tapping in the moment of transition, re-enable, and confirm the score still arrives. This is best-effort — Plan 04-03 explicitly disables buttons on disconnect, so this only validates the underlying DataClient buffering of the `/brdy/hole` channel from phone -> watch.

    Expected results in summary:
    - WEAR-01: ✓ if step 2 succeeds.
    - WEAR-02: ✓ if step 3 succeeds.
    - WEAR-03: ✓ if steps 5+6 succeed.
  </how-to-verify>
  <resume-signal>Type "approved" only if ALL THREE requirements passed: WEAR-01 (step 2), WEAR-02 (step 3), WEAR-03 (steps 5+6). If only some passed, state which (e.g. "WEAR-01 and WEAR-02 pass, WEAR-03 fails at step 5") so a targeted follow-up task can be created rather than re-running everything.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incoming /brdy/score/... DataItem → phone Drift write | Untrusted-ish: any peer with same applicationId + key, but the cap is "anyone who built this app", so threat is low. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | duplicate or replayed `/brdy/score/{hole}-{ts}` items | mitigate | `_isDuplicate(ts)` dedupe + path includes timestamp; stale-hole events ignored. |
| T-04-11 | Spoofing | event for wrong hole index | mitigate | `if (event.holeIndex != currentHoleIndex) return;` |
| T-04-12 | Repudiation | watch tap recorded but phone shows no toast | mitigate | Reuses existing `_handleOutcomeTapped` path, guaranteeing identical UX as phone tap. |
| T-04-SC | Tampering | no new external packages introduced in this plan | accept | No new dependencies. |
</threat_model>

<verification>
- `flutter test` on phone passes.
- `EMULATOR-PAIRING.md` exists.
- Human checkpoint passed: all three requirements observed working on paired emulators.
</verification>

<success_criteria>
- WEAR-01, WEAR-02, WEAR-03 all observed working end-to-end.
- No regression in existing phone-tap scoring flow.
- Developer signs off on the checkpoint.
</success_criteria>

<output>
Create `.planning/phases/04-wear-os/04-04-SUMMARY.md` documenting:
- Verified emulator profiles (phone API level, watch API level)
- Observed latency phone-tap-to-DB vs watch-tap-to-DB
- Any deviations or follow-ups (e.g., real-device validation queued for a later phase)
- Phase 4 closeout note: mark ROADMAP requirements WEAR-01/02/03 as done
</output>
