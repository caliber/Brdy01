---
phase: 05-gps-voice-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/AndroidManifest.xml
  - lib/data/local/database/daos/shot_dao.dart
  - lib/data/local/database/app_database.dart
autonomous: true
requirements:
  - SHOT-08

must_haves:
  truths:
    - "GPS permission dialog appears on Android when location is first requested"
    - "ShotDao is accessible via db.shotDao in AppDatabase"
    - "ShotDao.watchShotsForHole(holeId) returns a live stream from Drift"
    - "ShotDao.insertShot persists a shot row with latitude, longitude, shotNumber, holeId, recordedAt"
    - "HoleDao.getHoleByRoundAndNumber resolves the Drift row id for a hole"
  artifacts:
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Android location permissions required for geolocator"
      contains: "ACCESS_FINE_LOCATION"
    - path: "lib/data/local/database/daos/shot_dao.dart"
      provides: "ShotDao with insertShot, watchShotsForHole, deleteShotsForHole, getShotCountForHole"
      exports: ["ShotDao"]
    - path: "lib/data/local/database/daos/shot_dao.g.dart"
      provides: "Generated Drift mixin for ShotDao"
    - path: "lib/data/local/database/app_database.dart"
      provides: "AppDatabase with ShotDao registered in daos list"
      contains: "ShotDao"
  key_links:
    - from: "lib/data/local/database/app_database.dart"
      to: "lib/data/local/database/daos/shot_dao.dart"
      via: "@DriftDatabase daos list"
      pattern: "daos.*ShotDao"
    - from: "lib/data/local/database/daos/shot_dao.dart"
      to: "lib/data/local/database/tables/shots_table.dart"
      via: "@DriftAccessor tables list"
      pattern: "DriftAccessor.*Shots"
---

<objective>
Add Android location permissions (blocking Wave 1 gate for GPS) and implement ShotDao so the shots table has a working data layer for pin persistence.

Purpose: GPS silently fails on Android without ACCESS_FINE_LOCATION. ShotDao is the persistence layer for all shot pin operations in Waves 2 and 3.
Output: AndroidManifest.xml with location permissions; shot_dao.dart + shot_dao.g.dart; AppDatabase registering ShotDao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonnewland/development/brdy01/.planning/PROJECT.md
@/Users/simonnewland/development/brdy01/.planning/ROADMAP.md
@/Users/simonnewland/development/brdy01/.planning/STATE.md

<interfaces>
From lib/data/local/database/tables/shots_table.dart:
```dart
class Shots extends Table {
  IntColumn get id => integer().autoIncrement()();
  IntColumn get holeId => integer().references(Holes, #id)();
  RealColumn get latitude => real()();
  RealColumn get longitude => real()();
  IntColumn get shotNumber => integer()();
  DateTimeColumn get recordedAt => dateTime()();
}
```

From lib/data/local/database/app_database.dart:
```dart
@DriftDatabase(tables: [Rounds, Holes, Shots], daos: [RoundDao, HoleDao])
class AppDatabase extends _$AppDatabase { ... }
```

From lib/data/local/database/daos/hole_dao.dart (model to follow exactly):
```dart
@DriftAccessor(tables: [Holes])
class HoleDao extends DatabaseAccessor<AppDatabase> with _$HoleDaoMixin {
  HoleDao(super.db);
  ...
}
```

From lib/data/local/database/daos/hole_dao.dart (add this query method):
```dart
// Needed by ShotDao to resolve holeId FK
Future<Hole?> getHoleByRoundAndNumber(int roundId, int holeNumber) =>
    (select(holes)
      ..where((h) => h.roundId.equals(roundId))
      ..where((h) => h.holeNumber.equals(holeNumber)))
    .getSingleOrNull();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Android location permissions to AndroidManifest.xml</name>
  <files>android/app/src/main/AndroidManifest.xml</files>
  <read_first>android/app/src/main/AndroidManifest.xml</read_first>
  <action>
    Read the current AndroidManifest.xml. Insert two uses-permission entries immediately after the existing MODIFY_AUDIO_SETTINGS permission line (before the application element). The two entries to add:
    - android:name="android.permission.ACCESS_FINE_LOCATION"
    - android:name="android.permission.ACCESS_COARSE_LOCATION"

    Do not add BACKGROUND_LOCATION — geolocator on-demand only needs foreground permissions. Do not move or alter any existing permission entries.

    This is the Wave 1 GPS gate. Without these entries, geolocator.checkPermission() always returns LocationPermission.denied on Android regardless of runtime grant.
  </action>
  <verify>
    <automated>grep -c 'ACCESS_FINE_LOCATION\|ACCESS_COARSE_LOCATION' /Users/simonnewland/development/brdy01/android/app/src/main/AndroidManifest.xml</automated>
    Expected output: 2
  </verify>
  <done>AndroidManifest.xml contains both ACCESS_FINE_LOCATION and ACCESS_COARSE_LOCATION uses-permission entries; existing permissions unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create ShotDao and register in AppDatabase; extend HoleDao</name>
  <files>
    lib/data/local/database/daos/shot_dao.dart,
    lib/data/local/database/app_database.dart,
    lib/data/local/database/daos/hole_dao.dart
  </files>
  <read_first>
    lib/data/local/database/daos/hole_dao.dart,
    lib/data/local/database/daos/hole_dao.g.dart,
    lib/data/local/database/app_database.dart,
    lib/data/local/database/tables/shots_table.dart
  </read_first>
  <action>
    STEP A — Create lib/data/local/database/daos/shot_dao.dart:
    Follow the exact structure of hole_dao.dart. Annotate with @DriftAccessor(tables: [Shots]). The class ShotDao extends DatabaseAccessor&lt;AppDatabase&gt; with _$ShotDaoMixin.

    Implement four methods:
    1. insertShot — requires int holeId, double latitude, double longitude, int shotNumber. Uses ShotsCompanion.insert with all four fields plus recordedAt: DateTime.now(). Returns Future&lt;void&gt; via into(shots).insert(...).
    2. watchShotsForHole(int holeId) — returns Stream&lt;List&lt;Shot&gt;&gt; from (select(shots)..where((s) => s.holeId.equals(holeId))).watch().
    3. deleteShotsForHole(int holeId) — returns Future&lt;void&gt; via (delete(shots)..where((s) => s.holeId.equals(holeId))).go().
    4. getShotCountForHole(int holeId) — returns Future&lt;int&gt; using (select(shots)..where((s) => s.holeId.equals(holeId))).get() then .length. Used by the map overlay to assign sequential shotNumber.

    Required imports: package:drift/drift.dart, ../app_database.dart, ../tables/shots_table.dart. Include the part directive for shot_dao.g.dart.

    STEP B — Extend HoleDao (hole_dao.dart):
    Add one method after the existing getHolesForRound method:
      Future&lt;Hole?&gt; getHoleByRoundAndNumber(int roundId, int holeNumber) — uses select(holes) with two where clauses (roundId + holeNumber), returns getSingleOrNull(). This is how the map overlay resolves the hole FK without bumping schema.

    STEP C — Update AppDatabase (app_database.dart):
    Add ShotDao to the @DriftDatabase annotation daos list: daos: [RoundDao, HoleDao, ShotDao].
    Add the import for shot_dao.dart alongside the existing hole_dao.dart import.
    Do NOT change schemaVersion — the shots table is already at v1. No migration case needed.

    STEP D — Run build_runner:
      dart run build_runner build --delete-conflicting-outputs
    Confirm shot_dao.g.dart is generated and app_database.g.dart is regenerated without errors.

    CRITICAL: Do not bump schemaVersion. The Shots table was already defined at v1. Adding a DAO is not a schema change. Bumping without a table/column change causes migration exceptions.
  </action>
  <verify>
    <automated>
      cd /Users/simonnewland/development/brdy01 && flutter analyze lib/data/local/database/ 2>&1 | tail -5
    </automated>
    Expected: "No issues found" or only existing warnings unrelated to database/. Generated file lib/data/local/database/daos/shot_dao.g.dart must exist.
    Also verify: grep -c 'ShotDao' /Users/simonnewland/development/brdy01/lib/data/local/database/app_database.dart
    Expected output: at least 2 (import line + daos list entry)
  </verify>
  <done>
    shot_dao.dart exists with insertShot, watchShotsForHole, deleteShotsForHole, getShotCountForHole. shot_dao.g.dart generated. AppDatabase daos list includes ShotDao. HoleDao has getHoleByRoundAndNumber. flutter analyze reports no new errors on the database directory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPS position → Drift shots table | geolocator returns system-provided coordinates; no user-typed input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | GPS coordinates in Drift | accept | Data is local-only, no sync, no cloud; local DB is app-sandboxed on Android |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all packages from prior phases |
</threat_model>

<verification>
1. grep -c 'ACCESS_FINE_LOCATION' android/app/src/main/AndroidManifest.xml — must return 1
2. grep -c 'ACCESS_COARSE_LOCATION' android/app/src/main/AndroidManifest.xml — must return 1
3. ls lib/data/local/database/daos/shot_dao.g.dart — must exist
4. flutter analyze lib/data/local/database/ — no new errors
5. grep 'ShotDao' lib/data/local/database/app_database.dart — must appear in both import and daos list
6. grep 'schemaVersion' lib/data/local/database/app_database.dart — must still be 1
</verification>

<success_criteria>
- AndroidManifest.xml contains ACCESS_FINE_LOCATION and ACCESS_COARSE_LOCATION
- ShotDao provides insertShot, watchShotsForHole, deleteShotsForHole, getShotCountForHole
- HoleDao provides getHoleByRoundAndNumber
- AppDatabase.daos includes ShotDao; schemaVersion remains 1
- build_runner generates shot_dao.g.dart without errors
- flutter analyze clean on database directory
</success_criteria>

<output>
Create /Users/simonnewland/development/brdy01/.planning/phases/05-gps-voice-polish/05-01-SUMMARY.md when done
</output>
