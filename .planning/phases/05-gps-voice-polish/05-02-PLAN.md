---
phase: 05-gps-voice-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - lib/features/shot_capture/providers/shots_for_hole_provider.dart
  - lib/features/shot_capture/widgets/map_overlay_widget.dart
  - lib/features/shot_capture/shot_capture_screen.dart
autonomous: false
requirements:
  - SHOT-08

must_haves:
  truths:
    - "A map occupies the top 36% zone of ShotCaptureScreen — same zone previously showing HoleHeader + HoleNavDrawer"
    - "Tapping the map triggers a GPS position fetch and drops a pin at the device's actual GPS location (not tap coordinate)"
    - "GPS pins are rendered as accent-coloured circles on the map"
    - "Pins persist in Drift and reappear on map rebuild (survives hot restart)"
    - "When location permission is denied, a snackbar appears and no pin is dropped"
    - "When GPS times out, a 'GPS unavailable — try again' snackbar appears and no pin is dropped"
    - "Map shows a black loading placeholder until courseForRoundProvider resolves — no null LatLng crash"
    - "HoleHeader and HoleNavDrawer remain accessible (move into BottomZone or keep in layout per research pattern)"
  artifacts:
    - path: "lib/features/shot_capture/providers/shots_for_hole_provider.dart"
      provides: "shotsForHoleProvider stream — watches shots from Drift for active hole"
      exports: ["shotsForHoleProvider"]
    - path: "lib/features/shot_capture/providers/shots_for_hole_provider.g.dart"
      provides: "Generated Riverpod provider"
    - path: "lib/features/shot_capture/widgets/map_overlay_widget.dart"
      provides: "MapOverlayWidget — FlutterMap with FMTC TileLayer + MarkerLayer + tap handler"
      exports: ["MapOverlayWidget"]
    - path: "lib/features/shot_capture/shot_capture_screen.dart"
      provides: "ShotCaptureScreen with map overlay in top zone, GPS pin-drop logic, permission handling"
  key_links:
    - from: "lib/features/shot_capture/widgets/map_overlay_widget.dart"
      to: "lib/features/shot_capture/providers/shots_for_hole_provider.dart"
      via: "ref.watch(shotsForHoleProvider) in MapOverlayWidget"
      pattern: "shotsForHoleProvider"
    - from: "lib/features/shot_capture/shot_capture_screen.dart"
      to: "lib/data/local/database/daos/shot_dao.dart"
      via: "db.shotDao.insertShot via _dropPinAtCurrentPosition"
      pattern: "shotDao.insertShot"
    - from: "lib/features/shot_capture/shot_capture_screen.dart"
      to: "lib/features/shot_capture/widgets/map_overlay_widget.dart"
      via: "MapOverlayWidget in _TopZone replacing static zone"
      pattern: "MapOverlayWidget"
---

<objective>
Build the GPS map overlay for ShotCaptureScreen: a tap-to-drop-pin map that shows the golfer's shot positions as orange dots, backed by shotsForHoleProvider watching the shots table.

Purpose: SHOT-08 — golfer taps the map to record GPS shot positions that survive app restart. All pin drops go to Drift write-through via ShotDao.
Output: MapOverlayWidget, shotsForHoleProvider, GPS pin-drop logic wired into ShotCaptureScreen top zone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonnewland/development/brdy01/.planning/PROJECT.md
@/Users/simonnewland/development/brdy01/.planning/ROADMAP.md
@/Users/simonnewland/development/brdy01/.planning/STATE.md
@/Users/simonnewland/development/brdy01/.planning/phases/05-gps-voice-polish/05-01-SUMMARY.md

<interfaces>
From lib/features/shot_capture/shot_capture_screen.dart — _TopZone widget (current state, to be modified):
```dart
class _TopZone extends StatelessWidget {
  final int roundId;
  final int highestScoredHoleIndex;
  final bool navStripOpen;
  final VoidCallback onHoleNumberTap;
  // Renders HoleHeader + HoleNavDrawer stacked in a Column
}
```

From lib/features/shot_capture/providers/course_for_round_provider.dart — async provider of CourseModel:
```dart
// courseForRoundProvider(roundId) is AsyncNotifier<CourseModel?>
// use: final courseAsync = ref.watch(courseForRoundProvider(roundId))
// initial frame: courseAsync.valueOrNull is null — never pass null to MapOptions.initialCenter
```

From lib/data/local/database/app_database.dart (after 05-01):
```dart
// db.shotDao is available
// db.holeDao.getHoleByRoundAndNumber(roundId, holeNumber) returns Future<Hole?>
```

From lib/data/local/database/daos/shot_dao.dart (created in 05-01):
```dart
Future<void> insertShot({required int holeId, required double latitude, required double longitude, required int shotNumber})
Stream<List<Shot>> watchShotsForHole(int holeId)
Future<int> getShotCountForHole(int holeId)
```

From lib/theme/brdy_colors.dart:
```dart
// BrdyColors.accent = Color(0xFFE8520A)
// BrdyColors.background = black
```

FMTC tile provider pattern (already used in TileCacheRepository):
```dart
// Verify exact method name against flutter_map_tile_caching ^9 before using.
// Likely: FMTCStore('brdy_tiles').getTileProvider()
// AppConstants.tileCacheStoreName holds the store name — read that file before using.
```

flutter_map ^7 options pattern:
```dart
MapOptions(
  initialCenter: LatLng(lat, lng),  // NOT MapOptions.center (v6 API, removed in v7)
  initialZoom: 17.0,
  onTap: (tapPosition, latlng) => callback(),  // tap triggers GPS fetch, NOT pin at latlng
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shotsForHoleProvider watching shots for active hole</name>
  <files>
    lib/features/shot_capture/providers/shots_for_hole_provider.dart
  </files>
  <read_first>
    lib/features/shot_capture/providers/hole_list_provider.dart,
    lib/features/shot_capture/providers/hole_list_provider.g.dart,
    lib/data/local/database/app_database_provider.dart
  </read_first>
  <action>
    Create shots_for_hole_provider.dart following the @riverpod auto-dispose stream pattern used by other screen-level providers.

    The provider signature is: shotsForHole(ShotsForHoleRef ref, int roundId, int holeIndex).
    It returns Stream&lt;List&lt;Shot&gt;&gt;.

    Implementation steps inside the provider:
    1. Watch appDatabaseProvider to get db.
    2. Compute holeNumber = holeIndex + 1.
    3. Watch db.holeDao.watchHolesForRound(roundId) as a stream.
    4. Use asyncExpand to find the hole where holeNumber matches. If no hole row exists yet (unscored hole), yield an empty list immediately — do NOT crash or wait.
    5. When a hole row is found, switchMap/yield* to db.shotDao.watchShotsForHole(hole.id).

    Required imports: riverpod_annotation, app_database_provider, app_database (for Shot type). Include the part directive for shots_for_hole_provider.g.dart.

    After creating the file, run build_runner to generate shots_for_hole_provider.g.dart:
      dart run build_runner build --delete-conflicting-outputs

    CONSTRAINT: This is a screen-level provider — use @riverpod (auto-dispose), NOT @Riverpod(keepAlive: true).
  </action>
  <verify>
    <automated>
      ls /Users/simonnewland/development/brdy01/lib/features/shot_capture/providers/shots_for_hole_provider.g.dart
    </automated>
    Expected: file exists. Also: flutter analyze lib/features/shot_capture/providers/shots_for_hole_provider.dart — no errors.
  </verify>
  <done>shots_for_hole_provider.dart + .g.dart exist; provider returns Stream&lt;List&lt;Shot&gt;&gt;; auto-dispose; flutter analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build MapOverlayWidget and wire GPS pin-drop into ShotCaptureScreen</name>
  <files>
    lib/features/shot_capture/widgets/map_overlay_widget.dart,
    lib/features/shot_capture/shot_capture_screen.dart
  </files>
  <read_first>
    lib/features/shot_capture/shot_capture_screen.dart,
    lib/features/shot_capture/widgets/fairway_gir_toggles.dart,
    lib/data/local/database/app_database_provider.dart
  </read_first>
  <action>
    STEP A — Create lib/features/shot_capture/widgets/map_overlay_widget.dart:

    MapOverlayWidget is a ConsumerStatefulWidget. Constructor params: int roundId, int holeIndex, CourseModel? course (nullable — null during async load).

    When course is null: render a Container filled with BrdyColors.background (black). Do not touch FlutterMap or LatLng. This prevents the Null LatLng crash documented in RESEARCH Pitfall 4.

    When course is not null:
    - Watch shotsForHoleProvider(roundId, holeIndex) — use AsyncValue.when, show empty markers during loading.
    - Resolve FMTC tile provider: read TileCacheRepository source (lib/infrastructure/tile_cache_repository.dart) to confirm the exact FMTCStore method name before using. The store name is in AppConstants — read that file to get the constant.
    - Render FlutterMap with MapOptions(initialCenter: LatLng(course.holes[holeIndex].latitude ?? fallback, ...), initialZoom: 17.0, onTap: (_, __) => onMapTapped()). Use a fallback LatLng(-36.86, 174.76) (Auckland) if hole coordinates are null.
    - Children: TileLayer using FMTC provider, MarkerLayer mapping shots to Marker(width:24, height:24, child: Icon(Icons.circle, color: BrdyColors.accent, size:16)).
    - Expose onMapTapped as a VoidCallback constructor parameter — the screen provides this.

    STEP B — Modify ShotCaptureScreen (_ShotCaptureScreenState):

    Add _dropPinAtCurrentPosition() as an async method on _ShotCaptureScreenState:
    1. Call Geolocator.checkPermission(). If denied, call Geolocator.requestPermission(). If still denied or deniedForever, show a ScaffoldMessenger snackbar with text 'LOCATION PERMISSION REQUIRED' (BrdyColors.accent text color, brutalist style) and return.
    2. If deniedForever, call Geolocator.openAppSettings() after the snackbar.
    3. Call Geolocator.getCurrentPosition(desiredAccuracy: LocationAccuracy.high) with a timeout. Verify the exact timeout parameter name against geolocator ^12 API (check pubspec.lock or pub.dev) before writing — it may be timeLimit or a LocationSettings object. Use try/catch for TimeoutException. On timeout, show snackbar 'GPS UNAVAILABLE — TRY AGAIN' and return.
    4. On success: fetch holeId via db.holeDao.getHoleByRoundAndNumber(widget.roundId, holeIndex + 1). If holeId is null (unscored hole), ensure hole row exists by calling db.holeDao.insertOrUpdateHole with roundId, holeNumber, and holePar (Value(holePar)), outcome: const Value(null), to satisfy the FK constraint (RESEARCH Pitfall 3).
    5. Fetch shotNumber = await db.shotDao.getShotCountForHole(holeId) + 1.
    6. Call db.shotDao.insertShot(holeId: holeId, latitude: pos.latitude, longitude: pos.longitude, shotNumber: shotNumber).

    Update _TopZone to include MapOverlayWidget. The _TopZone currently renders HoleHeader + HoleNavDrawer in a Column. Replace or restructure so that:
    - MapOverlayWidget fills the available height in _TopZone, receiving roundId, holeIndex, courseAsync.valueOrNull, and onMapTapped: _dropPinAtCurrentPosition.
    - HoleHeader and HoleNavDrawer are overlaid on the map (Stack widget) so hole number and nav strip remain visible.

    IMPORTANT: Pass the courseAsync value from the parent _ShotCaptureScreenState to _TopZone. _TopZone already receives highestScoredHoleIndex — add courseModel parameter alongside it, sourced from courseAsync.valueOrNull.

    Add required imports to shot_capture_screen.dart: flutter_map, latlong2, geolocator, the new MapOverlayWidget.

    After edits, run: flutter analyze lib/features/shot_capture/ — resolve all new errors.
  </action>
  <verify>
    <automated>
      cd /Users/simonnewland/development/brdy01 && flutter analyze lib/features/shot_capture/ 2>&1 | tail -10
    </automated>
    Expected: no errors. Warnings about deprecated APIs acceptable if pre-existing.
  </verify>
  <done>
    MapOverlayWidget renders map with shot pins from Drift; ShotCaptureScreen._dropPinAtCurrentPosition handles permission denied, permission deniedForever, GPS timeout, and success paths; pins written to Drift via ShotDao; flutter analyze clean on shot_capture directory.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    GPS map overlay on ShotCaptureScreen. FlutterMap occupies the top zone. Tapping the map drops a pin at your device's GPS position (accent orange circle). Pins persist across hot restart. Permission and GPS error states show snackbars.
  </what-built>
  <how-to-verify>
    1. Run: flutter run -d &lt;android-device&gt; --dart-define=GOLF_API_KEY=&lt;key&gt;
    2. Start a round and navigate to ShotCaptureScreen.
    3. Verify: Top zone shows a map (OSM tiles or grey tiles if offline) — NOT blank/black.
    4. Tap the map. Verify: location permission dialog appears (first time).
    5. Grant permission and tap again. Verify: an orange dot appears on the map within ~15 seconds.
    6. Hot restart the app and return to the same hole. Verify: the orange dot is still visible (pin survived restart).
    7. Enable airplane mode and tap the map. Verify: map shows cached tiles (or grey), GPS shows snackbar 'GPS UNAVAILABLE — TRY AGAIN'.
    8. Deny location permission in device settings (Settings > Apps > brdy01 > Permissions > Location > Deny). Tap map. Verify: snackbar 'LOCATION PERMISSION REQUIRED' appears; no crash.
    9. Verify: outcome buttons, fairway/GIR toggles, and NEXT button still work normally.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS GPS → Flutter app | GPS coordinates from system; user cannot manipulate these values |
| Map tile network → TileLayer | OSM tile fetches; FMTC caches; no user data transmitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | GPS coordinates stored in local Drift | accept | App-sandboxed local DB; no cloud sync; ASVS V1 not applicable (local only) |
| T-05-03 | Denial of Service | GPS timeout blocks UI | mitigate | 15s timeout with try/catch; snackbar shown; pin not dropped; UI remains interactive |
| T-05-04 | Tampering | Malformed shot row from getShotCountForHole | accept | shotNumber is informational only (decorative pin label in future phases); no scoring impact |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. flutter analyze lib/features/shot_capture/ — no new errors
2. shots_for_hole_provider.g.dart exists
3. grep -c 'shotsForHoleProvider' lib/features/shot_capture/widgets/map_overlay_widget.dart — at least 1
4. grep -c 'ACCESS_FINE_LOCATION' android/app/src/main/AndroidManifest.xml — 1 (from Plan 01)
5. Human verify checkpoint passed
</verification>

<success_criteria>
- Map visible in top zone of ShotCaptureScreen on Android device
- Tapping map drops orange pin at device GPS location
- Pins survive hot restart (Drift write-through confirmed)
- Permission denied path shows snackbar without crash
- GPS timeout path shows snackbar without crash
- Outcome buttons remain fully functional
- flutter analyze clean
</success_criteria>

<output>
Create /Users/simonnewland/development/brdy01/.planning/phases/05-gps-voice-polish/05-02-SUMMARY.md when done
</output>
