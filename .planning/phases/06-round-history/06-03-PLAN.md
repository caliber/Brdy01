---
phase: 06-round-history
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - lib/features/round_review/round_review_screen.dart
  - lib/app/router.dart
autonomous: false
requirements:
  - HIST-02

must_haves:
  truths:
    - "RoundReviewScreen accepts readOnly: bool constructor parameter (default false)"
    - "In readOnly mode: back button is shown instead of Share + New Round buttons"
    - "In readOnly mode: PopScope(canPop: true) so the user can navigate back to history"
    - "Post-round mode is completely unchanged — existing Share + New Round flow unaffected"
    - "Router /round-review/:roundId passes readOnly=true from query parameter"
    - "User can open a past round, scroll the scorecard, and return to history list"
  artifacts:
    - path: "lib/features/round_review/round_review_screen.dart"
      provides: "readOnly constructor param; conditional _ActionButtons vs _BackButton"
      contains: "readOnly"
    - path: "lib/app/router.dart"
      provides: "/round-review/:roundId passes readOnly query param to constructor"
      contains: "readOnly"
  key_links:
    - from: "lib/features/round_history/widgets/round_history_tile.dart"
      to: "lib/features/round_review/round_review_screen.dart"
      via: "context.push('/round-review/$id?readOnly=true') → RoundReviewScreen(readOnly: true)"
      pattern: "readOnly.*true"
    - from: "lib/features/round_review/round_review_screen.dart"
      to: "PopScope"
      via: "canPop: widget.readOnly"
      pattern: "widget.readOnly"
---

<objective>
Add readOnly mode to RoundReviewScreen and wire the router to pass it; then human-verify the full history flow end-to-end.

Purpose: Wave 2 wired tile taps to /round-review/:id?readOnly=true but RoundReviewScreen doesn't yet accept that parameter. Without this wave, the router either discards the flag or throws a compile error. The readOnly parameter also unblocks the PopScope trap (canPop: false currently traps users in history-viewed scorecards with no escape).

Output: RoundReviewScreen with readOnly constructor param; router wired to pass it; post-round mode verified unchanged; human-verify of the full history-browse-view-delete flow.
</objective>

<execution_context>
@/Users/simonnewland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonnewland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonnewland/development/brdy01/.planning/PROJECT.md
@/Users/simonnewland/development/brdy01/.planning/ROADMAP.md
@/Users/simonnewland/development/brdy01/.planning/STATE.md
@/Users/simonnewland/development/brdy01/.planning/phases/06-round-history/06-02-SUMMARY.md

<interfaces>
<!-- Existing RoundReviewScreen contract — must be extended, not broken. -->

Current RoundReviewScreen (from round_review_screen.dart):
  class RoundReviewScreen extends ConsumerStatefulWidget {
    final int roundId;
    const RoundReviewScreen({super.key, required this.roundId});
  }

  // In build():
  return PopScope(
    canPop: false,                             // must become: canPop: widget.readOnly
    child: Scaffold(
      ...
      SliverToBoxAdapter(
        child: _ActionButtons(                 // must become conditional
          onShare: _handleShare,
          onStartNewRound: _handleStartNewRound,
        ),
      ),
    ),
  );

  // _ActionButtons (existing — preserve unchanged for post-round mode):
  class _ActionButtons extends StatelessWidget {
    final VoidCallback onShare;
    final VoidCallback onStartNewRound;
    // renders Share + Start New Round buttons
  }

Router /round-review/:roundId (current — needs readOnly wired in):
  GoRoute(
    path: '/round-review/:roundId',
    builder: (_, state) => RoundReviewScreen(
      roundId: int.parse(state.pathParameters['roundId']!),
      // readOnly not yet passed — Wave 3 adds it
    ),
  )

Navigation TO review from RoundHistoryTile (Wave 2):
  context.push('/round-review/${round.id}?readOnly=true')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add readOnly param to RoundReviewScreen and wire router</name>
  <read_first>
    lib/features/round_review/round_review_screen.dart
    lib/app/router.dart
  </read_first>
  <files>
    lib/features/round_review/round_review_screen.dart
    lib/app/router.dart
  </files>
  <action>
    --- round_review_screen.dart ---

    1. Add readOnly field to RoundReviewScreen:
       - Add `final bool readOnly;` after the existing `final int roundId;` field
       - Add `this.readOnly = false` to the constructor with a default of false — this preserves all existing call sites which pass only roundId

    2. Change PopScope canPop:
       - Change `canPop: false` to `canPop: widget.readOnly`
       - When readOnly is false (post-round): canPop is false — existing back-block behaviour preserved
       - When readOnly is true (history): canPop is true — user can navigate back

    3. Make the bottom action area conditional:
       - In the slivers list, find the SliverToBoxAdapter that wraps _ActionButtons
       - Replace the static _ActionButtons widget with a ternary:
           widget.readOnly ? const _BackButton() : _ActionButtons(onShare: _handleShare, onStartNewRound: _handleStartNewRound)

    4. Add a _BackButton private class at the bottom of the file (after _ActionButtons):
       - StatelessWidget
       - Renders a single OutlinedButton with label 'BACK'
       - onPressed: uses Navigator.of(context).pop() — NOT context.go (preserves back-stack)
       - Style matches existing OutlinedButton in _ActionButtons: BorderSide color BrdyColors.divider, minimumSize Size.fromHeight(52)
       - Label text: GoogleFonts.sometypeMono size 16 weight w700 BrdyColors.onSurface
       - Wrap in Padding with same horizontal + vertical padding as _ActionButtons (BrdySpacing.md horizontal, BrdySpacing.lg vertical)

    5. The _screenshotKey and _shareService fields remain on the state class — they are still used when readOnly is false. Do not remove them.

    --- router.dart ---
    Update the /round-review/:roundId route builder to pass the readOnly query parameter:
      builder: (_, state) => RoundReviewScreen(
        roundId: int.parse(state.pathParameters['roundId']!),
        readOnly: state.uri.queryParameters['readOnly'] == 'true',
      )
    This replaces the current builder that only passes roundId (or the TODO placeholder left by Wave 2).

    No other router changes are needed in this wave.
  </action>
  <verify>
    <automated>grep -n "readOnly\|canPop.*widget.readOnly\|_BackButton" /Users/simonnewland/development/brdy01/lib/features/round_review/round_review_screen.dart && grep -n "readOnly.*queryParameters" /Users/simonnewland/development/brdy01/lib/app/router.dart</automated>
  </verify>
  <done>RoundReviewScreen has readOnly constructor param defaulting to false; PopScope uses widget.readOnly; _BackButton exists; router passes readOnly from query string; flutter analyze exits with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 6 round history flow:
    - Setup screen HISTORY button (Wave 2)
    - RoundHistoryScreen with list of completed rounds or empty state (Wave 2)
    - Swipe-to-delete with confirmation dialog (Wave 2)
    - Tap into past round → read-only scorecard (Wave 3)
    - Back navigation from read-only review → history list (Wave 3)
    - Post-round flow unchanged: Share + New Round still work (Wave 3)
  </what-built>
  <how-to-verify>
    Run the app (flutter run) with at least one completed round in the database.

    1. Setup screen entry point:
       - Open the app to the Setup screen
       - Confirm a 'ROUND HISTORY' button appears below the LOAD button
       - Tap it — confirm navigation to RoundHistoryScreen (title: ROUND HISTORY in AppBar)

    2. Round history list:
       - Confirm completed rounds appear in the list newest-first
       - Each tile shows: date in format like "19 May 2026", course name, and a shot count / score-to-par
       - Confirm '--' placeholders show briefly while stats load (or immediately if fast enough)

    3. Tap into past round:
       - Tap a round tile
       - Confirm RoundReviewScreen opens showing the scorecard and stats
       - Confirm NO Share Scorecard button and NO Start New Round button are visible
       - Confirm a BACK button is visible at the bottom
       - Tap BACK — confirm navigation returns to RoundHistoryScreen (not Setup)

    4. Swipe-to-delete:
       - Swipe a tile from right to left
       - Confirm a red delete background with a delete icon appears
       - Confirm 'DELETE ROUND?' dialog appears with course name, CANCEL and DELETE actions
       - Tap CANCEL — confirm tile snaps back; round still in list
       - Swipe again and tap DELETE — confirm round is removed from the list immediately

    5. Post-round flow regression (complete a new round if possible, or navigate directly to /round-review/:id):
       - Open a round review via the normal post-round flow
       - Confirm Share Scorecard button is visible
       - Confirm Start New Round button is visible
       - Confirm the back hardware/gesture button does NOT navigate away (canPop still false in post-round mode)

    6. Empty state (if no rounds in database):
       - Confirm 'NO ROUNDS YET' text is shown instead of a list
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query parameter → RoundReviewScreen readOnly | readOnly is a display mode flag, not an access control gate — no sensitive data at risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | PopScope canPop regression — post-round mode inadvertently made poppable | mitigate | readOnly defaults to false; existing RoundReviewScreen call sites (from round_complete flow via context.go) pass no readOnly, so canPop stays false |
| T-06-07 | Spoofing | User manually navigates to /round-review/123?readOnly=false and gets Share/New Round on a completed round | accept | Local-only app; no privileged action available from those buttons that isn't already accessible; not a meaningful threat |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
1. `grep "readOnly" lib/features/round_review/round_review_screen.dart` — at least 4 occurrences (field, constructor, canPop, ternary)
2. `grep "canPop: widget.readOnly" lib/features/round_review/round_review_screen.dart` — exact match
3. `grep "_BackButton" lib/features/round_review/round_review_screen.dart` — class exists
4. `grep "readOnly.*queryParameters" lib/app/router.dart` — router wired
5. `flutter analyze` exits with no errors
6. Human-verify checkpoint passed
</verification>

<success_criteria>
- RoundReviewScreen(readOnly: true) shows _BackButton; no Share or New Round buttons visible
- RoundReviewScreen(readOnly: false) shows _ActionButtons exactly as before; canPop is false
- Back button in read-only mode returns to RoundHistoryScreen via Navigator.pop
- Router passes readOnly from ?readOnly=true query parameter
- flutter analyze passes
- Human-verify: all six flow checks confirmed approved
</success_criteria>

<output>
Create /Users/simonnewland/development/brdy01/.planning/phases/06-round-history/06-03-SUMMARY.md when done.
</output>
