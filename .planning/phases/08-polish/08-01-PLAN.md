---
phase: 08-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/shot_capture/shot_capture_screen.dart
autonomous: true
requirements: []
must_haves:
  truths:
    - "Eagle tap produces a celebratory success haptic distinct from all other outcomes"
    - "Birdie tap produces a heavy impact haptic — stronger than par"
    - "Par tap produces a medium impact haptic — neutral"
    - "Bogey tap produces a light impact haptic — softer than par"
    - "Double bogey tap produces a warning haptic — perceptibly different from bogey"
    - "Pickup tap produces a rigid haptic — distinct stop-pattern unlike any score outcome"
  artifacts:
    - path: "lib/features/shot_capture/shot_capture_screen.dart"
      provides: "Per-outcome haptic dispatch replacing generic mediumImpact"
      contains: "Haptics.vibrate"
  key_links:
    - from: "_handleOutcomeTapped"
      to: "Haptics.vibrate"
      via: "switch on HoleOutcome"
      pattern: "Haptics\\.vibrate"
---

<objective>
Replace the single `HapticFeedback.mediumImpact()` call in `_handleOutcomeTapped` with a per-outcome `Haptics.vibrate(HapticsType.*)` dispatch so each scoring outcome produces a distinct tactile pattern.

Purpose: Golfers score in gloves, eyes on the course — haptic differentiation gives immediate physical confirmation of which outcome was registered without looking at the screen.
Output: Modified `shot_capture_screen.dart` with per-outcome haptic mapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/simonnewland/development/brdy01/.planning/PROJECT.md
@/Users/simonnewland/development/brdy01/.planning/ROADMAP.md
@/Users/simonnewland/development/brdy01/.planning/phases/08-polish/08-RESEARCH.md

<interfaces>
<!-- Key identifiers the executor needs. Extracted from codebase. -->

From lib/features/shot_capture/shot_capture_screen.dart:
- `_handleOutcomeTapped(HoleOutcome outcome, int par, int? si)` — async method in `_ShotCaptureScreenState`
- Current haptic call on line 264: `await HapticFeedback.mediumImpact();`
- Existing import: `import 'package:flutter/services.dart';` — provides `HapticFeedback` (keep, used by chevrons/NEXT/undo)

From lib/domain/enums/hole_outcome.dart:
- `enum HoleOutcome { eagle, birdie, par, bogey, doubleBogey, pickup }`

From pubspec.yaml (already resolved):
- `haptic_feedback: ^0.5.0+2` — provides `Haptics.vibrate(HapticsType.*)` and `HapticsType` enum
- Import: `import 'package:haptic_feedback/haptic_feedback.dart';`
- API: `await Haptics.vibrate(HapticsType.success)` — async, matches existing await pattern
- HapticsType values available: success, warning, error, light, medium, heavy, rigid, soft, selection
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire per-outcome haptic patterns in _handleOutcomeTapped</name>
  <files>lib/features/shot_capture/shot_capture_screen.dart</files>
  <action>
Add `import 'package:haptic_feedback/haptic_feedback.dart';` to the import block in shot_capture_screen.dart (after the existing `flutter/services.dart` import — keep both, they provide different classes: `Haptics` vs `HapticFeedback`).

In `_ShotCaptureScreenState._handleOutcomeTapped`, replace the single line:
  `await HapticFeedback.mediumImpact();`
with:
  `await Haptics.vibrate(switch (outcome) {`
  `  HoleOutcome.eagle       => HapticsType.success,`
  `  HoleOutcome.birdie      => HapticsType.heavy,`
  `  HoleOutcome.par         => HapticsType.medium,`
  `  HoleOutcome.bogey       => HapticsType.light,`
  `  HoleOutcome.doubleBogey => HapticsType.warning,`
  `  HoleOutcome.pickup      => HapticsType.rigid,`
  `});`

Do NOT remove `import 'package:flutter/services.dart'` — it is still used by `HapticFeedback.lightImpact()` in `_handleNext`, `_handleUndo`, and chevron taps in `hole_header.dart`.

Do NOT wrap in a try/catch — `Haptics.vibrate` is documented as safe on all platforms; let any platform error surface during testing.

Do NOT call `Haptics.canVibrate()` on every tap — this adds async overhead before the Drift write.
  </action>
  <verify>
    <automated>grep -n "Haptics.vibrate" /Users/simonnewland/development/brdy01/lib/features/shot_capture/shot_capture_screen.dart</automated>
  </verify>
  <done>
`shot_capture_screen.dart` contains `Haptics.vibrate(switch (outcome) {` with all six HoleOutcome cases mapped to distinct HapticsType values. `HapticFeedback.mediumImpact()` no longer appears in `_handleOutcomeTapped`. Both imports (`flutter/services.dart` and `haptic_feedback/haptic_feedback.dart`) are present. `flutter analyze` reports no new errors on this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter → device haptic API | Platform call with no user input; no trust boundary concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | Haptics.vibrate on rapid double-tap | accept | Flutter event loop naturally queues taps; no loop or timer involved |
</threat_model>

<verification>
Run `flutter analyze lib/features/shot_capture/shot_capture_screen.dart` — zero new errors or warnings.
</verification>

<success_criteria>
Each of the six HoleOutcome values triggers a distinct HapticsType. The existing `HapticFeedback` calls on chevrons, NEXT, and undo are unchanged. Build succeeds with no analyzer warnings.
</success_criteria>

<output>
Create `.planning/phases/08-polish/08-01-SUMMARY.md` when done.
</output>
