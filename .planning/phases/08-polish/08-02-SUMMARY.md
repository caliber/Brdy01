---
phase: 08-polish
plan: "08-02"
status: complete
completed: "2026-05-20"
---

# Phase 08 Plan 02 — Summary

**Plan:** Score Counter Flash Animation
**Status:** Complete

## What was built

- `lib/features/shot_capture/providers/last_scored_outcome_provider.dart` — Auto-dispose family provider holding current flash outcome (null between animations)
- `lib/features/shot_capture/providers/last_scored_outcome_provider.g.dart` — Generated by build_runner
- `lib/features/shot_capture/widgets/hole_header.dart` — 100sp shot-counter Text wrapped with `.animate(key: ValueKey(flashOutcome)).tint()` chain; `_outcomeFlashColor` static helper added
- `lib/features/shot_capture/shot_capture_screen.dart` — `lastScoredOutcomeProvider` set after Drift write in `_handleOutcomeTapped`

## Decisions made

- Provider resets to null via `.callback()` at end of animation (null→outcome→null key cycle guarantees re-fire)
- Provider NOT set in voice handler to avoid conflict with hole transition
- 300ms fade-in, 200ms fade-out timing per plan spec

## Verification

- `flutter analyze lib/features/shot_capture/` clean (15 pre-existing issues, zero new)
- Generated `.g.dart` present at `lib/features/shot_capture/providers/last_scored_outcome_provider.g.dart`

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED

- `lib/features/shot_capture/providers/last_scored_outcome_provider.dart` — FOUND
- `lib/features/shot_capture/providers/last_scored_outcome_provider.g.dart` — FOUND
- `lib/features/shot_capture/widgets/hole_header.dart` — modified, contains `lastScoredOutcomeProvider` and `ValueKey(flashOutcome)`
- `lib/features/shot_capture/shot_capture_screen.dart` — modified, contains `lastScoredOutcomeProvider` import and set call
- Commit 532ce13 — FOUND
